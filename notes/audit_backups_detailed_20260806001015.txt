=== AUDIT BACKUPS – DETAILED LINE CONTEXT ===

--- FILE: godot_project/scripts/build_terrain.gd.bak.20260718141333 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
1527-	print("[VERBATIM] POLL: _poll_controls() entered, game_state=", _game_state)
1528-
1529-	if _game_state == GameState.IN_PLANE:
1530:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J)
1531-		if _exit_pressed:
1532-			print("[DIAG] _poll_controls: exit aircraft triggered")
1533-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
807-	return null
808-
809-
810:func _deploy_canopy() -> void:
811-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
812-	if _canopy_deployed or not _canopy_instance:
813-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1561-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1562-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1563-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1564:		_deploy_canopy()
1565-
1566-	if _game_state == GameState.DIAGNOSIS:
1567-		var turn_input := 0.0
--
1584-		# ----- Direct key checks (bypass Input Map) -----
1585-		# Deploy (SPACE)
1586-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1587:		_deploy_canopy()
1588-	# Turn left (Q)
1589-	if Input.is_key_pressed(KEY_Q):
1590-		_turn_input = -1.0
--
1615-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1616-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1617-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1618:			_deploy_canopy()
1619-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
532-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
533-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
534-	# Headless auto‑start: simulate SPACE press
535:	if OS.get_environment("GODOT_HEADLESS") == "1":
536-		Input.action_press("ui_accept")
537-		Input.action_release("ui_accept")
538-		print("[VERBATIM] Headless auto‑start triggered.")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260718141333 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260718141803 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
1527-	print("[VERBATIM] POLL: _poll_controls() entered, game_state=", _game_state)
1528-
1529-	if _game_state == GameState.IN_PLANE:
1530:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J)
1531-		if _exit_pressed:
1532-			print("[DIAG] _poll_controls: exit aircraft triggered")
1533-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
807-	return null
808-
809-
810:func _deploy_canopy() -> void:
811-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
812-	if _canopy_deployed or not _canopy_instance:
813-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1561-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1562-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1563-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1564:		_deploy_canopy()
1565-
1566-	if _game_state == GameState.DIAGNOSIS:
1567-		var turn_input := 0.0
--
1584-		# ----- Direct key checks (bypass Input Map) -----
1585-		# Deploy (SPACE)
1586-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1587:		_deploy_canopy()
1588-	# Turn left (Q)
1589-	if Input.is_key_pressed(KEY_Q):
1590-		_turn_input = -1.0
--
1615-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1616-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1617-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1618:			_deploy_canopy()
1619-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
532-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
533-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
534-	# Headless auto‑start: simulate SPACE press
535:	if OS.get_environment("GODOT_HEADLESS") == "1":
536-		Input.action_press("ui_accept")
537-		Input.action_release("ui_accept")
538-		print("[VERBATIM] Headless auto‑start triggered.")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260718141803 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260718162836 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
1527-	print("[VERBATIM] POLL: _poll_controls() entered, game_state=", _game_state)
1528-
1529-	if _game_state == GameState.IN_PLANE:
1530:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J)
1531-		if _exit_pressed:
1532-			print("[DIAG] _poll_controls: exit aircraft triggered")
1533-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
807-	return null
808-
809-
810:func _deploy_canopy() -> void:
811-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
812-	if _canopy_deployed or not _canopy_instance:
813-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1561-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1562-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1563-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1564:		_deploy_canopy()
1565-
1566-	if _game_state == GameState.DIAGNOSIS:
1567-		var turn_input := 0.0
--
1584-		# ----- Direct key checks (bypass Input Map) -----
1585-		# Deploy (SPACE)
1586-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1587:		_deploy_canopy()
1588-	# Turn left (Q)
1589-	if Input.is_key_pressed(KEY_Q):
1590-		_turn_input = -1.0
--
1615-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1616-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1617-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1618:			_deploy_canopy()
1619-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
532-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
533-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
534-	# Headless auto‑start: simulate SPACE press
535:	if OS.get_environment("GODOT_HEADLESS") == "1":
536-		Input.action_press("ui_accept")
537-		Input.action_release("ui_accept")
538-		print("[VERBATIM] Headless auto‑start triggered.")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260718162836 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260720041743 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
1527-	print("[VERBATIM] POLL: _poll_controls() entered, game_state=", _game_state)
1528-
1529-	if _game_state == GameState.IN_PLANE:
1530:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J)
1531-		if _exit_pressed:
1532-			print("[DIAG] _poll_controls: exit aircraft triggered")
1533-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
807-	return null
808-
809-
810:func _deploy_canopy() -> void:
811-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
812-	if _canopy_deployed or not _canopy_instance:
813-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1561-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1562-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1563-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1564:		_deploy_canopy()
1565-
1566-	if _game_state == GameState.DIAGNOSIS:
1567-		var turn_input := 0.0
--
1584-		# ----- Direct key checks (bypass Input Map) -----
1585-		# Deploy (SPACE)
1586-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1587:		_deploy_canopy()
1588-	# Turn left (Q)
1589-	if Input.is_key_pressed(KEY_Q):
1590-		_turn_input = -1.0
--
1615-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1616-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1617-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1618:			_deploy_canopy()
1619-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
532-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
533-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
534-	# Headless auto‑start: simulate SPACE press
535:	if OS.get_environment("GODOT_HEADLESS") == "1":
536-		Input.action_press("ui_accept")
537-		Input.action_release("ui_accept")
538-		print("[VERBATIM] Headless auto‑start triggered.")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260720041743 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260720042511 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
1527-	print("[VERBATIM] POLL: _poll_controls() entered, game_state=", _game_state)
1528-
1529-	if _game_state == GameState.IN_PLANE:
1530:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J)
1531-		if _exit_pressed:
1532-			print("[DIAG] _poll_controls: exit aircraft triggered")
1533-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
807-	return null
808-
809-
810:func _deploy_canopy() -> void:
811-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
812-	if _canopy_deployed or not _canopy_instance:
813-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1561-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1562-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1563-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1564:		_deploy_canopy()
1565-
1566-	if _game_state == GameState.DIAGNOSIS:
1567-		var turn_input := 0.0
--
1584-		# ----- Direct key checks (bypass Input Map) -----
1585-		# Deploy (SPACE)
1586-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1587:		_deploy_canopy()
1588-	# Turn left (Q)
1589-	if Input.is_key_pressed(KEY_Q):
1590-		_turn_input = -1.0
--
1615-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1616-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1617-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1618:			_deploy_canopy()
1619-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
532-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
533-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
534-	# Headless auto‑start: simulate SPACE press
535:	if OS.get_environment("GODOT_HEADLESS") == "1":
536-		Input.action_press("ui_accept")
537-		Input.action_release("ui_accept")
538-		print("[VERBATIM] Headless auto‑start triggered.")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260720042511 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260720091744 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
1527-	print("[VERBATIM] POLL: _poll_controls() entered, game_state=", _game_state)
1528-
1529-	if _game_state == GameState.IN_PLANE:
1530:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J)
1531-		if _exit_pressed:
1532-			print("[DIAG] _poll_controls: exit aircraft triggered")
1533-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
807-	return null
808-
809-
810:func _deploy_canopy() -> void:
811-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
812-	if _canopy_deployed or not _canopy_instance:
813-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1561-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1562-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1563-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1564:		_deploy_canopy()
1565-
1566-	if _game_state == GameState.DIAGNOSIS:
1567-		var turn_input := 0.0
--
1584-		# ----- Direct key checks (bypass Input Map) -----
1585-		# Deploy (SPACE)
1586-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1587:		_deploy_canopy()
1588-	# Turn left (Q)
1589-	if Input.is_key_pressed(KEY_Q):
1590-		_turn_input = -1.0
--
1615-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1616-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1617-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1618:			_deploy_canopy()
1619-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
532-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
533-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
534-	# Headless auto‑start: simulate SPACE press
535:	if OS.get_environment("GODOT_HEADLESS") == "1":
536-		Input.action_press("ui_accept")
537-		Input.action_release("ui_accept")
538-		print("[VERBATIM] Headless auto‑start triggered.")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260720091744 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260722203347 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
1527-	print("[VERBATIM] POLL: _poll_controls() entered, game_state=", _game_state)
1528-
1529-	if _game_state == GameState.IN_PLANE:
1530:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J)
1531-		if _exit_pressed:
1532-			print("[DIAG] _poll_controls: exit aircraft triggered")
1533-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
807-	return null
808-
809-
810:func _deploy_canopy() -> void:
811-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
812-	if _canopy_deployed or not _canopy_instance:
813-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1561-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1562-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1563-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1564:		_deploy_canopy()
1565-
1566-	if _game_state == GameState.DIAGNOSIS:
1567-		var turn_input := 0.0
--
1584-		# ----- Direct key checks (bypass Input Map) -----
1585-		# Deploy (SPACE)
1586-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1587:		_deploy_canopy()
1588-	# Turn left (Q)
1589-	if Input.is_key_pressed(KEY_Q):
1590-		_turn_input = -1.0
--
1615-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1616-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1617-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1618:			_deploy_canopy()
1619-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
532-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
533-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
534-	# Headless auto‑start: simulate SPACE press
535:	if OS.get_environment("GODOT_HEADLESS") == "1":
536-		Input.action_press("ui_accept")
537-		Input.action_release("ui_accept")
538-		print("[VERBATIM] Headless auto‑start triggered.")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260722203347 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260723095811 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
1527-	print("[VERBATIM] POLL: _poll_controls() entered, game_state=", _game_state)
1528-
1529-	if _game_state == GameState.IN_PLANE:
1530:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J)
1531-		if _exit_pressed:
1532-			print("[DIAG] _poll_controls: exit aircraft triggered")
1533-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
807-	return null
808-
809-
810:func _deploy_canopy() -> void:
811-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
812-	if _canopy_deployed or not _canopy_instance:
813-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1561-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1562-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1563-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1564:		_deploy_canopy()
1565-
1566-	if _game_state == GameState.DIAGNOSIS:
1567-		var turn_input := 0.0
--
1584-		# ----- Direct key checks (bypass Input Map) -----
1585-		# Deploy (SPACE)
1586-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1587:		_deploy_canopy()
1588-	# Turn left (Q)
1589-	if Input.is_key_pressed(KEY_Q):
1590-		_turn_input = -1.0
--
1615-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1616-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1617-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1618:			_deploy_canopy()
1619-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
532-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
533-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
534-	# Headless auto‑start: simulate SPACE press
535:	if OS.get_environment("GODOT_HEADLESS") == "1":
536-		Input.action_press("ui_accept")
537-		Input.action_release("ui_accept")
538-		print("[VERBATIM] Headless auto‑start triggered.")
--
1903-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1904-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1905-			#  by _0283.txt showing plane position update before stall)
1906:			if OS.get_environment("GODOT_HEADLESS") == "1":
1907-				if not ProjectSettings.has_setting("_headless_space_fired"):
1908-					ProjectSettings.set_setting("_headless_space_fired", true)
1909-					Input.action_press("ui_accept")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260723095811 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260723100530 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
1527-	print("[VERBATIM] POLL: _poll_controls() entered, game_state=", _game_state)
1528-
1529-	if _game_state == GameState.IN_PLANE:
1530:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J)
1531-		if _exit_pressed:
1532-			print("[DIAG] _poll_controls: exit aircraft triggered")
1533-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
807-	return null
808-
809-
810:func _deploy_canopy() -> void:
811-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
812-	if _canopy_deployed or not _canopy_instance:
813-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1561-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1562-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1563-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1564:		_deploy_canopy()
1565-
1566-	if _game_state == GameState.DIAGNOSIS:
1567-		var turn_input := 0.0
--
1584-		# ----- Direct key checks (bypass Input Map) -----
1585-		# Deploy (SPACE)
1586-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1587:		_deploy_canopy()
1588-	# Turn left (Q)
1589-	if Input.is_key_pressed(KEY_Q):
1590-		_turn_input = -1.0
--
1615-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1616-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1617-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1618:			_deploy_canopy()
1619-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
532-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
533-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
534-	# Headless auto‑start: simulate SPACE press
535:	if OS.get_environment("GODOT_HEADLESS") == "1":
536-		Input.action_press("ui_accept")
537-		Input.action_release("ui_accept")
538-		print("[VERBATIM] Headless auto‑start triggered.")
--
1903-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1904-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1905-			#  by _0283.txt showing plane position update before stall)
1906:			if OS.get_environment("GODOT_HEADLESS") == "1":
1907-				if not ProjectSettings.has_setting("_headless_space_fired"):
1908-					ProjectSettings.set_setting("_headless_space_fired", true)
1909-					Input.action_press("ui_accept")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260723100530 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260723101430 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
1527-	print("[VERBATIM] POLL: _poll_controls() entered, game_state=", _game_state)
1528-
1529-	if _game_state == GameState.IN_PLANE:
1530:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J)
1531-		if _exit_pressed:
1532-			print("[DIAG] _poll_controls: exit aircraft triggered")
1533-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
807-	return null
808-
809-
810:func _deploy_canopy() -> void:
811-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
812-	if _canopy_deployed or not _canopy_instance:
813-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1561-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1562-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1563-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1564:		_deploy_canopy()
1565-
1566-	if _game_state == GameState.DIAGNOSIS:
1567-		var turn_input := 0.0
--
1584-		# ----- Direct key checks (bypass Input Map) -----
1585-		# Deploy (SPACE)
1586-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1587:		_deploy_canopy()
1588-	# Turn left (Q)
1589-	if Input.is_key_pressed(KEY_Q):
1590-		_turn_input = -1.0
--
1615-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1616-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1617-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1618:			_deploy_canopy()
1619-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
532-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
533-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
534-	# Headless auto‑start: simulate SPACE press
535:	if OS.get_environment("GODOT_HEADLESS") == "1":
536-		Input.action_press("deploy")
537-		Input.action_release("deploy")
538-		print("[VERBATIM] Headless auto‑start triggered.")
--
1903-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1904-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1905-			#  by _0283.txt showing plane position update before stall)
1906:			if OS.get_environment("GODOT_HEADLESS") == "1":
1907-				if not ProjectSettings.has_setting("_headless_space_fired"):
1908-					ProjectSettings.set_setting("_headless_space_fired", true)
1909-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260723101430 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260723102552 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
124-var _canopy_instance: Node3D
125-var _canopy_material: StandardMaterial3D
126-var _canopy_deployed: bool = false
127:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
128-var _deployment_timer
129-var _screenshot_save_timer: float = 0.0
130-const DEPLOY_TIME: float = 1.2
--
1531-		# Also check headless auto-jump flag (set in _physics_process;
1532-		# reliable across _physics_process/_process frame boundary).
1533-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1534:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J) or _headless_auto_jump
1535:		_headless_auto_jump = false  # consume the flag
1536-		if _exit_pressed:
1537-			print("[DIAG] _poll_controls: exit aircraft triggered")
1538-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
808-	return null
809-
810-
811:func _deploy_canopy() -> void:
812-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
813-	if _canopy_deployed or not _canopy_instance:
814-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1348-	if current_time >= event["time"]:
1349-		match event["action"]:
1350-			"deploy":
1351:				_deploy_canopy()
1352-			"cutaway":
1353-				_do_cutaway()
1354-			"reserve":
--
1566-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1567-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1568-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1569:		_deploy_canopy()
1570-
1571-	if _game_state == GameState.DIAGNOSIS:
1572-		var turn_input := 0.0
--
1589-		# ----- Direct key checks (bypass Input Map) -----
1590-		# Deploy (SPACE)
1591-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1592:		_deploy_canopy()
1593-	# Turn left (Q)
1594-	if Input.is_key_pressed(KEY_Q):
1595-		_turn_input = -1.0
--
1620-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1621-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1622-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1623:			_deploy_canopy()
1624-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1625-	# Turn left (Q)
1626-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
533-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536:	if OS.get_environment("GODOT_HEADLESS") == "1":
537-		Input.action_press("deploy")
538-		Input.action_release("deploy")
539-		print("[VERBATIM] Headless auto‑start triggered.")
--
1908-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1909-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1910-			#  by _0283.txt showing plane position update before stall)
1911:			if OS.get_environment("GODOT_HEADLESS") == "1":
1912-				if not ProjectSettings.has_setting("_headless_space_fired"):
1913-					ProjectSettings.set_setting("_headless_space_fired", true)
1914-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260723102552 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260723111748 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
124-var _canopy_instance: Node3D
125-var _canopy_material: StandardMaterial3D
126-var _canopy_deployed: bool = false
127:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
128-var _deployment_timer
129-var _screenshot_save_timer: float = 0.0
130-const DEPLOY_TIME: float = 1.2
--
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536-	if OS.get_environment("GODOT_HEADLESS") == "1":
537:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
538-		# Input.action_release("deploy") removed — flag-based now
539-		print("[VERBATIM] Headless auto‑start triggered.")
540-	_check_arm_pose_safe()
--
1531-		# Also check headless auto-jump flag (set in _physics_process;
1532-		# reliable across _physics_process/_process frame boundary).
1533-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1534:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J) or _headless_auto_jump
1535:		_headless_auto_jump = false  # consume the flag
1536-		if _exit_pressed:
1537-			print("[DIAG] _poll_controls: exit aircraft triggered")
1538-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
808-	return null
809-
810-
811:func _deploy_canopy() -> void:
812-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
813-	if _canopy_deployed or not _canopy_instance:
814-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1348-	if current_time >= event["time"]:
1349-		match event["action"]:
1350-			"deploy":
1351:				_deploy_canopy()
1352-			"cutaway":
1353-				_do_cutaway()
1354-			"reserve":
--
1566-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1567-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1568-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1569:		_deploy_canopy()
1570-
1571-	if _game_state == GameState.DIAGNOSIS:
1572-		var turn_input := 0.0
--
1589-		# ----- Direct key checks (bypass Input Map) -----
1590-		# Deploy (SPACE)
1591-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1592:		_deploy_canopy()
1593-	# Turn left (Q)
1594-	if Input.is_key_pressed(KEY_Q):
1595-		_turn_input = -1.0
--
1620-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1621-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1622-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1623:			_deploy_canopy()
1624-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1625-	# Turn left (Q)
1626-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
533-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536:	if OS.get_environment("GODOT_HEADLESS") == "1":
537-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
538-		# Input.action_release("deploy") removed — flag-based now
539-		print("[VERBATIM] Headless auto‑start triggered.")
--
1908-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1909-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1910-			#  by _0283.txt showing plane position update before stall)
1911:			if OS.get_environment("GODOT_HEADLESS") == "1":
1912-				if not ProjectSettings.has_setting("_headless_space_fired"):
1913-					ProjectSettings.set_setting("_headless_space_fired", true)
1914-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260723111748 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260723114647 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
124-var _canopy_instance: Node3D
125-var _canopy_material: StandardMaterial3D
126-var _canopy_deployed: bool = false
127:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
128-var _deployment_timer
129-var _screenshot_save_timer: float = 0.0
130-const DEPLOY_TIME: float = 1.2
--
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536-	if OS.get_environment("GODOT_HEADLESS") == "1":
537:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
538-		# Input.action_release("deploy") removed — flag-based now
539-		print("[VERBATIM] Headless auto‑start triggered.")
540-	_check_arm_pose_safe()
--
1531-		# Also check headless auto-jump flag (set in _physics_process;
1532-		# reliable across _physics_process/_process frame boundary).
1533-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1534:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J) or _headless_auto_jump
1535:		_headless_auto_jump = false  # consume the flag
1536-		if _exit_pressed:
1537-			print("[DIAG] _poll_controls: exit aircraft triggered")
1538-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
808-	return null
809-
810-
811:func _deploy_canopy() -> void:
812-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
813-	if _canopy_deployed or not _canopy_instance:
814-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1348-	if current_time >= event["time"]:
1349-		match event["action"]:
1350-			"deploy":
1351:				_deploy_canopy()
1352-			"cutaway":
1353-				_do_cutaway()
1354-			"reserve":
--
1566-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1567-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1568-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1569:		_deploy_canopy()
1570-
1571-	if _game_state == GameState.DIAGNOSIS:
1572-		var turn_input := 0.0
--
1589-		# ----- Direct key checks (bypass Input Map) -----
1590-		# Deploy (SPACE)
1591-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1592:		_deploy_canopy()
1593-	# Turn left (Q)
1594-	if Input.is_key_pressed(KEY_Q):
1595-		_turn_input = -1.0
--
1620-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1621-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1622-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1623:			_deploy_canopy()
1624-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1625-	# Turn left (Q)
1626-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
533-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536:	if OS.get_environment("GODOT_HEADLESS") == "1":
537-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
538-		# Input.action_release("deploy") removed — flag-based now
539-		print("[VERBATIM] Headless auto‑start triggered.")
--
1908-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1909-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1910-			#  by _0283.txt showing plane position update before stall)
1911:			if OS.get_environment("GODOT_HEADLESS") == "1":
1912-				if not ProjectSettings.has_setting("_headless_space_fired"):
1913-					ProjectSettings.set_setting("_headless_space_fired", true)
1914-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260723114647 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260723203503 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
124-var _canopy_instance: Node3D
125-var _canopy_material: StandardMaterial3D
126-var _canopy_deployed: bool = false
127:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
128-var _deployment_timer
129-var _screenshot_save_timer: float = 0.0
130-const DEPLOY_TIME: float = 1.2
--
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
537:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
538-		# Input.action_release("deploy") removed — flag-based now
539-		print("[VERBATIM] Headless auto‑start triggered.")
540-	_check_arm_pose_safe()
--
1531-		# Also check headless auto-jump flag (set in _physics_process;
1532-		# reliable across _physics_process/_process frame boundary).
1533-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1534:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J) or _headless_auto_jump
1535:		_headless_auto_jump = false  # consume the flag
1536-		if _exit_pressed:
1537-			print("[DIAG] _poll_controls: exit aircraft triggered")
1538-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
808-	return null
809-
810-
811:func _deploy_canopy() -> void:
812-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
813-	if _canopy_deployed or not _canopy_instance:
814-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1348-	if current_time >= event["time"]:
1349-		match event["action"]:
1350-			"deploy":
1351:				_deploy_canopy()
1352-			"cutaway":
1353-				_do_cutaway()
1354-			"reserve":
--
1566-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1567-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1568-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1569:		_deploy_canopy()
1570-
1571-	if _game_state == GameState.DIAGNOSIS:
1572-		var turn_input := 0.0
--
1589-		# ----- Direct key checks (bypass Input Map) -----
1590-		# Deploy (SPACE)
1591-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1592:		_deploy_canopy()
1593-	# Turn left (Q)
1594-	if Input.is_key_pressed(KEY_Q):
1595-		_turn_input = -1.0
--
1620-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1621-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1622-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1623:			_deploy_canopy()
1624-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1625-	# Turn left (Q)
1626-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
533-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
537-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
538-		# Input.action_release("deploy") removed — flag-based now
539-		print("[VERBATIM] Headless auto‑start triggered.")
--
1908-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1909-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1910-			#  by _0283.txt showing plane position update before stall)
1911:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1912-				if not ProjectSettings.has_setting("_headless_space_fired"):
1913-					ProjectSettings.set_setting("_headless_space_fired", true)
1914-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260723203503 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260724124148359987 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
124-var _canopy_instance: Node3D
125-var _canopy_material: StandardMaterial3D
126-var _canopy_deployed: bool = false
127:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
128-var _deployment_timer
129-var _screenshot_save_timer: float = 0.0
130-const DEPLOY_TIME: float = 1.2
--
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
537:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
538-		# Input.action_release("deploy") removed — flag-based now
539-		print("[VERBATIM] Headless auto‑start triggered.")
540-	_check_arm_pose_safe()
--
1531-		# Also check headless auto-jump flag (set in _physics_process;
1532-		# reliable across _physics_process/_process frame boundary).
1533-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1534:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J) or _headless_auto_jump
1535:		_headless_auto_jump = false  # consume the flag
1536-		if _exit_pressed:
1537-			print("[DIAG] _poll_controls: exit aircraft triggered")
1538-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
808-	return null
809-
810-
811:func _deploy_canopy() -> void:
812-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
813-	if _canopy_deployed or not _canopy_instance:
814-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1348-	if current_time >= event["time"]:
1349-		match event["action"]:
1350-			"deploy":
1351:				_deploy_canopy()
1352-			"cutaway":
1353-				_do_cutaway()
1354-			"reserve":
--
1566-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1567-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1568-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1569:		_deploy_canopy()
1570-
1571-	if _game_state == GameState.DIAGNOSIS:
1572-		var turn_input := 0.0
--
1589-		# ----- Direct key checks (bypass Input Map) -----
1590-		# Deploy (SPACE)
1591-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1592:		_deploy_canopy()
1593-	# Turn left (Q)
1594-	if Input.is_key_pressed(KEY_Q):
1595-		_turn_input = -1.0
--
1620-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1621-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1622-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1623:			_deploy_canopy()
1624-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1625-	# Turn left (Q)
1626-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
533-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
537-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
538-		# Input.action_release("deploy") removed — flag-based now
539-		print("[VERBATIM] Headless auto‑start triggered.")
--
1914-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1915-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1916-			#  by _0283.txt showing plane position update before stall)
1917:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1918-				if not ProjectSettings.has_setting("_headless_space_fired"):
1919-					ProjectSettings.set_setting("_headless_space_fired", true)
1920-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260724124148359987 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260724124900324562 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
124-var _canopy_instance: Node3D
125-var _canopy_material: StandardMaterial3D
126-var _canopy_deployed: bool = false
127:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
128-var _deployment_timer
129-var _screenshot_save_timer: float = 0.0
130-const DEPLOY_TIME: float = 1.2
--
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
537:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
538-		# Input.action_release("deploy") removed — flag-based now
539-		print("[VERBATIM] Headless auto‑start triggered.")
540-	_check_arm_pose_safe()
--
1531-		# Also check headless auto-jump flag (set in _physics_process;
1532-		# reliable across _physics_process/_process frame boundary).
1533-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1534:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J) or _headless_auto_jump
1535:		_headless_auto_jump = false  # consume the flag
1536-		if _exit_pressed:
1537-			print("[DIAG] _poll_controls: exit aircraft triggered")
1538-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
808-	return null
809-
810-
811:func _deploy_canopy() -> void:
812-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
813-	if _canopy_deployed or not _canopy_instance:
814-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1348-	if current_time >= event["time"]:
1349-		match event["action"]:
1350-			"deploy":
1351:				_deploy_canopy()
1352-			"cutaway":
1353-				_do_cutaway()
1354-			"reserve":
--
1566-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1567-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1568-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1569:		_deploy_canopy()
1570-
1571-	if _game_state == GameState.DIAGNOSIS:
1572-		var turn_input := 0.0
--
1589-		# ----- Direct key checks (bypass Input Map) -----
1590-		# Deploy (SPACE)
1591-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1592:		_deploy_canopy()
1593-	# Turn left (Q)
1594-	if Input.is_key_pressed(KEY_Q):
1595-		_turn_input = -1.0
--
1620-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1621-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1622-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1623:			_deploy_canopy()
1624-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1625-	# Turn left (Q)
1626-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
533-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
537-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
538-		# Input.action_release("deploy") removed — flag-based now
539-		print("[VERBATIM] Headless auto‑start triggered.")
--
1914-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1915-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1916-			#  by _0283.txt showing plane position update before stall)
1917:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1918-				if not ProjectSettings.has_setting("_headless_space_fired"):
1919-					ProjectSettings.set_setting("_headless_space_fired", true)
1920-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260724124900324562 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260724130454791646 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
124-var _canopy_instance: Node3D
125-var _canopy_material: StandardMaterial3D
126-var _canopy_deployed: bool = false
127:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
128-var _deployment_timer
129-var _screenshot_save_timer: float = 0.0
130-const DEPLOY_TIME: float = 1.2
--
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
537:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
538-		# Input.action_release("deploy") removed — flag-based now
539-		print("[VERBATIM] Headless auto‑start triggered.")
540-	_check_arm_pose_safe()
--
1531-		# Also check headless auto-jump flag (set in _physics_process;
1532-		# reliable across _physics_process/_process frame boundary).
1533-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1534:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J) or _headless_auto_jump
1535:		_headless_auto_jump = false  # consume the flag
1536-		if _exit_pressed:
1537-			print("[DIAG] _poll_controls: exit aircraft triggered")
1538-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
808-	return null
809-
810-
811:func _deploy_canopy() -> void:
812-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
813-	if _canopy_deployed or not _canopy_instance:
814-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1348-	if current_time >= event["time"]:
1349-		match event["action"]:
1350-			"deploy":
1351:				_deploy_canopy()
1352-			"cutaway":
1353-				_do_cutaway()
1354-			"reserve":
--
1566-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1567-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1568-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1569:		_deploy_canopy()
1570-
1571-	if _game_state == GameState.DIAGNOSIS:
1572-		var turn_input := 0.0
--
1589-		# ----- Direct key checks (bypass Input Map) -----
1590-		# Deploy (SPACE)
1591-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1592:		_deploy_canopy()
1593-	# Turn left (Q)
1594-	if Input.is_key_pressed(KEY_Q):
1595-		_turn_input = -1.0
--
1620-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1621-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1622-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1623:			_deploy_canopy()
1624-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1625-	# Turn left (Q)
1626-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
533-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
534-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
535-	# Headless auto‑start: simulate SPACE press
536:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
537-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
538-		# Input.action_release("deploy") removed — flag-based now
539-		print("[VERBATIM] Headless auto‑start triggered.")
--
1914-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1915-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1916-			#  by _0283.txt showing plane position update before stall)
1917:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1918-				if not ProjectSettings.has_setting("_headless_space_fired"):
1919-					ProjectSettings.set_setting("_headless_space_fired", true)
1920-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260724130454791646 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260724131753266426 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
124-var _canopy_instance: Node3D
125-var _canopy_material: StandardMaterial3D
126-var _canopy_deployed: bool = false
127:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
128-var _deployment_timer
129-var _screenshot_save_timer: float = 0.0
130-const DEPLOY_TIME: float = 1.2
--
536-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
537-	# Headless auto‑start: simulate SPACE press
538-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
539:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
540-		# Input.action_release("deploy") removed — flag-based now
541-		print("[VERBATIM] Headless auto‑start triggered.")
542-	_check_arm_pose_safe()
--
1533-		# Also check headless auto-jump flag (set in _physics_process;
1534-		# reliable across _physics_process/_process frame boundary).
1535-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1536:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J) or _headless_auto_jump
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
810-	return null
811-
812-
813:func _deploy_canopy() -> void:
814-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
815-	if _canopy_deployed or not _canopy_instance:
816-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1350-	if current_time >= event["time"]:
1351-		match event["action"]:
1352-			"deploy":
1353:				_deploy_canopy()
1354-			"cutaway":
1355-				_do_cutaway()
1356-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS:
1574-		var turn_input := 0.0
--
1591-		# ----- Direct key checks (bypass Input Map) -----
1592-		# Deploy (SPACE)
1593-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1594:		_deploy_canopy()
1595-	# Turn left (Q)
1596-	if Input.is_key_pressed(KEY_Q):
1597-		_turn_input = -1.0
--
1622-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1623-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1624-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1625:			_deploy_canopy()
1626-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1627-	# Turn left (Q)
1628-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
535-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
536-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
537-	# Headless auto‑start: simulate SPACE press
538:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
539-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
540-		# Input.action_release("deploy") removed — flag-based now
541-		print("[VERBATIM] Headless auto‑start triggered.")
--
1916-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1917-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1918-			#  by _0283.txt showing plane position update before stall)
1919:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1920-				if not ProjectSettings.has_setting("_headless_space_fired"):
1921-					ProjectSettings.set_setting("_headless_space_fired", true)
1922-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260724131753266426 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260725163513148483 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1534-		# Also check headless auto-jump flag (set in _physics_process;
1535-		# reliable across _physics_process/_process frame boundary).
1536-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1537:		var _exit_pressed = Input.is_action_just_pressed("deploy") or Input.is_key_pressed(KEY_J) or _headless_auto_jump
1538:		_headless_auto_jump = false  # consume the flag
1539-		if _exit_pressed:
1540-			print("[DIAG] _poll_controls: exit aircraft triggered")
1541-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1351-	if current_time >= event["time"]:
1352-		match event["action"]:
1353-			"deploy":
1354:				_deploy_canopy()
1355-			"cutaway":
1356-				_do_cutaway()
1357-			"reserve":
--
1569-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1570-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1571-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1572:		_deploy_canopy()
1573-
1574-	if _game_state == GameState.DIAGNOSIS:
1575-		var turn_input := 0.0
--
1592-		# ----- Direct key checks (bypass Input Map) -----
1593-		# Deploy (SPACE)
1594-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1595:		_deploy_canopy()
1596-	# Turn left (Q)
1597-	if Input.is_key_pressed(KEY_Q):
1598-		_turn_input = -1.0
--
1623-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1624-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1625-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1626:			_deploy_canopy()
1627-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1628-	# Turn left (Q)
1629-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1917-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1918-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1919-			#  by _0283.txt showing plane position update before stall)
1920:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1921-				if not ProjectSettings.has_setting("_headless_space_fired"):
1922-					ProjectSettings.set_setting("_headless_space_fired", true)
1923-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260725163513148483 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260726120627 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1537-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1538-		var _exit_pressed = (
1539-			Input.is_action_just_pressed("deploy")
1540:			or Input.is_key_pressed(KEY_J)
1541:			or _headless_auto_jump
1542-		)
1543:		_headless_auto_jump = false  # consume the flag
1544-		if _exit_pressed:
1545-			print("[DIAG] _poll_controls: exit aircraft triggered")
1546-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1352-	if current_time >= event["time"]:
1353-		match event["action"]:
1354-			"deploy":
1355:				_deploy_canopy()
1356-			"cutaway":
1357-				_do_cutaway()
1358-			"reserve":
--
1574-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1575-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1576-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1577:		_deploy_canopy()
1578-
1579-	if _game_state == GameState.DIAGNOSIS:
1580-		var turn_input := 0.0
--
1597-		# ----- Direct key checks (bypass Input Map) -----
1598-		# Deploy (SPACE)
1599-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1600:		_deploy_canopy()
1601-	# Turn left (Q)
1602-	if Input.is_key_pressed(KEY_Q):
1603-		_turn_input = -1.0
--
1628-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1629-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1630-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1631:			_deploy_canopy()
1632-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1922-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1923-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1924-			#  by _0283.txt showing plane position update before stall)
1925:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1926-				if not ProjectSettings.has_setting("_headless_space_fired"):
1927-					ProjectSettings.set_setting("_headless_space_fired", true)
1928-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260726120627 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260726121327 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1537-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1538-		var _exit_pressed = (
1539-			Input.is_action_just_pressed("deploy")
1540:			or Input.is_key_pressed(KEY_J)
1541:			or _headless_auto_jump
1542-		)
1543:		_headless_auto_jump = false  # consume the flag
1544-		if _exit_pressed:
1545-			print("[DIAG] _poll_controls: exit aircraft triggered")
1546-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1352-	if current_time >= event["time"]:
1353-		match event["action"]:
1354-			"deploy":
1355:				_deploy_canopy()
1356-			"cutaway":
1357-				_do_cutaway()
1358-			"reserve":
--
1574-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1575-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1576-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1577:		_deploy_canopy()
1578-
1579-	if _game_state == GameState.DIAGNOSIS:
1580-		var turn_input := 0.0
--
1597-		# ----- Direct key checks (bypass Input Map) -----
1598-		# Deploy (SPACE)
1599-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1600:		_deploy_canopy()
1601-	# Turn left (Q)
1602-	if Input.is_key_pressed(KEY_Q):
1603-		_turn_input = -1.0
--
1628-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1629-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1630-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1631:			_deploy_canopy()
1632-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1922-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1923-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1924-			#  by _0283.txt showing plane position update before stall)
1925:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1926-				if not ProjectSettings.has_setting("_headless_space_fired"):
1927-					ProjectSettings.set_setting("_headless_space_fired", true)
1928-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260726121327 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260726122535 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1537-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1538-		var _exit_pressed = (
1539-			Input.is_action_just_pressed("deploy")
1540:			or Input.is_key_pressed(KEY_J)
1541:			or _headless_auto_jump
1542-		)
1543:		_headless_auto_jump = false  # consume the flag
1544-		if _exit_pressed:
1545-			print("[DIAG] _poll_controls: exit aircraft triggered")
1546-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1352-	if current_time >= event["time"]:
1353-		match event["action"]:
1354-			"deploy":
1355:				_deploy_canopy()
1356-			"cutaway":
1357-				_do_cutaway()
1358-			"reserve":
--
1574-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1575-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1576-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1577:		_deploy_canopy()
1578-
1579-	if _game_state == GameState.DIAGNOSIS:
1580-		var turn_input := 0.0
--
1597-		# ----- Direct key checks (bypass Input Map) -----
1598-		# Deploy (SPACE)
1599-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1600:		_deploy_canopy()
1601-	# Turn left (Q)
1602-	if Input.is_key_pressed(KEY_Q):
1603-		_turn_input = -1.0
--
1628-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1629-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1630-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1631:			_deploy_canopy()
1632-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1922-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1923-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1924-			#  by _0283.txt showing plane position update before stall)
1925:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1926-				if not ProjectSettings.has_setting("_headless_space_fired"):
1927-					ProjectSettings.set_setting("_headless_space_fired", true)
1928-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260726122535 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260726125122 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args() or OS.get_environment("GODOT_HEADLESS") != "":  # FIX-P1: also trigger on GODOT_HEADLESS env var (set by autostall_patched.py)
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1537-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1538-		var _exit_pressed = (
1539-			Input.is_action_just_pressed("deploy")
1540:			or Input.is_key_pressed(KEY_J)
1541:			or _headless_auto_jump
1542-		)
1543:		_headless_auto_jump = false  # consume the flag
1544-		if _exit_pressed:
1545-			print("[DIAG] _poll_controls: exit aircraft triggered")
1546-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1352-	if current_time >= event["time"]:
1353-		match event["action"]:
1354-			"deploy":
1355:				_deploy_canopy()
1356-			"cutaway":
1357-				_do_cutaway()
1358-			"reserve":
--
1574-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1575-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1576-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1577:		_deploy_canopy()
1578-
1579-	if _game_state == GameState.DIAGNOSIS:
1580-		var turn_input := 0.0
--
1597-		# ----- Direct key checks (bypass Input Map) -----
1598-		# Deploy (SPACE)
1599-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1600:		_deploy_canopy()
1601-	# Turn left (Q)
1602-	if Input.is_key_pressed(KEY_Q):
1603-		_turn_input = -1.0
--
1628-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1629-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1630-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1631:			_deploy_canopy()
1632-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args() or OS.get_environment("GODOT_HEADLESS") != "":  # FIX-P1: also trigger on GODOT_HEADLESS env var (set by autostall_patched.py)
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1922-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1923-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1924-			#  by _0283.txt showing plane position update before stall)
1925:			if "--headless" in OS.get_cmdline_args() or OS.get_environment("GODOT_HEADLESS") != "":  # FIX-P1: also trigger on GODOT_HEADLESS env var (set by autostall_patched.py)
1926-				if not ProjectSettings.has_setting("_headless_space_fired"):
1927-					ProjectSettings.set_setting("_headless_space_fired", true)
1928-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260726125122 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260726142423 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1537-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1538-		var _exit_pressed = (
1539-			Input.is_action_just_pressed("deploy")
1540:			or Input.is_key_pressed(KEY_J)
1541:			or _headless_auto_jump
1542-		)
1543:		_headless_auto_jump = false  # consume the flag
1544-		if _exit_pressed:
1545-			print("[DIAG] _poll_controls: exit aircraft triggered")
1546-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1352-	if current_time >= event["time"]:
1353-		match event["action"]:
1354-			"deploy":
1355:				_deploy_canopy()
1356-			"cutaway":
1357-				_do_cutaway()
1358-			"reserve":
--
1574-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1575-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1576-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1577:		_deploy_canopy()
1578-
1579-	if _game_state == GameState.DIAGNOSIS:
1580-		var turn_input := 0.0
--
1597-		# ----- Direct key checks (bypass Input Map) -----
1598-		# Deploy (SPACE)
1599-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1600:		_deploy_canopy()
1601-	# Turn left (Q)
1602-	if Input.is_key_pressed(KEY_Q):
1603-		_turn_input = -1.0
--
1628-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1629-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1630-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1631:			_deploy_canopy()
1632-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1922-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1923-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1924-			#  by _0283.txt showing plane position update before stall)
1925:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1926-				if not ProjectSettings.has_setting("_headless_space_fired"):
1927-					ProjectSettings.set_setting("_headless_space_fired", true)
1928-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260726142423 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260727115140 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1537-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1538-		var _exit_pressed = (
1539-			Input.is_action_just_pressed("deploy")
1540:			or Input.is_key_pressed(KEY_J)
1541:			or _headless_auto_jump
1542-		)
1543:		_headless_auto_jump = false  # consume the flag
1544-		if _exit_pressed:
1545-			print("[DIAG] _poll_controls: exit aircraft triggered")
1546-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1352-	if current_time >= event["time"]:
1353-		match event["action"]:
1354-			"deploy":
1355:				_deploy_canopy()
1356-			"cutaway":
1357-				_do_cutaway()
1358-			"reserve":
--
1574-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1575-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1576-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1577:		_deploy_canopy()
1578-
1579-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1580-		var turn_input := 0.0
--
1599-		# ----- Direct key checks (bypass Input Map) -----
1600-		# Deploy (SPACE)
1601-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1602:		_deploy_canopy()
1603-	# Turn left (Q)
1604-	if Input.is_key_pressed(KEY_Q):
1605-		_turn_input = -1.0
--
1630-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1631-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1632-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1633:			_deploy_canopy()
1634-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1635-	# Turn left (Q)
1636-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1924-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1925-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1926-			#  by _0283.txt showing plane position update before stall)
1927:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1928-				if not ProjectSettings.has_setting("_headless_space_fired"):
1929-					ProjectSettings.set_setting("_headless_space_fired", true)
1930-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260727115140 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260727115256 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1537-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1538-		var _exit_pressed = (
1539-			Input.is_action_just_pressed("deploy")
1540:			or Input.is_key_pressed(KEY_J)
1541:			or _headless_auto_jump
1542-		)
1543:		_headless_auto_jump = false  # consume the flag
1544-		if _exit_pressed:
1545-			print("[DIAG] _poll_controls: exit aircraft triggered")
1546-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1352-	if current_time >= event["time"]:
1353-		match event["action"]:
1354-			"deploy":
1355:				_deploy_canopy()
1356-			"cutaway":
1357-				_do_cutaway()
1358-			"reserve":
--
1574-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1575-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1576-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1577:		_deploy_canopy()
1578-
1579-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1580-		var turn_input := 0.0
--
1598-
1599-		# ----- Direct key checks (bypass Input Map) -----
1600-		# Deploy (SPACE)
1601:		# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1602-		if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1603:			_deploy_canopy()
1604-	# Turn left (Q)
1605-	if Input.is_key_pressed(KEY_Q):
1606-		_turn_input = -1.0
--
1629-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1630-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1631-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1632:			_deploy_canopy()
1633-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1634-	# Turn left (Q)
1635-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1923-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1924-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1925-			#  by _0283.txt showing plane position update before stall)
1926:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1927-				if not ProjectSettings.has_setting("_headless_space_fired"):
1928-					ProjectSettings.set_setting("_headless_space_fired", true)
1929-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260727115256 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260727152133 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1537-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1538-		var _exit_pressed = (
1539-			Input.is_action_just_pressed("deploy")
1540:			or Input.is_key_pressed(KEY_J)
1541:			or _headless_auto_jump
1542-		)
1543:		_headless_auto_jump = false  # consume the flag
1544-		if _exit_pressed:
1545-			print("[DIAG] _poll_controls: exit aircraft triggered")
1546-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1352-	if current_time >= event["time"]:
1353-		match event["action"]:
1354-			"deploy":
1355:				_deploy_canopy()
1356-			"cutaway":
1357-				_do_cutaway()
1358-			"reserve":
--
1574-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1575-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1576-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1577:		_deploy_canopy()
1578-
1579-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1580-		var turn_input := 0.0
--
1615-
1616-		# ----- Direct key checks (bypass Input Map) -----
1617-		# Deploy (SPACE)
1618:		# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1619-		if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1620:			_deploy_canopy()
1621-	# Turn left (Q)
1622-	if Input.is_key_pressed(KEY_Q):
1623-		_turn_input = -1.0
--
1648-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1649-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1650-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1651:			_deploy_canopy()
1652-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1653-	# Turn left (Q)
1654-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1942-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1943-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1944-			#  by _0283.txt showing plane position update before stall)
1945:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1946-				if not ProjectSettings.has_setting("_headless_space_fired"):
1947-					ProjectSettings.set_setting("_headless_space_fired", true)
1948-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260727152133 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729013912 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1537-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1538-		var _exit_pressed = (
1539-			Input.is_action_just_pressed("deploy")
1540:			or Input.is_key_pressed(KEY_J)
1541:			or _headless_auto_jump
1542-		)
1543:		_headless_auto_jump = false  # consume the flag
1544-		if _exit_pressed:
1545-			print("[DIAG] _poll_controls: exit aircraft triggered")
1546-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1352-	if current_time >= event["time"]:
1353-		match event["action"]:
1354-			"deploy":
1355:				_deploy_canopy()
1356-			"cutaway":
1357-				_do_cutaway()
1358-			"reserve":
--
1574-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1575-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1576-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1577:		_deploy_canopy()
1578-
1579-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1580-		var turn_input := 0.0
--
1612-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1613-			else:
1614-				print("[STEER_TELEM] /root/Main does not exist")
1615:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1616-		# ----- Direct key checks (bypass Input Map) -----
1617-		# Deploy (SPACE)
1618-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1619:		_deploy_canopy()
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):
1622-		_turn_input = -1.0
--
1647-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1648-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1649-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1650:			_deploy_canopy()
1651-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1652-	# Turn left (Q)
1653-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1941-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1942-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1943-			#  by _0283.txt showing plane position update before stall)
1944:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1945-				if not ProjectSettings.has_setting("_headless_space_fired"):
1946-					ProjectSettings.set_setting("_headless_space_fired", true)
1947-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729013912 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729014632 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1537-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1538-		var _exit_pressed = (
1539-			Input.is_action_just_pressed("deploy")
1540:			or Input.is_key_pressed(KEY_J)
1541:			or _headless_auto_jump
1542-		)
1543:		_headless_auto_jump = false  # consume the flag
1544-		if _exit_pressed:
1545-			print("[DIAG] _poll_controls: exit aircraft triggered")
1546-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1352-	if current_time >= event["time"]:
1353-		match event["action"]:
1354-			"deploy":
1355:				_deploy_canopy()
1356-			"cutaway":
1357-				_do_cutaway()
1358-			"reserve":
--
1574-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1575-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1576-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1577:		_deploy_canopy()
1578-
1579-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1580-		var turn_input := 0.0
--
1612-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1613-			else:
1614-				print("[STEER_TELEM] /root/Main does not exist")
1615:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1616-		# ----- Direct key checks (bypass Input Map) -----
1617-		# Deploy (SPACE)
1618-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1619:		_deploy_canopy()
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):
1622-		_turn_input = -1.0
--
1647-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1648-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1649-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1650:			_deploy_canopy()
1651-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1652-	# Turn left (Q)
1653-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1941-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1942-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1943-			#  by _0283.txt showing plane position update before stall)
1944:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1945-				if not ProjectSettings.has_setting("_headless_space_fired"):
1946-					ProjectSettings.set_setting("_headless_space_fired", true)
1947-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729014632 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729130249 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1537-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1538-		var _exit_pressed = (
1539-			Input.is_action_just_pressed("deploy")
1540:			or Input.is_key_pressed(KEY_J)
1541:			or _headless_auto_jump
1542-		)
1543:		_headless_auto_jump = false  # consume the flag
1544-		if _exit_pressed:
1545-			print("[DIAG] _poll_controls: exit aircraft triggered")
1546-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1352-	if current_time >= event["time"]:
1353-		match event["action"]:
1354-			"deploy":
1355:				_deploy_canopy()
1356-			"cutaway":
1357-				_do_cutaway()
1358-			"reserve":
--
1574-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1575-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1576-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1577:		_deploy_canopy()
1578-
1579-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1580-		var turn_input := 0.0
--
1612-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1613-			else:
1614-				print("[STEER_TELEM] /root/Main does not exist")
1615:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1616-		# ----- Direct key checks (bypass Input Map) -----
1617-		# Deploy (SPACE)
1618-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1619:		_deploy_canopy()
1620-	# Turn left (Q)
1621-	if Input.is_key_pressed(KEY_Q):
1622-		_turn_input = -1.0
--
1647-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1648-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1649-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1650:			_deploy_canopy()
1651-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1652-	# Turn left (Q)
1653-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1941-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1942-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1943-			#  by _0283.txt showing plane position update before stall)
1944:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1945-				if not ProjectSettings.has_setting("_headless_space_fired"):
1946-					ProjectSettings.set_setting("_headless_space_fired", true)
1947-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729130249 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729135040 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1536-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1537-		var _exit_pressed = (
1538-			Input.is_action_just_pressed("deploy")
1539:			or Input.is_key_pressed(KEY_J)
1540:			or _headless_auto_jump
1541-		)
1542:		_headless_auto_jump = false  # consume the flag
1543-		if _exit_pressed:
1544-			print("[DIAG] _poll_controls: exit aircraft triggered")
1545-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1351-	if current_time >= event["time"]:
1352-		match event["action"]:
1353-			"deploy":
1354:				_deploy_canopy()
1355-			"cutaway":
1356-				_do_cutaway()
1357-			"reserve":
--
1573-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1574-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1575-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1576:		_deploy_canopy()
1577-
1578-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1579-		var turn_input := 0.0
--
1611-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1612-			else:
1613-				print("[STEER_TELEM] /root/Main does not exist")
1614:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1615-		# ----- Direct key checks (bypass Input Map) -----
1616-		# Deploy (SPACE)
1617-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1618:		_deploy_canopy()
1619-	# Turn left (Q)
1620-	if Input.is_key_pressed(KEY_Q):
1621-		_turn_input = -1.0
--
1646-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1647-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1648-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1649:			_deploy_canopy()
1650-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1651-	# Turn left (Q)
1652-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1940-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1941-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1942-			#  by _0283.txt showing plane position update before stall)
1943:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1944-				if not ProjectSettings.has_setting("_headless_space_fired"):
1945-					ProjectSettings.set_setting("_headless_space_fired", true)
1946-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729135040 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729141016 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1939-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1940-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1941-			#  by _0283.txt showing plane position update before stall)
1942:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1943-				if not ProjectSettings.has_setting("_headless_space_fired"):
1944-					ProjectSettings.set_setting("_headless_space_fired", true)
1945-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729141016 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729141726 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1939-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1940-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1941-			#  by _0283.txt showing plane position update before stall)
1942:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1943-				if not ProjectSettings.has_setting("_headless_space_fired"):
1944-					ProjectSettings.set_setting("_headless_space_fired", true)
1945-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729141726 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729141915 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1939-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1940-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1941-			#  by _0283.txt showing plane position update before stall)
1942:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1943-				if not ProjectSettings.has_setting("_headless_space_fired"):
1944-					ProjectSettings.set_setting("_headless_space_fired", true)
1945-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729141915 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729142819 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1939-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1940-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1941-			#  by _0283.txt showing plane position update before stall)
1942:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1943-				if not ProjectSettings.has_setting("_headless_space_fired"):
1944-					ProjectSettings.set_setting("_headless_space_fired", true)
1945-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729142819 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729144519 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1939-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1940-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1941-			#  by _0283.txt showing plane position update before stall)
1942:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1943-				if not ProjectSettings.has_setting("_headless_space_fired"):
1944-					ProjectSettings.set_setting("_headless_space_fired", true)
1945-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729144519 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729150217 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1939-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1940-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1941-			#  by _0283.txt showing plane position update before stall)
1942:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1943-				if not ProjectSettings.has_setting("_headless_space_fired"):
1944-					ProjectSettings.set_setting("_headless_space_fired", true)
1945-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729150217 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729191024 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1939-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1940-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1941-			#  by _0283.txt showing plane position update before stall)
1942:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1943-				if not ProjectSettings.has_setting("_headless_space_fired"):
1944-					ProjectSettings.set_setting("_headless_space_fired", true)
1945-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729191024 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729191156 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1939-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1940-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1941-			#  by _0283.txt showing plane position update before stall)
1942:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1943-				if not ProjectSettings.has_setting("_headless_space_fired"):
1944-					ProjectSettings.set_setting("_headless_space_fired", true)
1945-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729191156 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729192116 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1940-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1941-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1942-			#  by _0283.txt showing plane position update before stall)
1943:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1944-				if not ProjectSettings.has_setting("_headless_space_fired"):
1945-					ProjectSettings.set_setting("_headless_space_fired", true)
1946-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729192116 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729192542 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1940-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1941-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1942-			#  by _0283.txt showing plane position update before stall)
1943:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1944-				if not ProjectSettings.has_setting("_headless_space_fired"):
1945-					ProjectSettings.set_setting("_headless_space_fired", true)
1946-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729192542 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729193332 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1940-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1941-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1942-			#  by _0283.txt showing plane position update before stall)
1943:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1944-				if not ProjectSettings.has_setting("_headless_space_fired"):
1945-					ProjectSettings.set_setting("_headless_space_fired", true)
1946-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729193332 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729194535 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1940-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1941-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1942-			#  by _0283.txt showing plane position update before stall)
1943:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1944-				if not ProjectSettings.has_setting("_headless_space_fired"):
1945-					ProjectSettings.set_setting("_headless_space_fired", true)
1946-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729194535 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729195031 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1941-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1942-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1943-			#  by _0283.txt showing plane position update before stall)
1944:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1945-				if not ProjectSettings.has_setting("_headless_space_fired"):
1946-					ProjectSettings.set_setting("_headless_space_fired", true)
1947-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729195031 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729204959 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3464-	"togglehud":    KEY_H,
3465-	"flightcheck":  KEY_G,
3466-	"restart":      KEY_R,
3467:	"j":            KEY_J,
3468-	"pause":        KEY_ESCAPE,
3469-}
3470-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1943-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1944-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1945-			#  by _0283.txt showing plane position update before stall)
1946:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1947-				if not ProjectSettings.has_setting("_headless_space_fired"):
1948-					ProjectSettings.set_setting("_headless_space_fired", true)
1949-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729204959 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729205524 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3464-	"togglehud":    KEY_H,
3465-	"flightcheck":  KEY_G,
3466-	"restart":      KEY_R,
3467:	"j":            KEY_J,
3468-	"pause":        KEY_ESCAPE,
3469-}
3470-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1943-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1944-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1945-			#  by _0283.txt showing plane position update before stall)
1946:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1947-				if not ProjectSettings.has_setting("_headless_space_fired"):
1948-					ProjectSettings.set_setting("_headless_space_fired", true)
1949-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729205524 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260729205620 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3464-	"togglehud":    KEY_H,
3465-	"flightcheck":  KEY_G,
3466-	"restart":      KEY_R,
3467:	"j":            KEY_J,
3468-	"pause":        KEY_ESCAPE,
3469-}
3470-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1943-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1944-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1945-			#  by _0283.txt showing plane position update before stall)
1946:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1947-				if not ProjectSettings.has_setting("_headless_space_fired"):
1948-					ProjectSettings.set_setting("_headless_space_fired", true)
1949-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260729205620 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260730123216 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3464-	"togglehud":    KEY_H,
3465-	"flightcheck":  KEY_G,
3466-	"restart":      KEY_R,
3467:	"j":            KEY_J,
3468-	"pause":        KEY_ESCAPE,
3469-}
3470-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1943-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1944-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1945-			#  by _0283.txt showing plane position update before stall)
1946:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1947-				if not ProjectSettings.has_setting("_headless_space_fired"):
1948-					ProjectSettings.set_setting("_headless_space_fired", true)
1949-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260730123216 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260730125028 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
543-	_check_arm_pose_safe()
--
1531-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1532-		var _exit_pressed = (
1533-			Input.is_action_just_pressed("deploy")
1534:			or Input.is_key_pressed(KEY_J)
1535:			or _headless_auto_jump
1536-		)
1537:		_headless_auto_jump = false  # consume the flag
1538-		if _exit_pressed:
1539-			print("[DIAG] _poll_controls: exit aircraft triggered")
1540-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3478-	"togglehud":    KEY_H,
3479-	"flightcheck":  KEY_G,
3480-	"restart":      KEY_R,
3481:	"j":            KEY_J,
3482-	"pause":        KEY_ESCAPE,
3483-}
3484-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
811-	return null
812-
813-
814:func _deploy_canopy() -> void:
815-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
816-	if _canopy_deployed or not _canopy_instance:
817-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1346-	if current_time >= event["time"]:
1347-		match event["action"]:
1348-			"deploy":
1349:				_deploy_canopy()
1350-			"cutaway":
1351-				_do_cutaway()
1352-			"reserve":
--
1568-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1569-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1570-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1571:		_deploy_canopy()
1572-
1573-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1574-		var turn_input := 0.0
--
1606-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1607-			else:
1608-				print("[STEER_TELEM] /root/Main does not exist")
1609:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1610-		# ----- Direct key checks (bypass Input Map) -----
1611-		# Deploy (SPACE)
1612-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1613:		_deploy_canopy()
1614-	# Turn left (Q)
1615-	if Input.is_key_pressed(KEY_Q):
1616-		_turn_input = -1.0
--
1641-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1642-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1643-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1644:			_deploy_canopy()
1645-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1646-	# Turn left (Q)
1647-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
536-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
537-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
538-	# Headless auto‑start: simulate SPACE press
539:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
540-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
541-		# Input.action_release("deploy") removed — flag-based now
542-		print("[VERBATIM] Headless auto‑start triggered.")
--
1943-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1944-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1945-			#  by _0283.txt showing plane position update before stall)
1946:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1947-				if not ProjectSettings.has_setting("_headless_space_fired"):
1948-					ProjectSettings.set_setting("_headless_space_fired", true)
1949-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260730125028 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260730164901 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
538-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
539-	# Headless auto‑start: simulate SPACE press
540-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
541:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
542-		# Input.action_release("deploy") removed — flag-based now
543-		print("[VERBATIM] Headless auto‑start triggered.")
544-	_check_arm_pose_safe()
--
1541-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1542-		var _exit_pressed = (
1543-			Input.is_action_just_pressed("deploy")
1544:			or Input.is_key_pressed(KEY_J)
1545:			or _headless_auto_jump
1546-		)
1547:		_headless_auto_jump = false  # consume the flag
1548-		if _exit_pressed:
1549-			print("[DIAG] _poll_controls: exit aircraft triggered")
1550-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3488-	"togglehud":    KEY_H,
3489-	"flightcheck":  KEY_G,
3490-	"restart":      KEY_R,
3491:	"j":            KEY_J,
3492-	"pause":        KEY_ESCAPE,
3493-}
3494-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1578-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1579-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1580-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1581:		_deploy_canopy()
1582-
1583-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1584-		var turn_input := 0.0
--
1616-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1617-			else:
1618-				print("[STEER_TELEM] /root/Main does not exist")
1619:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1620-		# ----- Direct key checks (bypass Input Map) -----
1621-		# Deploy (SPACE)
1622-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1623:		_deploy_canopy()
1624-	# Turn left (Q)
1625-	if Input.is_key_pressed(KEY_Q):
1626-		_turn_input = -1.0
--
1651-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1652-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1653-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1654:			_deploy_canopy()
1655-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1656-	# Turn left (Q)
1657-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
537-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
538-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
539-	# Headless auto‑start: simulate SPACE press
540:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
541-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
542-		# Input.action_release("deploy") removed — flag-based now
543-		print("[VERBATIM] Headless auto‑start triggered.")
--
1953-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1954-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1955-			#  by _0283.txt showing plane position update before stall)
1956:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1957-				if not ProjectSettings.has_setting("_headless_space_fired"):
1958-					ProjectSettings.set_setting("_headless_space_fired", true)
1959-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260730164901 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260730165932 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
538-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
539-	# Headless auto‑start: simulate SPACE press
540-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
541:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
542-		# Input.action_release("deploy") removed — flag-based now
543-		print("[VERBATIM] Headless auto‑start triggered.")
544-	_check_arm_pose_safe()
--
1541-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1542-		var _exit_pressed = (
1543-			Input.is_action_just_pressed("deploy")
1544:			or Input.is_key_pressed(KEY_J)
1545:			or _headless_auto_jump
1546-		)
1547:		_headless_auto_jump = false  # consume the flag
1548-		if _exit_pressed:
1549-			print("[DIAG] _poll_controls: exit aircraft triggered")
1550-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3488-	"togglehud":    KEY_H,
3489-	"flightcheck":  KEY_G,
3490-	"restart":      KEY_R,
3491:	"j":            KEY_J,
3492-	"pause":        KEY_ESCAPE,
3493-}
3494-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1578-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1579-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1580-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1581:		_deploy_canopy()
1582-
1583-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1584-		var turn_input := 0.0
--
1616-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1617-			else:
1618-				print("[STEER_TELEM] /root/Main does not exist")
1619:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1620-		# ----- Direct key checks (bypass Input Map) -----
1621-		# Deploy (SPACE)
1622-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1623:		_deploy_canopy()
1624-	# Turn left (Q)
1625-	if Input.is_key_pressed(KEY_Q):
1626-		_turn_input = -1.0
--
1651-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1652-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1653-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1654:			_deploy_canopy()
1655-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1656-	# Turn left (Q)
1657-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
537-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
538-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
539-	# Headless auto‑start: simulate SPACE press
540:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
541-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
542-		# Input.action_release("deploy") removed — flag-based now
543-		print("[VERBATIM] Headless auto‑start triggered.")
--
1953-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1954-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1955-			#  by _0283.txt showing plane position update before stall)
1956:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1957-				if not ProjectSettings.has_setting("_headless_space_fired"):
1958-					ProjectSettings.set_setting("_headless_space_fired", true)
1959-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260730165932 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260730170438 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
538-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
539-	# Headless auto‑start: simulate SPACE press
540-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
541:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
542-		# Input.action_release("deploy") removed — flag-based now
543-		print("[VERBATIM] Headless auto‑start triggered.")
544-	_check_arm_pose_safe()
--
1541-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1542-		var _exit_pressed = (
1543-			Input.is_action_just_pressed("deploy")
1544:			or Input.is_key_pressed(KEY_J)
1545:			or _headless_auto_jump
1546-		)
1547:		_headless_auto_jump = false  # consume the flag
1548-		if _exit_pressed:
1549-			print("[DIAG] _poll_controls: exit aircraft triggered")
1550-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3488-	"togglehud":    KEY_H,
3489-	"flightcheck":  KEY_G,
3490-	"restart":      KEY_R,
3491:	"j":            KEY_J,
3492-	"pause":        KEY_ESCAPE,
3493-}
3494-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1578-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1579-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1580-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1581:		_deploy_canopy()
1582-
1583-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1584-		var turn_input := 0.0
--
1616-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1617-			else:
1618-				print("[STEER_TELEM] /root/Main does not exist")
1619:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1620-		# ----- Direct key checks (bypass Input Map) -----
1621-		# Deploy (SPACE)
1622-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1623:		_deploy_canopy()
1624-	# Turn left (Q)
1625-	if Input.is_key_pressed(KEY_Q):
1626-		_turn_input = -1.0
--
1651-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1652-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1653-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1654:			_deploy_canopy()
1655-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1656-	# Turn left (Q)
1657-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
537-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
538-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
539-	# Headless auto‑start: simulate SPACE press
540:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
541-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
542-		# Input.action_release("deploy") removed — flag-based now
543-		print("[VERBATIM] Headless auto‑start triggered.")
--
1953-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1954-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1955-			#  by _0283.txt showing plane position update before stall)
1956:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1957-				if not ProjectSettings.has_setting("_headless_space_fired"):
1958-					ProjectSettings.set_setting("_headless_space_fired", true)
1959-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260730170438 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260730233326 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
538-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
539-	# Headless auto‑start: simulate SPACE press
540-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
541:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
542-		# Input.action_release("deploy") removed — flag-based now
543-		print("[VERBATIM] Headless auto‑start triggered.")
544-	_check_arm_pose_safe()
--
1541-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1542-		var _exit_pressed = (
1543-			Input.is_action_just_pressed("deploy")
1544:			or Input.is_key_pressed(KEY_J)
1545:			or _headless_auto_jump
1546-		)
1547:		_headless_auto_jump = false  # consume the flag
1548-		if _exit_pressed:
1549-			print("[DIAG] _poll_controls: exit aircraft triggered")
1550-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3501-	"togglehud":    KEY_H,
3502-	"flightcheck":  KEY_G,
3503-	"restart":      KEY_R,
3504:	"j":            KEY_J,
3505-	"pause":        KEY_ESCAPE,
3506-}
3507-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
812-	return null
813-
814-
815:func _deploy_canopy() -> void:
816-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
817-	if _canopy_deployed or not _canopy_instance:
818-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1347-	if current_time >= event["time"]:
1348-		match event["action"]:
1349-			"deploy":
1350:				_deploy_canopy()
1351-			"cutaway":
1352-				_do_cutaway()
1353-			"reserve":
--
1578-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1579-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1580-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1581:		_deploy_canopy()
1582-
1583-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1584-		var turn_input := 0.0
--
1616-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1617-			else:
1618-				print("[STEER_TELEM] /root/Main does not exist")
1619:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1620-		# ----- Direct key checks (bypass Input Map) -----
1621-		# Deploy (SPACE)
1622-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1623:		_deploy_canopy()
1624-	# Turn left (Q)
1625-	if Input.is_key_pressed(KEY_Q):
1626-		_turn_input = -1.0
--
1651-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1652-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1653-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1654:			_deploy_canopy()
1655-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1656-	# Turn left (Q)
1657-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
537-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
538-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
539-	# Headless auto‑start: simulate SPACE press
540:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
541-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
542-		# Input.action_release("deploy") removed — flag-based now
543-		print("[VERBATIM] Headless auto‑start triggered.")
--
1951-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1952-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1953-			#  by _0283.txt showing plane position update before stall)
1954:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1955-				if not ProjectSettings.has_setting("_headless_space_fired"):
1956-					ProjectSettings.set_setting("_headless_space_fired", true)
1957-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260730233326 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260731001411 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1547-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1548-		var _exit_pressed = (
1549-			Input.is_action_just_pressed("deploy")
1550:			or Input.is_key_pressed(KEY_J)
1551:			or _headless_auto_jump
1552-		)
1553:		_headless_auto_jump = false  # consume the flag
1554-		if _exit_pressed:
1555-			print("[DIAG] _poll_controls: exit aircraft triggered")
1556-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3507-	"togglehud":    KEY_H,
3508-	"flightcheck":  KEY_G,
3509-	"restart":      KEY_R,
3510:	"j":            KEY_J,
3511-	"pause":        KEY_ESCAPE,
3512-}
3513-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1353-	if current_time >= event["time"]:
1354-		match event["action"]:
1355-			"deploy":
1356:				_deploy_canopy()
1357-			"cutaway":
1358-				_do_cutaway()
1359-			"reserve":
--
1584-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1585-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1586-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1587:		_deploy_canopy()
1588-
1589-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1590-		var turn_input := 0.0
--
1622-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1623-			else:
1624-				print("[STEER_TELEM] /root/Main does not exist")
1625:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1626-		# ----- Direct key checks (bypass Input Map) -----
1627-		# Deploy (SPACE)
1628-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1629:		_deploy_canopy()
1630-	# Turn left (Q)
1631-	if Input.is_key_pressed(KEY_Q):
1632-		_turn_input = -1.0
--
1657-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1658-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1659-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1660:			_deploy_canopy()
1661-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1662-	# Turn left (Q)
1663-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1957-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1958-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1959-			#  by _0283.txt showing plane position update before stall)
1960:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1961-				if not ProjectSettings.has_setting("_headless_space_fired"):
1962-					ProjectSettings.set_setting("_headless_space_fired", true)
1963-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260731001411 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260731003707 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1547-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1548-		var _exit_pressed = (
1549-			Input.is_action_just_pressed("deploy")
1550:			or Input.is_key_pressed(KEY_J)
1551:			or _headless_auto_jump
1552-		)
1553:		_headless_auto_jump = false  # consume the flag
1554-		if _exit_pressed:
1555-			print("[DIAG] _poll_controls: exit aircraft triggered")
1556-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3507-	"togglehud":    KEY_H,
3508-	"flightcheck":  KEY_G,
3509-	"restart":      KEY_R,
3510:	"j":            KEY_J,
3511-	"pause":        KEY_ESCAPE,
3512-}
3513-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1353-	if current_time >= event["time"]:
1354-		match event["action"]:
1355-			"deploy":
1356:				_deploy_canopy()
1357-			"cutaway":
1358-				_do_cutaway()
1359-			"reserve":
--
1584-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1585-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1586-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1587:		_deploy_canopy()
1588-
1589-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1590-		var turn_input := 0.0
--
1622-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1623-			else:
1624-				print("[STEER_TELEM] /root/Main does not exist")
1625:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1626-		# ----- Direct key checks (bypass Input Map) -----
1627-		# Deploy (SPACE)
1628-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1629:		_deploy_canopy()
1630-	# Turn left (Q)
1631-	if Input.is_key_pressed(KEY_Q):
1632-		_turn_input = -1.0
--
1657-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1658-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1659-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1660:			_deploy_canopy()
1661-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1662-	# Turn left (Q)
1663-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1957-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1958-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1959-			#  by _0283.txt showing plane position update before stall)
1960:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1961-				if not ProjectSettings.has_setting("_headless_space_fired"):
1962-					ProjectSettings.set_setting("_headless_space_fired", true)
1963-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260731003707 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260731004756 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1547-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1548-		var _exit_pressed = (
1549-			Input.is_action_just_pressed("deploy")
1550:			or Input.is_key_pressed(KEY_J)
1551:			or _headless_auto_jump
1552-		)
1553:		_headless_auto_jump = false  # consume the flag
1554-		if _exit_pressed:
1555-			print("[DIAG] _poll_controls: exit aircraft triggered")
1556-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3507-	"togglehud":    KEY_H,
3508-	"flightcheck":  KEY_G,
3509-	"restart":      KEY_R,
3510:	"j":            KEY_J,
3511-	"pause":        KEY_ESCAPE,
3512-}
3513-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1353-	if current_time >= event["time"]:
1354-		match event["action"]:
1355-			"deploy":
1356:				_deploy_canopy()
1357-			"cutaway":
1358-				_do_cutaway()
1359-			"reserve":
--
1584-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1585-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1586-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1587:		_deploy_canopy()
1588-
1589-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1590-		var turn_input := 0.0
--
1622-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1623-			else:
1624-				print("[STEER_TELEM] /root/Main does not exist")
1625:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1626-		# ----- Direct key checks (bypass Input Map) -----
1627-		# Deploy (SPACE)
1628-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1629:		_deploy_canopy()
1630-	# Turn left (Q)
1631-	if Input.is_key_pressed(KEY_Q):
1632-		_turn_input = -1.0
--
1657-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1658-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1659-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1660:			_deploy_canopy()
1661-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1662-	# Turn left (Q)
1663-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1957-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1958-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1959-			#  by _0283.txt showing plane position update before stall)
1960:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1961-				if not ProjectSettings.has_setting("_headless_space_fired"):
1962-					ProjectSettings.set_setting("_headless_space_fired", true)
1963-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260731004756 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260731131248 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1547-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1548-		var _exit_pressed = (
1549-			Input.is_action_just_pressed("deploy")
1550:			or Input.is_key_pressed(KEY_J)
1551:			or _headless_auto_jump
1552-		)
1553:		_headless_auto_jump = false  # consume the flag
1554-		if _exit_pressed:
1555-			print("[DIAG] _poll_controls: exit aircraft triggered")
1556-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3507-	"togglehud":    KEY_H,
3508-	"flightcheck":  KEY_G,
3509-	"restart":      KEY_R,
3510:	"j":            KEY_J,
3511-	"pause":        KEY_ESCAPE,
3512-}
3513-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1353-	if current_time >= event["time"]:
1354-		match event["action"]:
1355-			"deploy":
1356:				_deploy_canopy()
1357-			"cutaway":
1358-				_do_cutaway()
1359-			"reserve":
--
1584-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1585-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1586-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1587:		_deploy_canopy()
1588-
1589-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1590-		var turn_input := 0.0
--
1622-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1623-			else:
1624-				print("[STEER_TELEM] /root/Main does not exist")
1625:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1626-		# ----- Direct key checks (bypass Input Map) -----
1627-		# Deploy (SPACE)
1628-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1629:		_deploy_canopy()
1630-	# Turn left (Q)
1631-	if Input.is_key_pressed(KEY_Q):
1632-		_turn_input = -1.0
--
1657-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1658-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1659-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1660:			_deploy_canopy()
1661-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1662-	# Turn left (Q)
1663-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1957-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1958-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1959-			#  by _0283.txt showing plane position update before stall)
1960:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1961-				if not ProjectSettings.has_setting("_headless_space_fired"):
1962-					ProjectSettings.set_setting("_headless_space_fired", true)
1963-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260731131248 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260731132644 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1547-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1548-		var _exit_pressed = (
1549-			Input.is_action_just_pressed("deploy")
1550:			or Input.is_key_pressed(KEY_J)
1551:			or _headless_auto_jump
1552-		)
1553:		_headless_auto_jump = false  # consume the flag
1554-		if _exit_pressed:
1555-			print("[DIAG] _poll_controls: exit aircraft triggered")
1556-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3511-	"togglehud":    KEY_H,
3512-	"flightcheck":  KEY_G,
3513-	"restart":      KEY_R,
3514:	"j":            KEY_J,
3515-	"pause":        KEY_ESCAPE,
3516-}
3517-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1353-	if current_time >= event["time"]:
1354-		match event["action"]:
1355-			"deploy":
1356:				_deploy_canopy()
1357-			"cutaway":
1358-				_do_cutaway()
1359-			"reserve":
--
1584-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1585-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1586-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1587:		_deploy_canopy()
1588-
1589-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1590-		var turn_input := 0.0
--
1622-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1623-			else:
1624-				print("[STEER_TELEM] /root/Main does not exist")
1625:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1626-		# ----- Direct key checks (bypass Input Map) -----
1627-		# Deploy (SPACE)
1628-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1629:		_deploy_canopy()
1630-	# Turn left (Q)
1631-	if Input.is_key_pressed(KEY_Q):
1632-		_turn_input = -1.0
--
1657-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1658-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1659-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1660:			_deploy_canopy()
1661-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1662-	# Turn left (Q)
1663-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1957-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1958-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1959-			#  by _0283.txt showing plane position update before stall)
1960:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1961-				if not ProjectSettings.has_setting("_headless_space_fired"):
1962-					ProjectSettings.set_setting("_headless_space_fired", true)
1963-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260731132644 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260731133047 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1547-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1548-		var _exit_pressed = (
1549-			Input.is_action_just_pressed("deploy")
1550:			or Input.is_key_pressed(KEY_J)
1551:			or _headless_auto_jump
1552-		)
1553:		_headless_auto_jump = false  # consume the flag
1554-		if _exit_pressed:
1555-			print("[DIAG] _poll_controls: exit aircraft triggered")
1556-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3477-	"togglehud":    KEY_H,
3478-	"flightcheck":  KEY_G,
3479-	"restart":      KEY_R,
3480:	"j":            KEY_J,
3481-	"pause":        KEY_ESCAPE,
3482-}
3483-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1353-	if current_time >= event["time"]:
1354-		match event["action"]:
1355-			"deploy":
1356:				_deploy_canopy()
1357-			"cutaway":
1358-				_do_cutaway()
1359-			"reserve":
--
1584-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1585-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1586-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1587:		_deploy_canopy()
1588-
1589-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1590-		var turn_input := 0.0
--
1604-
1605-		_turn_input = turn_input
1606-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1607:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1608-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1609-# canopy could never accumulate a turn. Guard was commented out; call removed.
1610-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1623-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1624-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1625-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1626:			_deploy_canopy()
1627-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1628-	# Turn left (Q)
1629-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1923-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1924-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1925-			#  by _0283.txt showing plane position update before stall)
1926:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1927-				if not ProjectSettings.has_setting("_headless_space_fired"):
1928-					ProjectSettings.set_setting("_headless_space_fired", true)
1929-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260731133047 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260801142925 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1547-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1548-		var _exit_pressed = (
1549-			Input.is_action_just_pressed("deploy")
1550:			or Input.is_key_pressed(KEY_J)
1551:			or _headless_auto_jump
1552-		)
1553:		_headless_auto_jump = false  # consume the flag
1554-		if _exit_pressed:
1555-			print("[DIAG] _poll_controls: exit aircraft triggered")
1556-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3477-	"togglehud":    KEY_H,
3478-	"flightcheck":  KEY_G,
3479-	"restart":      KEY_R,
3480:	"j":            KEY_J,
3481-	"pause":        KEY_ESCAPE,
3482-}
3483-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1353-	if current_time >= event["time"]:
1354-		match event["action"]:
1355-			"deploy":
1356:				_deploy_canopy()
1357-			"cutaway":
1358-				_do_cutaway()
1359-			"reserve":
--
1584-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1585-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1586-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1587:		_deploy_canopy()
1588-
1589-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1590-		var turn_input := 0.0
--
1604-
1605-		_turn_input = turn_input
1606-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1607:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1608-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1609-# canopy could never accumulate a turn. Guard was commented out; call removed.
1610-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1623-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1624-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1625-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1626:			_deploy_canopy()
1627-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1628-	# Turn left (Q)
1629-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1923-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1924-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1925-			#  by _0283.txt showing plane position update before stall)
1926:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1927-				if not ProjectSettings.has_setting("_headless_space_fired"):
1928-					ProjectSettings.set_setting("_headless_space_fired", true)
1929-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260801142925 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260801202543 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1547-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1548-		var _exit_pressed = (
1549-			Input.is_action_just_pressed("deploy")
1550:			or Input.is_key_pressed(KEY_J)
1551:			or _headless_auto_jump
1552-		)
1553:		_headless_auto_jump = false  # consume the flag
1554-		if _exit_pressed:
1555-			print("[DIAG] _poll_controls: exit aircraft triggered")
1556-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3477-	"togglehud":    KEY_H,
3478-	"flightcheck":  KEY_G,
3479-	"restart":      KEY_R,
3480:	"j":            KEY_J,
3481-	"pause":        KEY_ESCAPE,
3482-}
3483-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1353-	if current_time >= event["time"]:
1354-		match event["action"]:
1355-			"deploy":
1356:				_deploy_canopy()
1357-			"cutaway":
1358-				_do_cutaway()
1359-			"reserve":
--
1584-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1585-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1586-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1587:		_deploy_canopy()
1588-
1589-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1590-		var turn_input := 0.0
--
1604-
1605-		_turn_input = turn_input
1606-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1607:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1608-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1609-# canopy could never accumulate a turn. Guard was commented out; call removed.
1610-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1623-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1624-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1625-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1626:			_deploy_canopy()
1627-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1628-	# Turn left (Q)
1629-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1923-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1924-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1925-			#  by _0283.txt showing plane position update before stall)
1926:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1927-				if not ProjectSettings.has_setting("_headless_space_fired"):
1928-					ProjectSettings.set_setting("_headless_space_fired", true)
1929-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260801202543 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260801204134 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1547-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1548-		var _exit_pressed = (
1549-			Input.is_action_just_pressed("deploy")
1550:			or Input.is_key_pressed(KEY_J)
1551:			or _headless_auto_jump
1552-		)
1553:		_headless_auto_jump = false  # consume the flag
1554-		if _exit_pressed:
1555-			print("[DIAG] _poll_controls: exit aircraft triggered")
1556-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3477-	"togglehud":    KEY_H,
3478-	"flightcheck":  KEY_G,
3479-	"restart":      KEY_R,
3480:	"j":            KEY_J,
3481-	"pause":        KEY_ESCAPE,
3482-}
3483-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1353-	if current_time >= event["time"]:
1354-		match event["action"]:
1355-			"deploy":
1356:				_deploy_canopy()
1357-			"cutaway":
1358-				_do_cutaway()
1359-			"reserve":
--
1584-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1585-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1586-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1587:		_deploy_canopy()
1588-
1589-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1590-		var turn_input := 0.0
--
1604-
1605-		_turn_input = turn_input
1606-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1607:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1608-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1609-# canopy could never accumulate a turn. Guard was commented out; call removed.
1610-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1623-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1624-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1625-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1626:			_deploy_canopy()
1627-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1628-	# Turn left (Q)
1629-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1923-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1924-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1925-			#  by _0283.txt showing plane position update before stall)
1926:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1927-				if not ProjectSettings.has_setting("_headless_space_fired"):
1928-					ProjectSettings.set_setting("_headless_space_fired", true)
1929-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260801204134 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260801205254 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3496-	"togglehud":    KEY_H,
3497-	"flightcheck":  KEY_G,
3498-	"restart":      KEY_R,
3499:	"j":            KEY_J,
3500-	"pause":        KEY_ESCAPE,
3501-}
3502-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1368-	if current_time >= event["time"]:
1369-		match event["action"]:
1370-			"deploy":
1371:				_deploy_canopy()
1372-			"cutaway":
1373-				_do_cutaway()
1374-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1623-
1624-		_turn_input = turn_input
1625-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1626:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1627-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1628-# canopy could never accumulate a turn. Guard was commented out; call removed.
1629-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1642-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1643-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1644-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1645:			_deploy_canopy()
1646-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1647-	# Turn left (Q)
1648-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1942-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1943-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1944-			#  by _0283.txt showing plane position update before stall)
1945:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1946-				if not ProjectSettings.has_setting("_headless_space_fired"):
1947-					ProjectSettings.set_setting("_headless_space_fired", true)
1948-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260801205254 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260801205603 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1574-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1575-		var _exit_pressed = (
1576-			Input.is_action_just_pressed("deploy")
1577:			or Input.is_key_pressed(KEY_J)
1578:			or _headless_auto_jump
1579-		)
1580:		_headless_auto_jump = false  # consume the flag
1581-		if _exit_pressed:
1582-			print("[DIAG] _poll_controls: exit aircraft triggered")
1583-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3504-	"togglehud":    KEY_H,
3505-	"flightcheck":  KEY_G,
3506-	"restart":      KEY_R,
3507:	"j":            KEY_J,
3508-	"pause":        KEY_ESCAPE,
3509-}
3510-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1376-	if current_time >= event["time"]:
1377-		match event["action"]:
1378-			"deploy":
1379:				_deploy_canopy()
1380-			"cutaway":
1381-				_do_cutaway()
1382-			"reserve":
--
1611-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1612-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1613-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1614:		_deploy_canopy()
1615-
1616-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1617-		var turn_input := 0.0
--
1631-
1632-		_turn_input = turn_input
1633-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1634:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1635-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1636-# canopy could never accumulate a turn. Guard was commented out; call removed.
1637-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1650-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1651-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1652-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1653:			_deploy_canopy()
1654-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1655-	# Turn left (Q)
1656-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1950-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1951-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1952-			#  by _0283.txt showing plane position update before stall)
1953:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1954-				if not ProjectSettings.has_setting("_headless_space_fired"):
1955-					ProjectSettings.set_setting("_headless_space_fired", true)
1956-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260801205603 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260801210504 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1576-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1577-		var _exit_pressed = (
1578-			Input.is_action_just_pressed("deploy")
1579:			or Input.is_key_pressed(KEY_J)
1580:			or _headless_auto_jump
1581-		)
1582:		_headless_auto_jump = false  # consume the flag
1583-		if _exit_pressed:
1584-			print("[DIAG] _poll_controls: exit aircraft triggered")
1585-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3506-	"togglehud":    KEY_H,
3507-	"flightcheck":  KEY_G,
3508-	"restart":      KEY_R,
3509:	"j":            KEY_J,
3510-	"pause":        KEY_ESCAPE,
3511-}
3512-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1378-	if current_time >= event["time"]:
1379-		match event["action"]:
1380-			"deploy":
1381:				_deploy_canopy()
1382-			"cutaway":
1383-				_do_cutaway()
1384-			"reserve":
--
1613-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1614-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1615-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1616:		_deploy_canopy()
1617-
1618-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1619-		var turn_input := 0.0
--
1633-
1634-		_turn_input = turn_input
1635-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1636:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1637-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1638-# canopy could never accumulate a turn. Guard was commented out; call removed.
1639-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1652-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1653-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1654-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1655:			_deploy_canopy()
1656-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1657-	# Turn left (Q)
1658-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1952-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1953-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1954-			#  by _0283.txt showing plane position update before stall)
1955:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1956-				if not ProjectSettings.has_setting("_headless_space_fired"):
1957-					ProjectSettings.set_setting("_headless_space_fired", true)
1958-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260801210504 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260801211000 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1576-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1577-		var _exit_pressed = (
1578-			Input.is_action_just_pressed("deploy")
1579:			or Input.is_key_pressed(KEY_J)
1580:			or _headless_auto_jump
1581-		)
1582:		_headless_auto_jump = false  # consume the flag
1583-		if _exit_pressed:
1584-			print("[DIAG] _poll_controls: exit aircraft triggered")
1585-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3512-	"togglehud":    KEY_H,
3513-	"flightcheck":  KEY_G,
3514-	"restart":      KEY_R,
3515:	"j":            KEY_J,
3516-	"pause":        KEY_ESCAPE,
3517-}
3518-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1378-	if current_time >= event["time"]:
1379-		match event["action"]:
1380-			"deploy":
1381:				_deploy_canopy()
1382-			"cutaway":
1383-				_do_cutaway()
1384-			"reserve":
--
1613-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1614-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1615-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1616:		_deploy_canopy()
1617-
1618-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1619-		var turn_input := 0.0
--
1633-
1634-		_turn_input = turn_input
1635-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1636:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1637-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1638-# canopy could never accumulate a turn. Guard was commented out; call removed.
1639-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1652-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1653-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1654-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1655:			_deploy_canopy()
1656-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1657-	# Turn left (Q)
1658-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1952-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1953-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1954-			#  by _0283.txt showing plane position update before stall)
1955:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1956-				if not ProjectSettings.has_setting("_headless_space_fired"):
1957-					ProjectSettings.set_setting("_headless_space_fired", true)
1958-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260801211000 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802005936 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1577-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1578-		var _exit_pressed = (
1579-			Input.is_action_just_pressed("deploy")
1580:			or Input.is_key_pressed(KEY_J)
1581:			or _headless_auto_jump
1582-		)
1583:		_headless_auto_jump = false  # consume the flag
1584-		if _exit_pressed:
1585-			print("[DIAG] _poll_controls: exit aircraft triggered")
1586-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3513-	"togglehud":    KEY_H,
3514-	"flightcheck":  KEY_G,
3515-	"restart":      KEY_R,
3516:	"j":            KEY_J,
3517-	"pause":        KEY_ESCAPE,
3518-}
3519-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
818-	return null
819-
820-
821:func _deploy_canopy() -> void:
822-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
823-	if _canopy_deployed or not _canopy_instance:
824-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1379-	if current_time >= event["time"]:
1380-		match event["action"]:
1381-			"deploy":
1382:				_deploy_canopy()
1383-			"cutaway":
1384-				_do_cutaway()
1385-			"reserve":
--
1614-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1615-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1616-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1617:		_deploy_canopy()
1618-
1619-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1620-		var turn_input := 0.0
--
1634-
1635-		_turn_input = turn_input
1636-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1637:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1638-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1639-# canopy could never accumulate a turn. Guard was commented out; call removed.
1640-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1653-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1654-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1655-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1656:			_deploy_canopy()
1657-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1658-	# Turn left (Q)
1659-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1953-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1954-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1955-			#  by _0283.txt showing plane position update before stall)
1956:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1957-				if not ProjectSettings.has_setting("_headless_space_fired"):
1958-					ProjectSettings.set_setting("_headless_space_fired", true)
1959-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802005936 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802011150 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1578-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1579-		var _exit_pressed = (
1580-			Input.is_action_just_pressed("deploy")
1581:			or Input.is_key_pressed(KEY_J)
1582:			or _headless_auto_jump
1583-		)
1584:		_headless_auto_jump = false  # consume the flag
1585-		if _exit_pressed:
1586-			print("[DIAG] _poll_controls: exit aircraft triggered")
1587-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3525-	"togglehud":    KEY_H,
3526-	"flightcheck":  KEY_G,
3527-	"restart":      KEY_R,
3528:	"j":            KEY_J,
3529-	"pause":        KEY_ESCAPE,
3530-}
3531-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
819-	return null
820-
821-
822:func _deploy_canopy() -> void:
823-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
824-	if _canopy_deployed or not _canopy_instance:
825-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1380-	if current_time >= event["time"]:
1381-		match event["action"]:
1382-			"deploy":
1383:				_deploy_canopy()
1384-			"cutaway":
1385-				_do_cutaway()
1386-			"reserve":
--
1615-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1616-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1617-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1618:		_deploy_canopy()
1619-
1620-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1621-		var turn_input := 0.0
--
1635-
1636-		_turn_input = turn_input
1637-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1638:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1639-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1640-# canopy could never accumulate a turn. Guard was commented out; call removed.
1641-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1654-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1655-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1656-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1657:			_deploy_canopy()
1658-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1659-	# Turn left (Q)
1660-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1954-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1955-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1956-			#  by _0283.txt showing plane position update before stall)
1957:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1958-				if not ProjectSettings.has_setting("_headless_space_fired"):
1959-					ProjectSettings.set_setting("_headless_space_fired", true)
1960-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802011150 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802011712 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1578-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1579-		var _exit_pressed = (
1580-			Input.is_action_just_pressed("deploy")
1581:			or Input.is_key_pressed(KEY_J)
1582:			or _headless_auto_jump
1583-		)
1584:		_headless_auto_jump = false  # consume the flag
1585-		if _exit_pressed:
1586-			print("[DIAG] _poll_controls: exit aircraft triggered")
1587-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3525-	"togglehud":    KEY_H,
3526-	"flightcheck":  KEY_G,
3527-	"restart":      KEY_R,
3528:	"j":            KEY_J,
3529-	"pause":        KEY_ESCAPE,
3530-}
3531-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
819-	return null
820-
821-
822:func _deploy_canopy() -> void:
823-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
824-	if _canopy_deployed or not _canopy_instance:
825-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1380-	if current_time >= event["time"]:
1381-		match event["action"]:
1382-			"deploy":
1383:				_deploy_canopy()
1384-			"cutaway":
1385-				_do_cutaway()
1386-			"reserve":
--
1615-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1616-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1617-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1618:		_deploy_canopy()
1619-
1620-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1621-		var turn_input := 0.0
--
1635-
1636-		_turn_input = turn_input
1637-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1638:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1639-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1640-# canopy could never accumulate a turn. Guard was commented out; call removed.
1641-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1654-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1655-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1656-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1657:			_deploy_canopy()
1658-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1659-	# Turn left (Q)
1660-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1954-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1955-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1956-			#  by _0283.txt showing plane position update before stall)
1957:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1958-				if not ProjectSettings.has_setting("_headless_space_fired"):
1959-					ProjectSettings.set_setting("_headless_space_fired", true)
1960-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802011712 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802090153665563 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
541-	# Headless auto-start: simulate SPACE/deploy press
542-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
543-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
544:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
545-		# Input.action_release("deploy") removed — flag-based now
546-		print("[VERBATIM] Headless auto‑start triggered.")
547-	_check_arm_pose_safe()
--
1575-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1576-		var _exit_pressed = (
1577-			Input.is_action_just_pressed("deploy")
1578:			or Input.is_key_pressed(KEY_J)
1579:			or _headless_auto_jump
1580-		)
1581:		_headless_auto_jump = false  # consume the flag
1582-		if _exit_pressed:
1583-			print("[DIAG] _poll_controls: exit aircraft triggered")
1584-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3526-	"togglehud":    KEY_H,
3527-	"flightcheck":  KEY_G,
3528-	"restart":      KEY_R,
3529:	"j":            KEY_J,
3530-	"pause":        KEY_ESCAPE,
3531-}
3532-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
816-	return null
817-
818-
819:func _deploy_canopy() -> void:
820-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
821-	if _canopy_deployed or not _canopy_instance:
822-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1377-	if current_time >= event["time"]:
1378-		match event["action"]:
1379-			"deploy":
1380:				_deploy_canopy()
1381-			"cutaway":
1382-				_do_cutaway()
1383-			"reserve":
--
1612-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1613-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1614-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1615:		_deploy_canopy()
1616-
1617-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1618-		var turn_input := 0.0
--
1632-
1633-		_turn_input = turn_input
1634-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1635:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1636-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1637-# canopy could never accumulate a turn. Guard was commented out; call removed.
1638-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1651-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1652-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1653-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1654:			_deploy_canopy()
1655-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1656-	# Turn left (Q)
1657-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
540-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
541-	# Headless auto-start: simulate SPACE/deploy press
542-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
543:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
544-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
545-		# Input.action_release("deploy") removed — flag-based now
546-		print("[VERBATIM] Headless auto‑start triggered.")
--
1951-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1952-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1953-			#  by _0283.txt showing plane position update before stall)
1954:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1955-				if not ProjectSettings.has_setting("_headless_space_fired"):
1956-					ProjectSettings.set_setting("_headless_space_fired", true)
1957-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802090153665563 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802110730 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1578-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1579-		var _exit_pressed = (
1580-			Input.is_action_just_pressed("deploy")
1581:			or Input.is_key_pressed(KEY_J)
1582:			or _headless_auto_jump
1583-		)
1584:		_headless_auto_jump = false  # consume the flag
1585-		if _exit_pressed:
1586-			print("[DIAG] _poll_controls: exit aircraft triggered")
1587-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3529-	"togglehud":    KEY_H,
3530-	"flightcheck":  KEY_G,
3531-	"restart":      KEY_R,
3532:	"j":            KEY_J,
3533-	"pause":        KEY_ESCAPE,
3534-}
3535-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
819-	return null
820-
821-
822:func _deploy_canopy() -> void:
823-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
824-	if _canopy_deployed or not _canopy_instance:
825-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1380-	if current_time >= event["time"]:
1381-		match event["action"]:
1382-			"deploy":
1383:				_deploy_canopy()
1384-			"cutaway":
1385-				_do_cutaway()
1386-			"reserve":
--
1615-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1616-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1617-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1618:		_deploy_canopy()
1619-
1620-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1621-		var turn_input := 0.0
--
1635-
1636-		_turn_input = turn_input
1637-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1638:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1639-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1640-# canopy could never accumulate a turn. Guard was commented out; call removed.
1641-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1654-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1655-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1656-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1657:			_deploy_canopy()
1658-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1659-	# Turn left (Q)
1660-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1954-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1955-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1956-			#  by _0283.txt showing plane position update before stall)
1957:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1958-				if not ProjectSettings.has_setting("_headless_space_fired"):
1959-					ProjectSettings.set_setting("_headless_space_fired", true)
1960-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802110730 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802112204 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1578-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1579-		var _exit_pressed = (
1580-			Input.is_action_just_pressed("deploy")
1581:			or Input.is_key_pressed(KEY_J)
1582:			or _headless_auto_jump
1583-		)
1584:		_headless_auto_jump = false  # consume the flag
1585-		if _exit_pressed:
1586-			print("[DIAG] _poll_controls: exit aircraft triggered")
1587-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3529-	"togglehud":    KEY_H,
3530-	"flightcheck":  KEY_G,
3531-	"restart":      KEY_R,
3532:	"j":            KEY_J,
3533-	"pause":        KEY_ESCAPE,
3534-}
3535-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
819-	return null
820-
821-
822:func _deploy_canopy() -> void:
823-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
824-	if _canopy_deployed or not _canopy_instance:
825-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1380-	if current_time >= event["time"]:
1381-		match event["action"]:
1382-			"deploy":
1383:				_deploy_canopy()
1384-			"cutaway":
1385-				_do_cutaway()
1386-			"reserve":
--
1615-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1616-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1617-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1618:		_deploy_canopy()
1619-
1620-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1621-		var turn_input := 0.0
--
1635-
1636-		_turn_input = turn_input
1637-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1638:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1639-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1640-# canopy could never accumulate a turn. Guard was commented out; call removed.
1641-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1654-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1655-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1656-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1657:			_deploy_canopy()
1658-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1659-	# Turn left (Q)
1660-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1954-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1955-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1956-			#  by _0283.txt showing plane position update before stall)
1957:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1958-				if not ProjectSettings.has_setting("_headless_space_fired"):
1959-					ProjectSettings.set_setting("_headless_space_fired", true)
1960-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802112204 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802112524 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1578-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1579-		var _exit_pressed = (
1580-			Input.is_action_just_pressed("deploy")
1581:			or Input.is_key_pressed(KEY_J)
1582:			or _headless_auto_jump
1583-		)
1584:		_headless_auto_jump = false  # consume the flag
1585-		if _exit_pressed:
1586-			print("[DIAG] _poll_controls: exit aircraft triggered")
1587-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3529-	"togglehud":    KEY_H,
3530-	"flightcheck":  KEY_G,
3531-	"restart":      KEY_R,
3532:	"j":            KEY_J,
3533-	"pause":        KEY_ESCAPE,
3534-}
3535-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
819-	return null
820-
821-
822:func _deploy_canopy() -> void:
823-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
824-	if _canopy_deployed or not _canopy_instance:
825-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1380-	if current_time >= event["time"]:
1381-		match event["action"]:
1382-			"deploy":
1383:				_deploy_canopy()
1384-			"cutaway":
1385-				_do_cutaway()
1386-			"reserve":
--
1615-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1616-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1617-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1618:		_deploy_canopy()
1619-
1620-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1621-		var turn_input := 0.0
--
1635-
1636-		_turn_input = turn_input
1637-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1638:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1639-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1640-# canopy could never accumulate a turn. Guard was commented out; call removed.
1641-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1654-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1655-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1656-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1657:			_deploy_canopy()
1658-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1659-	# Turn left (Q)
1660-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1954-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1955-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1956-			#  by _0283.txt showing plane position update before stall)
1957:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1958-				if not ProjectSettings.has_setting("_headless_space_fired"):
1959-					ProjectSettings.set_setting("_headless_space_fired", true)
1960-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802112524 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802123500 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1580-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1581-		var _exit_pressed = (
1582-			Input.is_action_just_pressed("deploy")
1583:			or Input.is_key_pressed(KEY_J)
1584:			or _headless_auto_jump
1585-		)
1586:		_headless_auto_jump = false  # consume the flag
1587-		if _exit_pressed:
1588-			print("[DIAG] _poll_controls: exit aircraft triggered")
1589-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3531-	"togglehud":    KEY_H,
3532-	"flightcheck":  KEY_G,
3533-	"restart":      KEY_R,
3534:	"j":            KEY_J,
3535-	"pause":        KEY_ESCAPE,
3536-}
3537-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1382-	if current_time >= event["time"]:
1383-		match event["action"]:
1384-			"deploy":
1385:				_deploy_canopy()
1386-			"cutaway":
1387-				_do_cutaway()
1388-			"reserve":
--
1617-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1618-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1619-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1620:		_deploy_canopy()
1621-
1622-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1623-		var turn_input := 0.0
--
1637-
1638-		_turn_input = turn_input
1639-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1640:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1641-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1642-# canopy could never accumulate a turn. Guard was commented out; call removed.
1643-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1656-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1657-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1658-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1659:			_deploy_canopy()
1660-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1661-	# Turn left (Q)
1662-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1956-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1957-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1958-			#  by _0283.txt showing plane position update before stall)
1959:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1960-				if not ProjectSettings.has_setting("_headless_space_fired"):
1961-					ProjectSettings.set_setting("_headless_space_fired", true)
1962-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802123500 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802125429 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
550-	_check_arm_pose_safe()
--
1578-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1579-		var _exit_pressed = (
1580-			Input.is_action_just_pressed("deploy")
1581:			or Input.is_key_pressed(KEY_J)
1582:			or _headless_auto_jump
1583-		)
1584:		_headless_auto_jump = false  # consume the flag
1585-		if _exit_pressed:
1586-			print("[DIAG] _poll_controls: exit aircraft triggered")
1587-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3529-	"togglehud":    KEY_H,
3530-	"flightcheck":  KEY_G,
3531-	"restart":      KEY_R,
3532:	"j":            KEY_J,
3533-	"pause":        KEY_ESCAPE,
3534-}
3535-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
819-	return null
820-
821-
822:func _deploy_canopy() -> void:
823-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
824-	if _canopy_deployed or not _canopy_instance:
825-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1380-	if current_time >= event["time"]:
1381-		match event["action"]:
1382-			"deploy":
1383:				_deploy_canopy()
1384-			"cutaway":
1385-				_do_cutaway()
1386-			"reserve":
--
1615-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1616-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1617-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1618:		_deploy_canopy()
1619-
1620-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1621-		var turn_input := 0.0
--
1635-
1636-		_turn_input = turn_input
1637-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1638:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1639-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1640-# canopy could never accumulate a turn. Guard was commented out; call removed.
1641-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1654-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1655-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1656-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1657:			_deploy_canopy()
1658-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1659-	# Turn left (Q)
1660-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
543-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
544-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
545-	# Headless auto‑start: simulate SPACE press
546:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
547-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
548-		# Input.action_release("deploy") removed — flag-based now
549-		print("[VERBATIM] Headless auto‑start triggered.")
--
1954-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1955-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1956-			#  by _0283.txt showing plane position update before stall)
1957:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1958-				if not ProjectSettings.has_setting("_headless_space_fired"):
1959-					ProjectSettings.set_setting("_headless_space_fired", true)
1960-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802125429 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802130139 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
540-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
541-	# Headless auto‑start: simulate SPACE press
542-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
543:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
544-		# Input.action_release("deploy") removed — flag-based now
545-		print("[VERBATIM] Headless auto‑start triggered.")
546-	_check_arm_pose_safe()
--
1574-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1575-		var _exit_pressed = (
1576-			Input.is_action_just_pressed("deploy")
1577:			or Input.is_key_pressed(KEY_J)
1578:			or _headless_auto_jump
1579-		)
1580:		_headless_auto_jump = false  # consume the flag
1581-		if _exit_pressed:
1582-			print("[DIAG] _poll_controls: exit aircraft triggered")
1583-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3525-	"togglehud":    KEY_H,
3526-	"flightcheck":  KEY_G,
3527-	"restart":      KEY_R,
3528:	"j":            KEY_J,
3529-	"pause":        KEY_ESCAPE,
3530-}
3531-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
815-	return null
816-
817-
818:func _deploy_canopy() -> void:
819-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
820-	if _canopy_deployed or not _canopy_instance:
821-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1376-	if current_time >= event["time"]:
1377-		match event["action"]:
1378-			"deploy":
1379:				_deploy_canopy()
1380-			"cutaway":
1381-				_do_cutaway()
1382-			"reserve":
--
1611-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1612-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1613-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1614:		_deploy_canopy()
1615-
1616-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1617-		var turn_input := 0.0
--
1631-
1632-		_turn_input = turn_input
1633-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1634:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1635-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1636-# canopy could never accumulate a turn. Guard was commented out; call removed.
1637-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1650-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1651-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1652-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1653:			_deploy_canopy()
1654-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1655-	# Turn left (Q)
1656-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
539-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
540-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
541-	# Headless auto‑start: simulate SPACE press
542:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
543-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
544-		# Input.action_release("deploy") removed — flag-based now
545-		print("[VERBATIM] Headless auto‑start triggered.")
--
1950-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1951-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1952-			#  by _0283.txt showing plane position update before stall)
1953:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1954-				if not ProjectSettings.has_setting("_headless_space_fired"):
1955-					ProjectSettings.set_setting("_headless_space_fired", true)
1956-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802130139 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802152226 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
546-	# Headless auto-start: simulate SPACE/deploy press
547-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
548-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1580-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1581-		var _exit_pressed = (
1582-			Input.is_action_just_pressed("deploy")
1583:			or Input.is_key_pressed(KEY_J)
1584:			or _headless_auto_jump
1585-		)
1586:		_headless_auto_jump = false  # consume the flag
1587-		if _exit_pressed:
1588-			print("[DIAG] _poll_controls: exit aircraft triggered")
1589-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3531-	"togglehud":    KEY_H,
3532-	"flightcheck":  KEY_G,
3533-	"restart":      KEY_R,
3534:	"j":            KEY_J,
3535-	"pause":        KEY_ESCAPE,
3536-}
3537-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1382-	if current_time >= event["time"]:
1383-		match event["action"]:
1384-			"deploy":
1385:				_deploy_canopy()
1386-			"cutaway":
1387-				_do_cutaway()
1388-			"reserve":
--
1617-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1618-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1619-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1620:		_deploy_canopy()
1621-
1622-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1623-		var turn_input := 0.0
--
1637-
1638-		_turn_input = turn_input
1639-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1640:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1641-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1642-# canopy could never accumulate a turn. Guard was commented out; call removed.
1643-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1656-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1657-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1658-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1659:			_deploy_canopy()
1660-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1661-	# Turn left (Q)
1662-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
539-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
540-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
541-	# Headless auto‑start: simulate SPACE press
542:	if OS.get_environment("GODOT_HEADLESS") == "1":
543-		Input.action_press("ui_accept")
544-		Input.action_release("ui_accept")
545-		print("[VERBATIM] Headless auto‑start triggered.")
546-	# Headless auto-start: simulate SPACE/deploy press
547-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
548:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1956-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1957-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1958-			#  by _0283.txt showing plane position update before stall)
1959:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1960-				if not ProjectSettings.has_setting("_headless_space_fired"):
1961-					ProjectSettings.set_setting("_headless_space_fired", true)
1962-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802152226 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802160516 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
547-	# Headless auto-start: simulate SPACE/deploy press
548-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
549-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
550:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
551-		# Input.action_release("deploy") removed — flag-based now
552-		print("[VERBATIM] Headless auto‑start triggered.")
553-	_check_arm_pose_safe()
--
1581-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1582-		var _exit_pressed = (
1583-			Input.is_action_just_pressed("deploy")
1584:			or Input.is_key_pressed(KEY_J)
1585:			or _headless_auto_jump
1586-		)
1587:		_headless_auto_jump = false  # consume the flag
1588-		if _exit_pressed:
1589-			print("[DIAG] _poll_controls: exit aircraft triggered")
1590-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3542-	"togglehud":    KEY_H,
3543-	"flightcheck":  KEY_G,
3544-	"restart":      KEY_R,
3545:	"j":            KEY_J,
3546-	"pause":        KEY_ESCAPE,
3547-}
3548-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
822-	return null
823-
824-
825:func _deploy_canopy() -> void:
826-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
827-	if _canopy_deployed or not _canopy_instance:
828-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1383-	if current_time >= event["time"]:
1384-		match event["action"]:
1385-			"deploy":
1386:				_deploy_canopy()
1387-			"cutaway":
1388-				_do_cutaway()
1389-			"reserve":
--
1617-
1618-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1619-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1620:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1621-	# so it fires on the first FREEFALL _poll_controls() call.
1622-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-	# (general knowledge — not retrieved this session)
1624-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1625-		_headless_auto_deploy = false
1626-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1627:		_deploy_canopy()
1628-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1629-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1630:		_deploy_canopy()
1631-
1632-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1633-		var turn_input := 0.0
--
1647-
1648-		_turn_input = turn_input
1649-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1650:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1651-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1652-# canopy could never accumulate a turn. Guard was commented out; call removed.
1653-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1666-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1667-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1668-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1669:			_deploy_canopy()
1670-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1671-	# Turn left (Q)
1672-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
540-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
541-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
542-	# Headless auto‑start: simulate SPACE press
543:	if OS.get_environment("GODOT_HEADLESS") == "1":
544-		Input.action_press("ui_accept")
545-		Input.action_release("ui_accept")
546-		print("[VERBATIM] Headless auto‑start triggered.")
547-	# Headless auto-start: simulate SPACE/deploy press
548-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
549:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
550-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
551-		# Input.action_release("deploy") removed — flag-based now
552-		print("[VERBATIM] Headless auto‑start triggered.")
--
1966-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1967-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1968-			#  by _0283.txt showing plane position update before stall)
1969:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1970-				if not ProjectSettings.has_setting("_headless_space_fired"):
1971-					ProjectSettings.set_setting("_headless_space_fired", true)
1972-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802160516 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260802161550 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
547-	# Headless auto-start: simulate SPACE/deploy press
548-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
549-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
550:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
551-		# Input.action_release("deploy") removed — flag-based now
552-		print("[VERBATIM] Headless auto‑start triggered.")
553-	_check_arm_pose_safe()
--
1593-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1594-		var _exit_pressed = (
1595-			Input.is_action_just_pressed("deploy")
1596:			or Input.is_key_pressed(KEY_J)
1597:			or _headless_auto_jump
1598-		)
1599:		_headless_auto_jump = false  # consume the flag
1600-		if _exit_pressed:
1601-			print("[DIAG] _poll_controls: exit aircraft triggered")
1602-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3646-	"togglehud":    KEY_H,
3647-	"flightcheck":  KEY_G,
3648-	"restart":      KEY_R,
3649:	"j":            KEY_J,
3650-	"pause":        KEY_ESCAPE,
3651-}
3652-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
834-	return null
835-
836-
837:func _deploy_canopy() -> void:
838-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
839-	if _canopy_deployed or not _canopy_instance:
840-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1395-	if current_time >= event["time"]:
1396-		match event["action"]:
1397-			"deploy":
1398:				_deploy_canopy()
1399-			"cutaway":
1400-				_do_cutaway()
1401-			"reserve":
--
1629-
1630-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1631-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1632:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1633-	# so it fires on the first FREEFALL _poll_controls() call.
1634-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1635-	# (general knowledge — not retrieved this session)
1636-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1637-		_headless_auto_deploy = false
1638-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1639:		_deploy_canopy()
1640-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1641-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1642:		_deploy_canopy()
1643-
1644-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1645-		var turn_input := 0.0
--
1659-
1660-		_turn_input = turn_input
1661-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1662:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1663-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1664-# canopy could never accumulate a turn. Guard was commented out; call removed.
1665-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1678-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1679-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1680-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1681:			_deploy_canopy()
1682-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1683-	# Turn left (Q)
1684-	if Input.is_key_pressed(KEY_Q):
--
2091-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2092-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2093-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2094:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2095-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2096-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2097-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
540-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
541-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
542-	# Headless auto‑start: simulate SPACE press
543:	if OS.get_environment("GODOT_HEADLESS") == "1":
544-		Input.action_press("ui_accept")
545-		Input.action_release("ui_accept")
546-		print("[VERBATIM] Headless auto‑start triggered.")
547-	# Headless auto-start: simulate SPACE/deploy press
548-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
549:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
550-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
551-		# Input.action_release("deploy") removed — flag-based now
552-		print("[VERBATIM] Headless auto‑start triggered.")
--
1990-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1991-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1992-			#  by _0283.txt showing plane position update before stall)
1993:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1994-				if not ProjectSettings.has_setting("_headless_space_fired"):
1995-					ProjectSettings.set_setting("_headless_space_fired", true)
1996-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260802161550 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260803232319 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622-			Input.is_action_just_pressed("deploy")
1623:			or Input.is_key_pressed(KEY_J)
1624:			or _headless_auto_jump
1625-		)
1626:		_headless_auto_jump = false  # consume the flag
1627-		if _exit_pressed:
1628-			print("[DIAG] _poll_controls: exit aircraft triggered")
1629-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3701-	"togglehud":    KEY_H,
3702-	"flightcheck":  KEY_G,
3703-	"restart":      KEY_R,
3704:	"j":            KEY_J,
3705-	"pause":        KEY_ESCAPE,
3706-}
3707-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1656-
1657-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1658-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1659:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1660-	# so it fires on the first FREEFALL _poll_controls() call.
1661-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1662-	# (general knowledge — not retrieved this session)
1663-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1664-		_headless_auto_deploy = false
1665-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1666:		_deploy_canopy()
1667-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1668-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1669:		_deploy_canopy()
1670-
1671-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1672-		var turn_input := 0.0
--
1686-
1687-		_turn_input = turn_input
1688-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1689:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1690-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1691-# canopy could never accumulate a turn. Guard was commented out; call removed.
1692-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1705-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1706-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1707-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1708:			_deploy_canopy()
1709-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1710-	# Turn left (Q)
1711-	if Input.is_key_pressed(KEY_Q):
--
2126-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2127-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2128-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2129:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2130-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2131-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2132-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
2017-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2018-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2019-			#  by _0283.txt showing plane position update before stall)
2020:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2021-				if not ProjectSettings.has_setting("_headless_space_fired"):
2022-					ProjectSettings.set_setting("_headless_space_fired", true)
2023-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260803232319 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260803232424 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622-			Input.is_action_just_pressed("deploy")
1623:			or Input.is_key_pressed(KEY_J)
1624:			or _headless_auto_jump
1625-		)
1626:		_headless_auto_jump = false  # consume the flag
1627-		if _exit_pressed:
1628-			print("[DIAG] _poll_controls: exit aircraft triggered")
1629-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3701-	"togglehud":    KEY_H,
3702-	"flightcheck":  KEY_G,
3703-	"restart":      KEY_R,
3704:	"j":            KEY_J,
3705-	"pause":        KEY_ESCAPE,
3706-}
3707-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1656-
1657-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1658-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1659:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1660-	# so it fires on the first FREEFALL _poll_controls() call.
1661-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1662-	# (general knowledge — not retrieved this session)
1663-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1664-		_headless_auto_deploy = false
1665-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1666:		_deploy_canopy()
1667-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1668-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1669:		_deploy_canopy()
1670-
1671-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1672-		var turn_input := 0.0
--
1686-
1687-		_turn_input = turn_input
1688-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1689:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1690-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1691-# canopy could never accumulate a turn. Guard was commented out; call removed.
1692-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1705-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1706-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1707-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1708:			_deploy_canopy()
1709-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1710-	# Turn left (Q)
1711-	if Input.is_key_pressed(KEY_Q):
--
2126-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2127-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2128-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2129:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2130-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2131-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2132-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
2017-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2018-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2019-			#  by _0283.txt showing plane position update before stall)
2020:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2021-				if not ProjectSettings.has_setting("_headless_space_fired"):
2022-					ProjectSettings.set_setting("_headless_space_fired", true)
2023-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260803232424 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804002229 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
126-var _canopy_instance: Node3D
127-var _canopy_material: StandardMaterial3D
128-var _canopy_deployed: bool = false
129:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
130-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
131-var _deployment_timer
132-var _screenshot_save_timer: float = 0.0
--
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
565-	_check_arm_pose_safe()
--
1622-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-		var _exit_pressed = (
1624-			Input.is_action_just_pressed("deploy")
1625:			or Input.is_key_pressed(KEY_J)
1626:			or _headless_auto_jump
1627-		)
1628:		_headless_auto_jump = false  # consume the flag
1629-		if _exit_pressed:
1630-			print("[DIAG] _poll_controls: exit aircraft triggered")
1631-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3713-	"togglehud":    KEY_H,
3714-	"flightcheck":  KEY_G,
3715-	"restart":      KEY_R,
3716:	"j":            KEY_J,
3717-	"pause":        KEY_ESCAPE,
3718-}
3719-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
863-	return null
864-
865-
866:func _deploy_canopy() -> void:
867-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
868-	if _canopy_deployed or not _canopy_instance:
869-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1424-	if current_time >= event["time"]:
1425-		match event["action"]:
1426-			"deploy":
1427:				_deploy_canopy()
1428-			"cutaway":
1429-				_do_cutaway()
1430-			"reserve":
--
1658-
1659-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1660-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1661:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1662-	# so it fires on the first FREEFALL _poll_controls() call.
1663-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1664-	# (general knowledge — not retrieved this session)
1665-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1666-		_headless_auto_deploy = false
1667-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1668:		_deploy_canopy()
1669-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1670-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1671:		_deploy_canopy()
1672-
1673-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1674-		var turn_input := 0.0
--
1688-
1689-		_turn_input = turn_input
1690-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1691:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1692-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1693-# canopy could never accumulate a turn. Guard was commented out; call removed.
1694-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1707-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1708-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1709-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1710:			_deploy_canopy()
1711-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1712-	# Turn left (Q)
1713-	if Input.is_key_pressed(KEY_Q):
--
2138-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2139-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2140-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2141:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2142-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2143-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2144-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
552-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
553-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
554-	# Headless auto‑start: simulate SPACE press
555:	if OS.get_environment("GODOT_HEADLESS") == "1":
556-		Input.action_press("ui_accept")
557-		Input.action_release("ui_accept")
558-		print("[VERBATIM] Headless auto‑start triggered.")
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
--
583-	# autostall's 30 s stall detector cannot trip on it.
584-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
585-	# (general knowledge - not retrieved this session)
586:	if OS.get_environment("GODOT_HEADLESS") == "1" \
587-			or "--headless" in OS.get_cmdline_args():
588-		var _pause_timer := Timer.new()
589-		_pause_timer.wait_time = 8.0
--
2029-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2030-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2031-			#  by _0283.txt showing plane position update before stall)
2032:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2033-				if not ProjectSettings.has_setting("_headless_space_fired"):
2034-					ProjectSettings.set_setting("_headless_space_fired", true)
2035-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804002229 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804104916 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
126-var _canopy_instance: Node3D
127-var _canopy_material: StandardMaterial3D
128-var _canopy_deployed: bool = false
129:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
130-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
131-var _deployment_timer
132-var _screenshot_save_timer: float = 0.0
--
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
565-	_check_arm_pose_safe()
--
1622-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-		var _exit_pressed = (
1624-			Input.is_action_just_pressed("deploy")
1625:			or Input.is_key_pressed(KEY_J)
1626:			or _headless_auto_jump
1627-		)
1628:		_headless_auto_jump = false  # consume the flag
1629-		if _exit_pressed:
1630-			print("[DIAG] _poll_controls: exit aircraft triggered")
1631-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3713-	"togglehud":    KEY_H,
3714-	"flightcheck":  KEY_G,
3715-	"restart":      KEY_R,
3716:	"j":            KEY_J,
3717-	"pause":        KEY_ESCAPE,
3718-}
3719-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
863-	return null
864-
865-
866:func _deploy_canopy() -> void:
867-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
868-	if _canopy_deployed or not _canopy_instance:
869-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1424-	if current_time >= event["time"]:
1425-		match event["action"]:
1426-			"deploy":
1427:				_deploy_canopy()
1428-			"cutaway":
1429-				_do_cutaway()
1430-			"reserve":
--
1658-
1659-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1660-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1661:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1662-	# so it fires on the first FREEFALL _poll_controls() call.
1663-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1664-	# (general knowledge — not retrieved this session)
1665-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1666-		_headless_auto_deploy = false
1667-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1668:		_deploy_canopy()
1669-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1670-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1671:		_deploy_canopy()
1672-
1673-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1674-		var turn_input := 0.0
--
1688-
1689-		_turn_input = turn_input
1690-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1691:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1692-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1693-# canopy could never accumulate a turn. Guard was commented out; call removed.
1694-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1707-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1708-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1709-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1710:			_deploy_canopy()
1711-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1712-	# Turn left (Q)
1713-	if Input.is_key_pressed(KEY_Q):
--
2138-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2139-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2140-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2141:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2142-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2143-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2144-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
552-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
553-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
554-	# Headless auto‑start: simulate SPACE press
555:	if OS.get_environment("GODOT_HEADLESS") == "1":
556-		Input.action_press("ui_accept")
557-		Input.action_release("ui_accept")
558-		print("[VERBATIM] Headless auto‑start triggered.")
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
--
583-	# autostall's 30 s stall detector cannot trip on it.
584-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
585-	# (general knowledge - not retrieved this session)
586:	if OS.get_environment("GODOT_HEADLESS") == "1" \
587-			or "--headless" in OS.get_cmdline_args():
588-		var _pause_timer := Timer.new()
589-		_pause_timer.wait_time = 8.0
--
2029-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2030-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2031-			#  by _0283.txt showing plane position update before stall)
2032:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2033-				if not ProjectSettings.has_setting("_headless_space_fired"):
2034-					ProjectSettings.set_setting("_headless_space_fired", true)
2035-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804104916 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804173410 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
126-var _canopy_instance: Node3D
127-var _canopy_material: StandardMaterial3D
128-var _canopy_deployed: bool = false
129:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
130-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
131-var _deployment_timer
132-var _screenshot_save_timer: float = 0.0
--
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
565-	_check_arm_pose_safe()
--
1622-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-		var _exit_pressed = (
1624-			Input.is_action_just_pressed("deploy")
1625:			or Input.is_key_pressed(KEY_J)
1626:			or _headless_auto_jump
1627-		)
1628:		_headless_auto_jump = false  # consume the flag
1629-		if _exit_pressed:
1630-			print("[DIAG] _poll_controls: exit aircraft triggered")
1631-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3713-	"togglehud":    KEY_H,
3714-	"flightcheck":  KEY_G,
3715-	"restart":      KEY_R,
3716:	"j":            KEY_J,
3717-	"pause":        KEY_ESCAPE,
3718-}
3719-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
863-	return null
864-
865-
866:func _deploy_canopy() -> void:
867-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
868-	if _canopy_deployed or not _canopy_instance:
869-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1424-	if current_time >= event["time"]:
1425-		match event["action"]:
1426-			"deploy":
1427:				_deploy_canopy()
1428-			"cutaway":
1429-				_do_cutaway()
1430-			"reserve":
--
1658-
1659-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1660-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1661:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1662-	# so it fires on the first FREEFALL _poll_controls() call.
1663-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1664-	# (general knowledge — not retrieved this session)
1665-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1666-		_headless_auto_deploy = false
1667-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1668:		_deploy_canopy()
1669-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1670-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1671:		_deploy_canopy()
1672-
1673-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1674-		var turn_input := 0.0
--
1688-
1689-		_turn_input = turn_input
1690-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1691:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1692-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1693-# canopy could never accumulate a turn. Guard was commented out; call removed.
1694-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1707-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1708-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1709-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1710:			_deploy_canopy()
1711-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1712-	# Turn left (Q)
1713-	if Input.is_key_pressed(KEY_Q):
--
2138-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2139-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2140-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2141:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2142-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2143-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2144-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
552-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
553-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
554-	# Headless auto‑start: simulate SPACE press
555:	if OS.get_environment("GODOT_HEADLESS") == "1":
556-		Input.action_press("ui_accept")
557-		Input.action_release("ui_accept")
558-		print("[VERBATIM] Headless auto‑start triggered.")
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
--
583-	# autostall's 30 s stall detector cannot trip on it.
584-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
585-	# (general knowledge - not retrieved this session)
586:	if OS.get_environment("GODOT_HEADLESS") == "1" \
587-			or "--headless" in OS.get_cmdline_args():
588-		var _pause_timer := Timer.new()
589-		_pause_timer.wait_time = 8.0
--
2029-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2030-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2031-			#  by _0283.txt showing plane position update before stall)
2032:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2033-				if not ProjectSettings.has_setting("_headless_space_fired"):
2034-					ProjectSettings.set_setting("_headless_space_fired", true)
2035-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804173410 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804184153 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
126-var _canopy_instance: Node3D
127-var _canopy_material: StandardMaterial3D
128-var _canopy_deployed: bool = false
129:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
130-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
131-var _deployment_timer
132-var _screenshot_save_timer: float = 0.0
--
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
565-	_check_arm_pose_safe()
--
1622-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-		var _exit_pressed = (
1624-			Input.is_action_just_pressed("deploy")
1625:			or Input.is_key_pressed(KEY_J)
1626:			or _headless_auto_jump
1627-		)
1628:		_headless_auto_jump = false  # consume the flag
1629-		if _exit_pressed:
1630-			print("[DIAG] _poll_controls: exit aircraft triggered")
1631-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3731-	"togglehud":    KEY_H,
3732-	"flightcheck":  KEY_G,
3733-	"restart":      KEY_R,
3734:	"j":            KEY_J,
3735-	"pause":        KEY_ESCAPE,
3736-}
3737-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
863-	return null
864-
865-
866:func _deploy_canopy() -> void:
867-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
868-	if _canopy_deployed or not _canopy_instance:
869-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1424-	if current_time >= event["time"]:
1425-		match event["action"]:
1426-			"deploy":
1427:				_deploy_canopy()
1428-			"cutaway":
1429-				_do_cutaway()
1430-			"reserve":
--
1658-
1659-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1660-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1661:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1662-	# so it fires on the first FREEFALL _poll_controls() call.
1663-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1664-	# (general knowledge — not retrieved this session)
1665-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1666-		_headless_auto_deploy = false
1667-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1668:		_deploy_canopy()
1669-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1670-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1671:		_deploy_canopy()
1672-
1673-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1674-		var turn_input := 0.0
--
1688-
1689-		_turn_input = turn_input
1690-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1691:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1692-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1693-# canopy could never accumulate a turn. Guard was commented out; call removed.
1694-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1707-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1708-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1709-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1710:			_deploy_canopy()
1711-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1712-	# Turn left (Q)
1713-	if Input.is_key_pressed(KEY_Q):
--
2156-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2157-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2158-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2159:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2160-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2161-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2162-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
552-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
553-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
554-	# Headless auto‑start: simulate SPACE press
555:	if OS.get_environment("GODOT_HEADLESS") == "1":
556-		Input.action_press("ui_accept")
557-		Input.action_release("ui_accept")
558-		print("[VERBATIM] Headless auto‑start triggered.")
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
--
583-	# autostall's 30 s stall detector cannot trip on it.
584-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
585-	# (general knowledge - not retrieved this session)
586:	if OS.get_environment("GODOT_HEADLESS") == "1" \
587-			or "--headless" in OS.get_cmdline_args():
588-		var _pause_timer := Timer.new()
589-		_pause_timer.wait_time = 8.0
--
2029-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2030-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2031-			#  by _0283.txt showing plane position update before stall)
2032:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2033-				if not ProjectSettings.has_setting("_headless_space_fired"):
2034-					ProjectSettings.set_setting("_headless_space_fired", true)
2035-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804184153 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804193845 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
126-var _canopy_instance: Node3D
127-var _canopy_material: StandardMaterial3D
128-var _canopy_deployed: bool = false
129:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
130-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
131-var _deployment_timer
132-var _screenshot_save_timer: float = 0.0
--
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
565-	_check_arm_pose_safe()
--
1622-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-		var _exit_pressed = (
1624-			Input.is_action_just_pressed("deploy")
1625:			or Input.is_key_pressed(KEY_J)
1626:			or _headless_auto_jump
1627-		)
1628:		_headless_auto_jump = false  # consume the flag
1629-		if _exit_pressed:
1630-			print("[DIAG] _poll_controls: exit aircraft triggered")
1631-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3747-	"togglehud":    KEY_H,
3748-	"flightcheck":  KEY_G,
3749-	"restart":      KEY_R,
3750:	"j":            KEY_J,
3751-	"pause":        KEY_ESCAPE,
3752-}
3753-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
863-	return null
864-
865-
866:func _deploy_canopy() -> void:
867-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
868-	if _canopy_deployed or not _canopy_instance:
869-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1424-	if current_time >= event["time"]:
1425-		match event["action"]:
1426-			"deploy":
1427:				_deploy_canopy()
1428-			"cutaway":
1429-				_do_cutaway()
1430-			"reserve":
--
1658-
1659-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1660-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1661:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1662-	# so it fires on the first FREEFALL _poll_controls() call.
1663-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1664-	# (general knowledge — not retrieved this session)
1665-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1666-		_headless_auto_deploy = false
1667-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1668:		_deploy_canopy()
1669-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1670-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1671:		_deploy_canopy()
1672-
1673-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1674-		var turn_input := 0.0
--
1688-
1689-		_turn_input = turn_input
1690-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1691:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1692-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1693-# canopy could never accumulate a turn. Guard was commented out; call removed.
1694-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1707-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1708-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1709-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1710:			_deploy_canopy()
1711-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1712-	# Turn left (Q)
1713-	if Input.is_key_pressed(KEY_Q):
--
2156-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2157-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2158-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2159:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2160-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2161-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2162-		# They are now in _update_hud_readouts(), called unconditionally from
--
2170-			_screenshot_save_timer = 5.0
2171-
2172-	# OPENING_ANIM -> DIAGNOSIS transition (this session):
2173:	# _deployment_timer is set to 2.0 at line 874 in _deploy_canopy()
2174-	# but was NEVER decremented anywhere in the file (proven by
2175-	# notes/diag_opening_anim_20260804182915.txt PART H: 0 hits for
2176-	# '_deployment_timer -= '). State stuck at OPENING_ANIM = 4896

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
552-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
553-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
554-	# Headless auto‑start: simulate SPACE press
555:	if OS.get_environment("GODOT_HEADLESS") == "1":
556-		Input.action_press("ui_accept")
557-		Input.action_release("ui_accept")
558-		print("[VERBATIM] Headless auto‑start triggered.")
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
--
583-	# autostall's 30 s stall detector cannot trip on it.
584-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
585-	# (general knowledge - not retrieved this session)
586:	if OS.get_environment("GODOT_HEADLESS") == "1" \
587-			or "--headless" in OS.get_cmdline_args():
588-		var _pause_timer := Timer.new()
589-		_pause_timer.wait_time = 8.0
--
2029-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2030-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2031-			#  by _0283.txt showing plane position update before stall)
2032:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2033-				if not ProjectSettings.has_setting("_headless_space_fired"):
2034-					ProjectSettings.set_setting("_headless_space_fired", true)
2035-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804193845 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804194643 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
126-var _canopy_instance: Node3D
127-var _canopy_material: StandardMaterial3D
128-var _canopy_deployed: bool = false
129:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
130-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
131-var _deployment_timer
132-var _screenshot_save_timer: float = 0.0
--
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
565-	_check_arm_pose_safe()
--
1622-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-		var _exit_pressed = (
1624-			Input.is_action_just_pressed("deploy")
1625:			or Input.is_key_pressed(KEY_J)
1626:			or _headless_auto_jump
1627-		)
1628:		_headless_auto_jump = false  # consume the flag
1629-		if _exit_pressed:
1630-			print("[DIAG] _poll_controls: exit aircraft triggered")
1631-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3754-	"togglehud":    KEY_H,
3755-	"flightcheck":  KEY_G,
3756-	"restart":      KEY_R,
3757:	"j":            KEY_J,
3758-	"pause":        KEY_ESCAPE,
3759-
3760-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
863-	return null
864-
865-
866:func _deploy_canopy() -> void:
867-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
868-	if _canopy_deployed or not _canopy_instance:
869-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1424-	if current_time >= event["time"]:
1425-		match event["action"]:
1426-			"deploy":
1427:				_deploy_canopy()
1428-			"cutaway":
1429-				_do_cutaway()
1430-			"reserve":
--
1658-
1659-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1660-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1661:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1662-	# so it fires on the first FREEFALL _poll_controls() call.
1663-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1664-	# (general knowledge — not retrieved this session)
1665-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1666-		_headless_auto_deploy = false
1667-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1668:		_deploy_canopy()
1669-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1670-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1671:		_deploy_canopy()
1672-
1673-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1674-		var turn_input := 0.0
--
1688-
1689-		_turn_input = turn_input
1690-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1691:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1692-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1693-# canopy could never accumulate a turn. Guard was commented out; call removed.
1694-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1707-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1708-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1709-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1710:			_deploy_canopy()
1711-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1712-	# Turn left (Q)
1713-	if Input.is_key_pressed(KEY_Q):
--
2156-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2157-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2158-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2159:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2160-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2161-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2162-		# They are now in _update_hud_readouts(), called unconditionally from
--
2170-			_screenshot_save_timer = 5.0
2171-
2172-	# OPENING_ANIM -> DIAGNOSIS transition (this session):
2173:	# _deployment_timer is set to 2.0 at line 874 in _deploy_canopy()
2174-	# but was NEVER decremented anywhere in the file (proven by
2175-	# notes/diag_opening_anim_20260804182915.txt PART H: 0 hits for
2176-	# '_deployment_timer -= '). State stuck at OPENING_ANIM = 4896

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
552-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
553-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
554-	# Headless auto‑start: simulate SPACE press
555:	if OS.get_environment("GODOT_HEADLESS") == "1":
556-		Input.action_press("ui_accept")
557-		Input.action_release("ui_accept")
558-		print("[VERBATIM] Headless auto‑start triggered.")
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
--
583-	# autostall's 30 s stall detector cannot trip on it.
584-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
585-	# (general knowledge - not retrieved this session)
586:	if OS.get_environment("GODOT_HEADLESS") == "1" \
587-			or "--headless" in OS.get_cmdline_args():
588-		var _pause_timer := Timer.new()
589-		_pause_timer.wait_time = 8.0
--
2029-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2030-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2031-			#  by _0283.txt showing plane position update before stall)
2032:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2033-				if not ProjectSettings.has_setting("_headless_space_fired"):
2034-					ProjectSettings.set_setting("_headless_space_fired", true)
2035-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804194643 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804194830 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
126-var _canopy_instance: Node3D
127-var _canopy_material: StandardMaterial3D
128-var _canopy_deployed: bool = false
129:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
130-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
131-var _deployment_timer
132-var _screenshot_save_timer: float = 0.0
--
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
565-	_check_arm_pose_safe()
--
1622-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-		var _exit_pressed = (
1624-			Input.is_action_just_pressed("deploy")
1625:			or Input.is_key_pressed(KEY_J)
1626:			or _headless_auto_jump
1627-		)
1628:		_headless_auto_jump = false  # consume the flag
1629-		if _exit_pressed:
1630-			print("[DIAG] _poll_controls: exit aircraft triggered")
1631-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3754-	"togglehud":    KEY_H,
3755-	"flightcheck":  KEY_G,
3756-	"restart":      KEY_R,
3757:	"j":            KEY_J,
3758-	"pause":        KEY_ESCAPE,
3759-
3760-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
863-	return null
864-
865-
866:func _deploy_canopy() -> void:
867-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
868-	if _canopy_deployed or not _canopy_instance:
869-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1424-	if current_time >= event["time"]:
1425-		match event["action"]:
1426-			"deploy":
1427:				_deploy_canopy()
1428-			"cutaway":
1429-				_do_cutaway()
1430-			"reserve":
--
1658-
1659-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1660-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1661:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1662-	# so it fires on the first FREEFALL _poll_controls() call.
1663-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1664-	# (general knowledge — not retrieved this session)
1665-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1666-		_headless_auto_deploy = false
1667-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1668:		_deploy_canopy()
1669-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1670-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1671:		_deploy_canopy()
1672-
1673-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1674-		var turn_input := 0.0
--
1688-
1689-		_turn_input = turn_input
1690-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1691:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1692-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1693-# canopy could never accumulate a turn. Guard was commented out; call removed.
1694-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1707-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1708-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1709-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1710:			_deploy_canopy()
1711-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1712-	# Turn left (Q)
1713-	if Input.is_key_pressed(KEY_Q):
--
2156-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2157-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2158-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2159:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2160-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2161-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2162-		# They are now in _update_hud_readouts(), called unconditionally from
--
2170-			_screenshot_save_timer = 5.0
2171-
2172-	# OPENING_ANIM -> DIAGNOSIS transition (this session):
2173:	# _deployment_timer is set to 2.0 at line 874 in _deploy_canopy()
2174-	# but was NEVER decremented anywhere in the file (proven by
2175-	# notes/diag_opening_anim_20260804182915.txt PART H: 0 hits for
2176-	# '_deployment_timer -= '). State stuck at OPENING_ANIM = 4896

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
552-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
553-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
554-	# Headless auto‑start: simulate SPACE press
555:	if OS.get_environment("GODOT_HEADLESS") == "1":
556-		Input.action_press("ui_accept")
557-		Input.action_release("ui_accept")
558-		print("[VERBATIM] Headless auto‑start triggered.")
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
--
583-	# autostall's 30 s stall detector cannot trip on it.
584-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
585-	# (general knowledge - not retrieved this session)
586:	if OS.get_environment("GODOT_HEADLESS") == "1" \
587-			or "--headless" in OS.get_cmdline_args():
588-		var _pause_timer := Timer.new()
589-		_pause_timer.wait_time = 8.0
--
2029-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2030-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2031-			#  by _0283.txt showing plane position update before stall)
2032:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2033-				if not ProjectSettings.has_setting("_headless_space_fired"):
2034-					ProjectSettings.set_setting("_headless_space_fired", true)
2035-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804194830 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804195226 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
126-var _canopy_instance: Node3D
127-var _canopy_material: StandardMaterial3D
128-var _canopy_deployed: bool = false
129:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
130-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
131-var _deployment_timer
132-var _screenshot_save_timer: float = 0.0
--
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
565-	_check_arm_pose_safe()
--
1622-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-		var _exit_pressed = (
1624-			Input.is_action_just_pressed("deploy")
1625:			or Input.is_key_pressed(KEY_J)
1626:			or _headless_auto_jump
1627-		)
1628:		_headless_auto_jump = false  # consume the flag
1629-		if _exit_pressed:
1630-			print("[DIAG] _poll_controls: exit aircraft triggered")
1631-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3754-	"togglehud":    KEY_H,
3755-	"flightcheck":  KEY_G,
3756-	"restart":      KEY_R,
3757:	"j":            KEY_J,
3758-	"pause":        KEY_ESCAPE,
3759-
3760-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
863-	return null
864-
865-
866:func _deploy_canopy() -> void:
867-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
868-	if _canopy_deployed or not _canopy_instance:
869-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1424-	if current_time >= event["time"]:
1425-		match event["action"]:
1426-			"deploy":
1427:				_deploy_canopy()
1428-			"cutaway":
1429-				_do_cutaway()
1430-			"reserve":
--
1658-
1659-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1660-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1661:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1662-	# so it fires on the first FREEFALL _poll_controls() call.
1663-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1664-	# (general knowledge — not retrieved this session)
1665-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1666-		_headless_auto_deploy = false
1667-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1668:		_deploy_canopy()
1669-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1670-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1671:		_deploy_canopy()
1672-
1673-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1674-		var turn_input := 0.0
--
1688-
1689-		_turn_input = turn_input
1690-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1691:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1692-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1693-# canopy could never accumulate a turn. Guard was commented out; call removed.
1694-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1707-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1708-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1709-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1710:			_deploy_canopy()
1711-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1712-	# Turn left (Q)
1713-	if Input.is_key_pressed(KEY_Q):
--
2156-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2157-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2158-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2159:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2160-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2161-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2162-		# They are now in _update_hud_readouts(), called unconditionally from
--
2170-			_screenshot_save_timer = 5.0
2171-
2172-	# OPENING_ANIM -> DIAGNOSIS transition (this session):
2173:	# _deployment_timer is set to 2.0 at line 874 in _deploy_canopy()
2174-	# but was NEVER decremented anywhere in the file (proven by
2175-	# notes/diag_opening_anim_20260804182915.txt PART H: 0 hits for
2176-	# '_deployment_timer -= '). State stuck at OPENING_ANIM = 4896

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
552-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
553-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
554-	# Headless auto‑start: simulate SPACE press
555:	if OS.get_environment("GODOT_HEADLESS") == "1":
556-		Input.action_press("ui_accept")
557-		Input.action_release("ui_accept")
558-		print("[VERBATIM] Headless auto‑start triggered.")
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
--
583-	# autostall's 30 s stall detector cannot trip on it.
584-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
585-	# (general knowledge - not retrieved this session)
586:	if OS.get_environment("GODOT_HEADLESS") == "1" \
587-			or "--headless" in OS.get_cmdline_args():
588-		var _pause_timer := Timer.new()
589-		_pause_timer.wait_time = 8.0
--
2029-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2030-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2031-			#  by _0283.txt showing plane position update before stall)
2032:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2033-				if not ProjectSettings.has_setting("_headless_space_fired"):
2034-					ProjectSettings.set_setting("_headless_space_fired", true)
2035-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804195226 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804195342 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
126-var _canopy_instance: Node3D
127-var _canopy_material: StandardMaterial3D
128-var _canopy_deployed: bool = false
129:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
130-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
131-var _deployment_timer
132-var _screenshot_save_timer: float = 0.0
--
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
565-	_check_arm_pose_safe()
--
1622-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-		var _exit_pressed = (
1624-			Input.is_action_just_pressed("deploy")
1625:			or Input.is_key_pressed(KEY_J)
1626:			or _headless_auto_jump
1627-		)
1628:		_headless_auto_jump = false  # consume the flag
1629-		if _exit_pressed:
1630-			print("[DIAG] _poll_controls: exit aircraft triggered")
1631-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3754-	"togglehud":    KEY_H,
3755-	"flightcheck":  KEY_G,
3756-	"restart":      KEY_R,
3757:	"j":            KEY_J,
3758-	"pause":        KEY_ESCAPE,
3759-
3760-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
863-	return null
864-
865-
866:func _deploy_canopy() -> void:
867-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
868-	if _canopy_deployed or not _canopy_instance:
869-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1424-	if current_time >= event["time"]:
1425-		match event["action"]:
1426-			"deploy":
1427:				_deploy_canopy()
1428-			"cutaway":
1429-				_do_cutaway()
1430-			"reserve":
--
1658-
1659-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1660-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1661:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1662-	# so it fires on the first FREEFALL _poll_controls() call.
1663-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1664-	# (general knowledge — not retrieved this session)
1665-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1666-		_headless_auto_deploy = false
1667-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1668:		_deploy_canopy()
1669-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1670-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1671:		_deploy_canopy()
1672-
1673-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1674-		var turn_input := 0.0
--
1688-
1689-		_turn_input = turn_input
1690-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1691:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1692-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1693-# canopy could never accumulate a turn. Guard was commented out; call removed.
1694-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1707-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1708-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1709-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1710:			_deploy_canopy()
1711-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1712-	# Turn left (Q)
1713-	if Input.is_key_pressed(KEY_Q):
--
2156-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2157-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2158-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2159:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2160-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2161-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2162-		# They are now in _update_hud_readouts(), called unconditionally from
--
2170-			_screenshot_save_timer = 5.0
2171-
2172-	# OPENING_ANIM -> DIAGNOSIS transition (this session):
2173:	# _deployment_timer is set to 2.0 at line 874 in _deploy_canopy()
2174-	# but was NEVER decremented anywhere in the file (proven by
2175-	# notes/diag_opening_anim_20260804182915.txt PART H: 0 hits for
2176-	# '_deployment_timer -= '). State stuck at OPENING_ANIM = 4896

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
552-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
553-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
554-	# Headless auto‑start: simulate SPACE press
555:	if OS.get_environment("GODOT_HEADLESS") == "1":
556-		Input.action_press("ui_accept")
557-		Input.action_release("ui_accept")
558-		print("[VERBATIM] Headless auto‑start triggered.")
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
--
583-	# autostall's 30 s stall detector cannot trip on it.
584-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
585-	# (general knowledge - not retrieved this session)
586:	if OS.get_environment("GODOT_HEADLESS") == "1" \
587-			or "--headless" in OS.get_cmdline_args():
588-		var _pause_timer := Timer.new()
589-		_pause_timer.wait_time = 8.0
--
2029-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2030-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2031-			#  by _0283.txt showing plane position update before stall)
2032:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2033-				if not ProjectSettings.has_setting("_headless_space_fired"):
2034-					ProjectSettings.set_setting("_headless_space_fired", true)
2035-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804195342 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804202042 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
126-var _canopy_instance: Node3D
127-var _canopy_material: StandardMaterial3D
128-var _canopy_deployed: bool = false
129:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
130-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
131-var _deployment_timer
132-var _screenshot_save_timer: float = 0.0
--
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
565-	_check_arm_pose_safe()
--
1622-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-		var _exit_pressed = (
1624-			Input.is_action_just_pressed("deploy")
1625:			or Input.is_key_pressed(KEY_J)
1626:			or _headless_auto_jump
1627-		)
1628:		_headless_auto_jump = false  # consume the flag
1629-		if _exit_pressed:
1630-			print("[DIAG] _poll_controls: exit aircraft triggered")
1631-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3754-	"togglehud":    KEY_H,
3755-	"flightcheck":  KEY_G,
3756-	"restart":      KEY_R,
3757:	"j":            KEY_J,
3758-	"pause":        KEY_ESCAPE,
3759-
3760-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
863-	return null
864-
865-
866:func _deploy_canopy() -> void:
867-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
868-	if _canopy_deployed or not _canopy_instance:
869-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1424-	if current_time >= event["time"]:
1425-		match event["action"]:
1426-			"deploy":
1427:				_deploy_canopy()
1428-			"cutaway":
1429-				_do_cutaway()
1430-			"reserve":
--
1658-
1659-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1660-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1661:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1662-	# so it fires on the first FREEFALL _poll_controls() call.
1663-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1664-	# (general knowledge — not retrieved this session)
1665-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1666-		_headless_auto_deploy = false
1667-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1668:		_deploy_canopy()
1669-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1670-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1671:		_deploy_canopy()
1672-
1673-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1674-		var turn_input := 0.0
--
1688-
1689-		_turn_input = turn_input
1690-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1691:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1692-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1693-# canopy could never accumulate a turn. Guard was commented out; call removed.
1694-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1707-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1708-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1709-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1710:			_deploy_canopy()
1711-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1712-	# Turn left (Q)
1713-	if Input.is_key_pressed(KEY_Q):
--
2156-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2157-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2158-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2159:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2160-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2161-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2162-		# They are now in _update_hud_readouts(), called unconditionally from
--
2170-			_screenshot_save_timer = 5.0
2171-
2172-	# OPENING_ANIM -> DIAGNOSIS transition (this session):
2173:	# _deployment_timer is set to 2.0 at line 874 in _deploy_canopy()
2174-	# but was NEVER decremented anywhere in the file (proven by
2175-	# notes/diag_opening_anim_20260804182915.txt PART H: 0 hits for
2176-	# '_deployment_timer -= '). State stuck at OPENING_ANIM = 4896

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
552-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
553-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
554-	# Headless auto‑start: simulate SPACE press
555:	if OS.get_environment("GODOT_HEADLESS") == "1":
556-		Input.action_press("ui_accept")
557-		Input.action_release("ui_accept")
558-		print("[VERBATIM] Headless auto‑start triggered.")
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
--
583-	# autostall's 30 s stall detector cannot trip on it.
584-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
585-	# (general knowledge - not retrieved this session)
586:	if OS.get_environment("GODOT_HEADLESS") == "1" \
587-			or "--headless" in OS.get_cmdline_args():
588-		var _pause_timer := Timer.new()
589-		_pause_timer.wait_time = 8.0
--
2029-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2030-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2031-			#  by _0283.txt showing plane position update before stall)
2032:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2033-				if not ProjectSettings.has_setting("_headless_space_fired"):
2034-					ProjectSettings.set_setting("_headless_space_fired", true)
2035-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804202042 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804202338 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
126-var _canopy_instance: Node3D
127-var _canopy_material: StandardMaterial3D
128-var _canopy_deployed: bool = false
129:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
130-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
131-var _deployment_timer
132-var _screenshot_save_timer: float = 0.0
--
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
565-	_check_arm_pose_safe()
--
1622-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-		var _exit_pressed = (
1624-			Input.is_action_just_pressed("deploy")
1625:			or Input.is_key_pressed(KEY_J)
1626:			or _headless_auto_jump
1627-		)
1628:		_headless_auto_jump = false  # consume the flag
1629-		if _exit_pressed:
1630-			print("[DIAG] _poll_controls: exit aircraft triggered")
1631-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3754-	"togglehud":    KEY_H,
3755-	"flightcheck":  KEY_G,
3756-	"restart":      KEY_R,
3757:	"j":            KEY_J,
3758-	"pause":        KEY_ESCAPE,
3759-
3760-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
863-	return null
864-
865-
866:func _deploy_canopy() -> void:
867-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
868-	if _canopy_deployed or not _canopy_instance:
869-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1424-	if current_time >= event["time"]:
1425-		match event["action"]:
1426-			"deploy":
1427:				_deploy_canopy()
1428-			"cutaway":
1429-				_do_cutaway()
1430-			"reserve":
--
1658-
1659-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1660-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1661:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1662-	# so it fires on the first FREEFALL _poll_controls() call.
1663-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1664-	# (general knowledge — not retrieved this session)
1665-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1666-		_headless_auto_deploy = false
1667-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1668:		_deploy_canopy()
1669-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1670-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1671:		_deploy_canopy()
1672-
1673-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1674-		var turn_input := 0.0
--
1688-
1689-		_turn_input = turn_input
1690-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1691:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1692-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1693-# canopy could never accumulate a turn. Guard was commented out; call removed.
1694-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1707-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1708-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1709-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1710:			_deploy_canopy()
1711-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1712-	# Turn left (Q)
1713-	if Input.is_key_pressed(KEY_Q):
--
2156-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2157-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2158-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2159:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2160-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2161-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2162-		# They are now in _update_hud_readouts(), called unconditionally from
--
2170-			_screenshot_save_timer = 5.0
2171-
2172-	# OPENING_ANIM -> DIAGNOSIS transition (this session):
2173:	# _deployment_timer is set to 2.0 at line 874 in _deploy_canopy()
2174-	# but was NEVER decremented anywhere in the file (proven by
2175-	# notes/diag_opening_anim_20260804182915.txt PART H: 0 hits for
2176-	# '_deployment_timer -= '). State stuck at OPENING_ANIM = 4896

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
552-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
553-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
554-	# Headless auto‑start: simulate SPACE press
555:	if OS.get_environment("GODOT_HEADLESS") == "1":
556-		Input.action_press("ui_accept")
557-		Input.action_release("ui_accept")
558-		print("[VERBATIM] Headless auto‑start triggered.")
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
--
583-	# autostall's 30 s stall detector cannot trip on it.
584-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
585-	# (general knowledge - not retrieved this session)
586:	if OS.get_environment("GODOT_HEADLESS") == "1" \
587-			or "--headless" in OS.get_cmdline_args():
588-		var _pause_timer := Timer.new()
589-		_pause_timer.wait_time = 8.0
--
2029-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2030-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2031-			#  by _0283.txt showing plane position update before stall)
2032:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2033-				if not ProjectSettings.has_setting("_headless_space_fired"):
2034-					ProjectSettings.set_setting("_headless_space_fired", true)
2035-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804202338 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804202613 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
126-var _canopy_instance: Node3D
127-var _canopy_material: StandardMaterial3D
128-var _canopy_deployed: bool = false
129:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
130-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
131-var _deployment_timer
132-var _screenshot_save_timer: float = 0.0
--
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
565-	_check_arm_pose_safe()
--
1622-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1623-		var _exit_pressed = (
1624-			Input.is_action_just_pressed("deploy")
1625:			or Input.is_key_pressed(KEY_J)
1626:			or _headless_auto_jump
1627-		)
1628:		_headless_auto_jump = false  # consume the flag
1629-		if _exit_pressed:
1630-			print("[DIAG] _poll_controls: exit aircraft triggered")
1631-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3754-	"togglehud":    KEY_H,
3755-	"flightcheck":  KEY_G,
3756-	"restart":      KEY_R,
3757:	"j":            KEY_J,
3758-	"pause":        KEY_ESCAPE,
3759-
3760-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
863-	return null
864-
865-
866:func _deploy_canopy() -> void:
867-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
868-	if _canopy_deployed or not _canopy_instance:
869-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1424-	if current_time >= event["time"]:
1425-		match event["action"]:
1426-			"deploy":
1427:				_deploy_canopy()
1428-			"cutaway":
1429-				_do_cutaway()
1430-			"reserve":
--
1658-
1659-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1660-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1661:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1662-	# so it fires on the first FREEFALL _poll_controls() call.
1663-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1664-	# (general knowledge — not retrieved this session)
1665-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1666-		_headless_auto_deploy = false
1667-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1668:		_deploy_canopy()
1669-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1670-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1671:		_deploy_canopy()
1672-
1673-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1674-		var turn_input := 0.0
--
1688-
1689-		_turn_input = turn_input
1690-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1691:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1692-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1693-# canopy could never accumulate a turn. Guard was commented out; call removed.
1694-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1707-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1708-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1709-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1710:			_deploy_canopy()
1711-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1712-	# Turn left (Q)
1713-	if Input.is_key_pressed(KEY_Q):
--
2156-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2157-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2158-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2159:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2160-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2161-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2162-		# They are now in _update_hud_readouts(), called unconditionally from
--
2170-			_screenshot_save_timer = 5.0
2171-
2172-	# OPENING_ANIM -> DIAGNOSIS transition (this session):
2173:	# _deployment_timer is set to 2.0 at line 874 in _deploy_canopy()
2174-	# but was NEVER decremented anywhere in the file (proven by
2175-	# notes/diag_opening_anim_20260804182915.txt PART H: 0 hits for
2176-	# '_deployment_timer -= '). State stuck at OPENING_ANIM = 4896

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
552-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
553-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
554-	# Headless auto‑start: simulate SPACE press
555:	if OS.get_environment("GODOT_HEADLESS") == "1":
556-		Input.action_press("ui_accept")
557-		Input.action_release("ui_accept")
558-		print("[VERBATIM] Headless auto‑start triggered.")
559-	# Headless auto-start: simulate SPACE/deploy press
560-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
561:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
562-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
563-		# Input.action_release("deploy") removed — flag-based now
564-		print("[VERBATIM] Headless auto‑start triggered.")
--
583-	# autostall's 30 s stall detector cannot trip on it.
584-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
585-	# (general knowledge - not retrieved this session)
586:	if OS.get_environment("GODOT_HEADLESS") == "1" \
587-			or "--headless" in OS.get_cmdline_args():
588-		var _pause_timer := Timer.new()
589-		_pause_timer.wait_time = 8.0
--
2029-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2030-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2031-			#  by _0283.txt showing plane position update before stall)
2032:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2033-				if not ProjectSettings.has_setting("_headless_space_fired"):
2034-					ProjectSettings.set_setting("_headless_space_fired", true)
2035-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804202613 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804211933 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-const DEPLOY_TIME: float = 1.2
--
561-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
562-	# Headless auto‑start: simulate SPACE press
563-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
564:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
565-		# Input.action_release("deploy") removed — flag-based now
566-		print("[VERBATIM] Headless auto‑start triggered.")
567-	_check_arm_pose_safe()
--
1564-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1565-		var _exit_pressed = (
1566-			Input.is_action_just_pressed("deploy")
1567:			or Input.is_key_pressed(KEY_J)
1568:			or _headless_auto_jump
1569-		)
1570:		_headless_auto_jump = false  # consume the flag
1571-		if _exit_pressed:
1572-			print("[DIAG] _poll_controls: exit aircraft triggered")
1573-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3537-	"togglehud":    KEY_H,
3538-	"flightcheck":  KEY_G,
3539-	"restart":      KEY_R,
3540:	"j":            KEY_J,
3541-	"pause":        KEY_ESCAPE,
3542-}
3543-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
835-	return null
836-
837-
838:func _deploy_canopy() -> void:
839-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
840-	if _canopy_deployed or not _canopy_instance:
841-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1370-	if current_time >= event["time"]:
1371-		match event["action"]:
1372-			"deploy":
1373:				_deploy_canopy()
1374-			"cutaway":
1375-				_do_cutaway()
1376-			"reserve":
--
1601-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1602-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1603-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1604:		_deploy_canopy()
1605-
1606-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1607-		var turn_input := 0.0
--
1639-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1640-			else:
1641-				print("[STEER_TELEM] /root/Main does not exist")
1642:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1643-		# ----- Direct key checks (bypass Input Map) -----
1644-		# Deploy (SPACE)
1645-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1646:		_deploy_canopy()
1647-	# Turn left (Q)
1648-	if Input.is_key_pressed(KEY_Q):
1649-		_turn_input = -1.0
--
1674-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1675-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1676-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1677:			_deploy_canopy()
1678-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1679-	# Turn left (Q)
1680-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
560-	# --- end auto-start ---
561-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
562-	# Headless auto‑start: simulate SPACE press
563:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
564-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
565-		# Input.action_release("deploy") removed — flag-based now
566-		print("[VERBATIM] Headless auto‑start triggered.")
--
1974-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1975-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1976-			#  by _0283.txt showing plane position update before stall)
1977:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1978-				if not ProjectSettings.has_setting("_headless_space_fired"):
1979-					ProjectSettings.set_setting("_headless_space_fired", true)
1980-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804211933 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804214002 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
568-	_check_arm_pose_safe()
--
1565-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1566-		var _exit_pressed = (
1567-			Input.is_action_just_pressed("deploy")
1568:			or Input.is_key_pressed(KEY_J)
1569:			or _headless_auto_jump
1570-		)
1571:		_headless_auto_jump = false  # consume the flag
1572-		if _exit_pressed:
1573-			print("[DIAG] _poll_controls: exit aircraft triggered")
1574-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3547-	"togglehud":    KEY_H,
3548-	"flightcheck":  KEY_G,
3549-	"restart":      KEY_R,
3550:	"j":            KEY_J,
3551-	"pause":        KEY_ESCAPE,
3552-}
3553-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
836-	return null
837-
838-
839:func _deploy_canopy() -> void:
840-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
841-	if _canopy_deployed or not _canopy_instance:
842-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1371-	if current_time >= event["time"]:
1372-		match event["action"]:
1373-			"deploy":
1374:				_deploy_canopy()
1375-			"cutaway":
1376-				_do_cutaway()
1377-			"reserve":
--
1602-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1603-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1604-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1605:		_deploy_canopy()
1606-
1607-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1608-		var turn_input := 0.0
--
1640-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1641-			else:
1642-				print("[STEER_TELEM] /root/Main does not exist")
1643:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1644-		# ----- Direct key checks (bypass Input Map) -----
1645-		# Deploy (SPACE)
1646-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1647:		_deploy_canopy()
1648-	# Turn left (Q)
1649-	if Input.is_key_pressed(KEY_Q):
1650-		_turn_input = -1.0
--
1675-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1676-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1677-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1678:			_deploy_canopy()
1679-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1680-	# Turn left (Q)
1681-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
561-	# --- end auto-start ---
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
--
1977-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1978-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1979-			#  by _0283.txt showing plane position update before stall)
1980:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1981-				if not ProjectSettings.has_setting("_headless_space_fired"):
1982-					ProjectSettings.set_setting("_headless_space_fired", true)
1983-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804214002 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804214043 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
568-	_check_arm_pose_safe()
--
1565-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1566-		var _exit_pressed = (
1567-			Input.is_action_just_pressed("deploy")
1568:			or Input.is_key_pressed(KEY_J)
1569:			or _headless_auto_jump
1570-		)
1571:		_headless_auto_jump = false  # consume the flag
1572-		if _exit_pressed:
1573-			print("[DIAG] _poll_controls: exit aircraft triggered")
1574-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3547-	"togglehud":    KEY_H,
3548-	"flightcheck":  KEY_G,
3549-	"restart":      KEY_R,
3550:	"j":            KEY_J,
3551-	"pause":        KEY_ESCAPE,
3552-}
3553-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
836-	return null
837-
838-
839:func _deploy_canopy() -> void:
840-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
841-	if _canopy_deployed or not _canopy_instance:
842-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1371-	if current_time >= event["time"]:
1372-		match event["action"]:
1373-			"deploy":
1374:				_deploy_canopy()
1375-			"cutaway":
1376-				_do_cutaway()
1377-			"reserve":
--
1602-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1603-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1604-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1605:		_deploy_canopy()
1606-
1607-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1608-		var turn_input := 0.0
--
1640-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1641-			else:
1642-				print("[STEER_TELEM] /root/Main does not exist")
1643:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1644-		# ----- Direct key checks (bypass Input Map) -----
1645-		# Deploy (SPACE)
1646-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1647:		_deploy_canopy()
1648-	# Turn left (Q)
1649-	if Input.is_key_pressed(KEY_Q):
1650-		_turn_input = -1.0
--
1675-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1676-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1677-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1678:			_deploy_canopy()
1679-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1680-	# Turn left (Q)
1681-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
561-	# --- end auto-start ---
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
--
1977-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1978-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1979-			#  by _0283.txt showing plane position update before stall)
1980:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1981-				if not ProjectSettings.has_setting("_headless_space_fired"):
1982-					ProjectSettings.set_setting("_headless_space_fired", true)
1983-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804214043 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804214419 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
568-	_check_arm_pose_safe()
--
1565-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1566-		var _exit_pressed = (
1567-			Input.is_action_just_pressed("deploy")
1568:			or Input.is_key_pressed(KEY_J)
1569:			or _headless_auto_jump
1570-		)
1571:		_headless_auto_jump = false  # consume the flag
1572-		if _exit_pressed:
1573-			print("[DIAG] _poll_controls: exit aircraft triggered")
1574-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3547-	"togglehud":    KEY_H,
3548-	"flightcheck":  KEY_G,
3549-	"restart":      KEY_R,
3550:	"j":            KEY_J,
3551-	"pause":        KEY_ESCAPE,
3552-}
3553-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
836-	return null
837-
838-
839:func _deploy_canopy() -> void:
840-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
841-	if _canopy_deployed or not _canopy_instance:
842-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1371-	if current_time >= event["time"]:
1372-		match event["action"]:
1373-			"deploy":
1374:				_deploy_canopy()
1375-			"cutaway":
1376-				_do_cutaway()
1377-			"reserve":
--
1602-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1603-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1604-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1605:		_deploy_canopy()
1606-
1607-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1608-		var turn_input := 0.0
--
1640-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1641-			else:
1642-				print("[STEER_TELEM] /root/Main does not exist")
1643:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1644-		# ----- Direct key checks (bypass Input Map) -----
1645-		# Deploy (SPACE)
1646-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1647:		_deploy_canopy()
1648-	# Turn left (Q)
1649-	if Input.is_key_pressed(KEY_Q):
1650-		_turn_input = -1.0
--
1675-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1676-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1677-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1678:			_deploy_canopy()
1679-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1680-	# Turn left (Q)
1681-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
561-	# --- end auto-start ---
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
--
1977-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1978-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1979-			#  by _0283.txt showing plane position update before stall)
1980:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1981-				if not ProjectSettings.has_setting("_headless_space_fired"):
1982-					ProjectSettings.set_setting("_headless_space_fired", true)
1983-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804214419 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804214652 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
568-	_check_arm_pose_safe()
--
1565-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1566-		var _exit_pressed = (
1567-			Input.is_action_just_pressed("deploy")
1568:			or Input.is_key_pressed(KEY_J)
1569:			or _headless_auto_jump
1570-		)
1571:		_headless_auto_jump = false  # consume the flag
1572-		if _exit_pressed:
1573-			print("[DIAG] _poll_controls: exit aircraft triggered")
1574-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3547-	"togglehud":    KEY_H,
3548-	"flightcheck":  KEY_G,
3549-	"restart":      KEY_R,
3550:	"j":            KEY_J,
3551-	"pause":        KEY_ESCAPE,
3552-}
3553-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
836-	return null
837-
838-
839:func _deploy_canopy() -> void:
840-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
841-	if _canopy_deployed or not _canopy_instance:
842-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1371-	if current_time >= event["time"]:
1372-		match event["action"]:
1373-			"deploy":
1374:				_deploy_canopy()
1375-			"cutaway":
1376-				_do_cutaway()
1377-			"reserve":
--
1602-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1603-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1604-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1605:		_deploy_canopy()
1606-
1607-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1608-		var turn_input := 0.0
--
1640-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1641-			else:
1642-				print("[STEER_TELEM] /root/Main does not exist")
1643:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1644-		# ----- Direct key checks (bypass Input Map) -----
1645-		# Deploy (SPACE)
1646-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1647:		_deploy_canopy()
1648-	# Turn left (Q)
1649-	if Input.is_key_pressed(KEY_Q):
1650-		_turn_input = -1.0
--
1675-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1676-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1677-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1678:			_deploy_canopy()
1679-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1680-	# Turn left (Q)
1681-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
561-	# --- end auto-start ---
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
--
1977-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1978-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1979-			#  by _0283.txt showing plane position update before stall)
1980:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1981-				if not ProjectSettings.has_setting("_headless_space_fired"):
1982-					ProjectSettings.set_setting("_headless_space_fired", true)
1983-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804214652 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804214926 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
568-	_check_arm_pose_safe()
--
1565-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1566-		var _exit_pressed = (
1567-			Input.is_action_just_pressed("deploy")
1568:			or Input.is_key_pressed(KEY_J)
1569:			or _headless_auto_jump
1570-		)
1571:		_headless_auto_jump = false  # consume the flag
1572-		if _exit_pressed:
1573-			print("[DIAG] _poll_controls: exit aircraft triggered")
1574-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3547-	"togglehud":    KEY_H,
3548-	"flightcheck":  KEY_G,
3549-	"restart":      KEY_R,
3550:	"j":            KEY_J,
3551-	"pause":        KEY_ESCAPE,
3552-}
3553-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
836-	return null
837-
838-
839:func _deploy_canopy() -> void:
840-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
841-	if _canopy_deployed or not _canopy_instance:
842-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1371-	if current_time >= event["time"]:
1372-		match event["action"]:
1373-			"deploy":
1374:				_deploy_canopy()
1375-			"cutaway":
1376-				_do_cutaway()
1377-			"reserve":
--
1602-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1603-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1604-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1605:		_deploy_canopy()
1606-
1607-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1608-		var turn_input := 0.0
--
1640-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1641-			else:
1642-				print("[STEER_TELEM] /root/Main does not exist")
1643:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1644-		# ----- Direct key checks (bypass Input Map) -----
1645-		# Deploy (SPACE)
1646-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1647:		_deploy_canopy()
1648-	# Turn left (Q)
1649-	if Input.is_key_pressed(KEY_Q):
1650-		_turn_input = -1.0
--
1675-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1676-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1677-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1678:			_deploy_canopy()
1679-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1680-	# Turn left (Q)
1681-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
561-	# --- end auto-start ---
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
--
1977-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1978-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1979-			#  by _0283.txt showing plane position update before stall)
1980:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1981-				if not ProjectSettings.has_setting("_headless_space_fired"):
1982-					ProjectSettings.set_setting("_headless_space_fired", true)
1983-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804214926 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804215645 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
568-	_check_arm_pose_safe()
--
1565-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1566-		var _exit_pressed = (
1567-			Input.is_action_just_pressed("deploy")
1568:			or Input.is_key_pressed(KEY_J)
1569:			or _headless_auto_jump
1570-		)
1571:		_headless_auto_jump = false  # consume the flag
1572-		if _exit_pressed:
1573-			print("[DIAG] _poll_controls: exit aircraft triggered")
1574-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3547-	"togglehud":    KEY_H,
3548-	"flightcheck":  KEY_G,
3549-	"restart":      KEY_R,
3550:	"j":            KEY_J,
3551-	"pause":        KEY_ESCAPE,
3552-}
3553-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
836-	return null
837-
838-
839:func _deploy_canopy() -> void:
840-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
841-	if _canopy_deployed or not _canopy_instance:
842-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1371-	if current_time >= event["time"]:
1372-		match event["action"]:
1373-			"deploy":
1374:				_deploy_canopy()
1375-			"cutaway":
1376-				_do_cutaway()
1377-			"reserve":
--
1602-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1603-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1604-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1605:		_deploy_canopy()
1606-
1607-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1608-		var turn_input := 0.0
--
1640-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1641-			else:
1642-				print("[STEER_TELEM] /root/Main does not exist")
1643:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1644-		# ----- Direct key checks (bypass Input Map) -----
1645-		# Deploy (SPACE)
1646-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1647:		_deploy_canopy()
1648-	# Turn left (Q)
1649-	if Input.is_key_pressed(KEY_Q):
1650-		_turn_input = -1.0
--
1675-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1676-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1677-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1678:			_deploy_canopy()
1679-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1680-	# Turn left (Q)
1681-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
561-	# --- end auto-start ---
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
--
1977-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1978-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1979-			#  by _0283.txt showing plane position update before stall)
1980:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1981-				if not ProjectSettings.has_setting("_headless_space_fired"):
1982-					ProjectSettings.set_setting("_headless_space_fired", true)
1983-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804215645 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804215805 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
568-	_check_arm_pose_safe()
--
1565-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1566-		var _exit_pressed = (
1567-			Input.is_action_just_pressed("deploy")
1568:			or Input.is_key_pressed(KEY_J)
1569:			or _headless_auto_jump
1570-		)
1571:		_headless_auto_jump = false  # consume the flag
1572-		if _exit_pressed:
1573-			print("[DIAG] _poll_controls: exit aircraft triggered")
1574-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3547-	"togglehud":    KEY_H,
3548-	"flightcheck":  KEY_G,
3549-	"restart":      KEY_R,
3550:	"j":            KEY_J,
3551-	"pause":        KEY_ESCAPE,
3552-}
3553-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
836-	return null
837-
838-
839:func _deploy_canopy() -> void:
840-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
841-	if _canopy_deployed or not _canopy_instance:
842-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1371-	if current_time >= event["time"]:
1372-		match event["action"]:
1373-			"deploy":
1374:				_deploy_canopy()
1375-			"cutaway":
1376-				_do_cutaway()
1377-			"reserve":
--
1602-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1603-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1604-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1605:		_deploy_canopy()
1606-
1607-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1608-		var turn_input := 0.0
--
1640-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1641-			else:
1642-				print("[STEER_TELEM] /root/Main does not exist")
1643:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1644-		# ----- Direct key checks (bypass Input Map) -----
1645-		# Deploy (SPACE)
1646-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1647:		_deploy_canopy()
1648-	# Turn left (Q)
1649-	if Input.is_key_pressed(KEY_Q):
1650-		_turn_input = -1.0
--
1675-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1676-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1677-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1678:			_deploy_canopy()
1679-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1680-	# Turn left (Q)
1681-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
561-	# --- end auto-start ---
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
--
1977-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1978-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1979-			#  by _0283.txt showing plane position update before stall)
1980:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1981-				if not ProjectSettings.has_setting("_headless_space_fired"):
1982-					ProjectSettings.set_setting("_headless_space_fired", true)
1983-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804215805 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804215921 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
568-	_check_arm_pose_safe()
--
1565-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1566-		var _exit_pressed = (
1567-			Input.is_action_just_pressed("deploy")
1568:			or Input.is_key_pressed(KEY_J)
1569:			or _headless_auto_jump
1570-		)
1571:		_headless_auto_jump = false  # consume the flag
1572-		if _exit_pressed:
1573-			print("[DIAG] _poll_controls: exit aircraft triggered")
1574-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3540-	"togglehud":    KEY_H,
3541-	"flightcheck":  KEY_G,
3542-	"restart":      KEY_R,
3543:	"j":            KEY_J,
3544-	"pause":        KEY_ESCAPE,
3545-}
3546-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
836-	return null
837-
838-
839:func _deploy_canopy() -> void:
840-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
841-	if _canopy_deployed or not _canopy_instance:
842-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1371-	if current_time >= event["time"]:
1372-		match event["action"]:
1373-			"deploy":
1374:				_deploy_canopy()
1375-			"cutaway":
1376-				_do_cutaway()
1377-			"reserve":
--
1602-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1603-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1604-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1605:		_deploy_canopy()
1606-
1607-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1608-		var turn_input := 0.0
--
1640-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1641-			else:
1642-				print("[STEER_TELEM] /root/Main does not exist")
1643:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1644-		# ----- Direct key checks (bypass Input Map) -----
1645-		# Deploy (SPACE)
1646-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1647:		_deploy_canopy()
1648-	# Turn left (Q)
1649-	if Input.is_key_pressed(KEY_Q):
1650-		_turn_input = -1.0
--
1675-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1676-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1677-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1678:			_deploy_canopy()
1679-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1680-	# Turn left (Q)
1681-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
561-	# --- end auto-start ---
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
--
1977-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1978-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1979-			#  by _0283.txt showing plane position update before stall)
1980:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1981-				if not ProjectSettings.has_setting("_headless_space_fired"):
1982-					ProjectSettings.set_setting("_headless_space_fired", true)
1983-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804215921 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804220004 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
568-	_check_arm_pose_safe()
--
1565-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1566-		var _exit_pressed = (
1567-			Input.is_action_just_pressed("deploy")
1568:			or Input.is_key_pressed(KEY_J)
1569:			or _headless_auto_jump
1570-		)
1571:		_headless_auto_jump = false  # consume the flag
1572-		if _exit_pressed:
1573-			print("[DIAG] _poll_controls: exit aircraft triggered")
1574-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3540-	"togglehud":    KEY_H,
3541-	"flightcheck":  KEY_G,
3542-	"restart":      KEY_R,
3543:	"j":            KEY_J,
3544-	"pause":        KEY_ESCAPE,
3545-}
3546-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
836-	return null
837-
838-
839:func _deploy_canopy() -> void:
840-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
841-	if _canopy_deployed or not _canopy_instance:
842-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1371-	if current_time >= event["time"]:
1372-		match event["action"]:
1373-			"deploy":
1374:				_deploy_canopy()
1375-			"cutaway":
1376-				_do_cutaway()
1377-			"reserve":
--
1602-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1603-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1604-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1605:		_deploy_canopy()
1606-
1607-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1608-		var turn_input := 0.0
--
1640-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1641-			else:
1642-				print("[STEER_TELEM] /root/Main does not exist")
1643:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1644-		# ----- Direct key checks (bypass Input Map) -----
1645-		# Deploy (SPACE)
1646-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1647:		_deploy_canopy()
1648-	# Turn left (Q)
1649-	if Input.is_key_pressed(KEY_Q):
1650-		_turn_input = -1.0
--
1675-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1676-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1677-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1678:			_deploy_canopy()
1679-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1680-	# Turn left (Q)
1681-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
561-	# --- end auto-start ---
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
--
1977-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1978-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1979-			#  by _0283.txt showing plane position update before stall)
1980:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1981-				if not ProjectSettings.has_setting("_headless_space_fired"):
1982-					ProjectSettings.set_setting("_headless_space_fired", true)
1983-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804220004 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804220201 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
568-	_check_arm_pose_safe()
--
1565-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1566-		var _exit_pressed = (
1567-			Input.is_action_just_pressed("deploy")
1568:			or Input.is_key_pressed(KEY_J)
1569:			or _headless_auto_jump
1570-		)
1571:		_headless_auto_jump = false  # consume the flag
1572-		if _exit_pressed:
1573-			print("[DIAG] _poll_controls: exit aircraft triggered")
1574-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3541-	"togglehud":    KEY_H,
3542-	"flightcheck":  KEY_G,
3543-	"restart":      KEY_R,
3544:	"j":            KEY_J,
3545-	"pause":        KEY_ESCAPE,
3546-}
3547-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
836-	return null
837-
838-
839:func _deploy_canopy() -> void:
840-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
841-	if _canopy_deployed or not _canopy_instance:
842-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1371-	if current_time >= event["time"]:
1372-		match event["action"]:
1373-			"deploy":
1374:				_deploy_canopy()
1375-			"cutaway":
1376-				_do_cutaway()
1377-			"reserve":
--
1602-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1603-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1604-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1605:		_deploy_canopy()
1606-
1607-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1608-		var turn_input := 0.0
--
1640-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1641-			else:
1642-				print("[STEER_TELEM] /root/Main does not exist")
1643:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1644-		# ----- Direct key checks (bypass Input Map) -----
1645-		# Deploy (SPACE)
1646-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1647:		_deploy_canopy()
1648-	# Turn left (Q)
1649-	if Input.is_key_pressed(KEY_Q):
1650-		_turn_input = -1.0
--
1675-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1676-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1677-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1678:			_deploy_canopy()
1679-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1680-	# Turn left (Q)
1681-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
561-	# --- end auto-start ---
562-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
563-	# Headless auto‑start: simulate SPACE press
564:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
565-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
566-		# Input.action_release("deploy") removed — flag-based now
567-		print("[VERBATIM] Headless auto‑start triggered.")
--
1977-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1978-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1979-			#  by _0283.txt showing plane position update before stall)
1980:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1981-				if not ProjectSettings.has_setting("_headless_space_fired"):
1982-					ProjectSettings.set_setting("_headless_space_fired", true)
1983-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804220201 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804220604 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
566-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
567-	# Headless auto‑start: simulate SPACE press
568-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
569:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
570-		# Input.action_release("deploy") removed — flag-based now
571-		print("[VERBATIM] Headless auto‑start triggered.")
572-	_check_arm_pose_safe()
--
1569-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1570-		var _exit_pressed = (
1571-			Input.is_action_just_pressed("deploy")
1572:			or Input.is_key_pressed(KEY_J)
1573:			or _headless_auto_jump
1574-		)
1575:		_headless_auto_jump = false  # consume the flag
1576-		if _exit_pressed:
1577-			print("[DIAG] _poll_controls: exit aircraft triggered")
1578-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3545-	"togglehud":    KEY_H,
3546-	"flightcheck":  KEY_G,
3547-	"restart":      KEY_R,
3548:	"j":            KEY_J,
3549-	"pause":        KEY_ESCAPE,
3550-}
3551-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
840-	return null
841-
842-
843:func _deploy_canopy() -> void:
844-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
845-	if _canopy_deployed or not _canopy_instance:
846-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1375-	if current_time >= event["time"]:
1376-		match event["action"]:
1377-			"deploy":
1378:				_deploy_canopy()
1379-			"cutaway":
1380-				_do_cutaway()
1381-			"reserve":
--
1606-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1607-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1608-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1609:		_deploy_canopy()
1610-
1611-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1612-		var turn_input := 0.0
--
1644-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1645-			else:
1646-				print("[STEER_TELEM] /root/Main does not exist")
1647:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1648-		# ----- Direct key checks (bypass Input Map) -----
1649-		# Deploy (SPACE)
1650-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1651:		_deploy_canopy()
1652-	# Turn left (Q)
1653-	if Input.is_key_pressed(KEY_Q):
1654-		_turn_input = -1.0
--
1679-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1680-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1681-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1682:			_deploy_canopy()
1683-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1684-	# Turn left (Q)
1685-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
565-	# --- end auto-start ---
566-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
567-	# Headless auto‑start: simulate SPACE press
568:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
569-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
570-		# Input.action_release("deploy") removed — flag-based now
571-		print("[VERBATIM] Headless auto‑start triggered.")
--
1981-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1982-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1983-			#  by _0283.txt showing plane position update before stall)
1984:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1985-				if not ProjectSettings.has_setting("_headless_space_fired"):
1986-					ProjectSettings.set_setting("_headless_space_fired", true)
1987-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804220604 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804220950 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
566-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
567-	# Headless auto‑start: simulate SPACE press
568-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
569:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
570-		# Input.action_release("deploy") removed — flag-based now
571-		print("[VERBATIM] Headless auto‑start triggered.")
572-	_check_arm_pose_safe()
--
1569-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1570-		var _exit_pressed = (
1571-			Input.is_action_just_pressed("deploy")
1572:			or Input.is_key_pressed(KEY_J)
1573:			or _headless_auto_jump
1574-		)
1575:		_headless_auto_jump = false  # consume the flag
1576-		if _exit_pressed:
1577-			print("[DIAG] _poll_controls: exit aircraft triggered")
1578-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3545-	"togglehud":    KEY_H,
3546-	"flightcheck":  KEY_G,
3547-	"restart":      KEY_R,
3548:	"j":            KEY_J,
3549-	"pause":        KEY_ESCAPE,
3550-}
3551-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
840-	return null
841-
842-
843:func _deploy_canopy() -> void:
844-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
845-	if _canopy_deployed or not _canopy_instance:
846-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1375-	if current_time >= event["time"]:
1376-		match event["action"]:
1377-			"deploy":
1378:				_deploy_canopy()
1379-			"cutaway":
1380-				_do_cutaway()
1381-			"reserve":
--
1606-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1607-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1608-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1609:		_deploy_canopy()
1610-
1611-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1612-		var turn_input := 0.0
--
1644-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1645-			else:
1646-				print("[STEER_TELEM] /root/Main does not exist")
1647:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1648-		# ----- Direct key checks (bypass Input Map) -----
1649-		# Deploy (SPACE)
1650-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1651:		_deploy_canopy()
1652-	# Turn left (Q)
1653-	if Input.is_key_pressed(KEY_Q):
1654-		_turn_input = -1.0
--
1679-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1680-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1681-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1682:			_deploy_canopy()
1683-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1684-	# Turn left (Q)
1685-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
565-	# --- end auto-start ---
566-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
567-	# Headless auto‑start: simulate SPACE press
568:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
569-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
570-		# Input.action_release("deploy") removed — flag-based now
571-		print("[VERBATIM] Headless auto‑start triggered.")
--
1981-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1982-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1983-			#  by _0283.txt showing plane position update before stall)
1984:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1985-				if not ProjectSettings.has_setting("_headless_space_fired"):
1986-					ProjectSettings.set_setting("_headless_space_fired", true)
1987-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804220950 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804221154 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
565-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
566-	# Headless auto‑start: simulate SPACE press
567-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
568:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
569-		# Input.action_release("deploy") removed — flag-based now
570-		print("[VERBATIM] Headless auto‑start triggered.")
571-	_check_arm_pose_safe()
--
1568-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1569-		var _exit_pressed = (
1570-			Input.is_action_just_pressed("deploy")
1571:			or Input.is_key_pressed(KEY_J)
1572:			or _headless_auto_jump
1573-		)
1574:		_headless_auto_jump = false  # consume the flag
1575-		if _exit_pressed:
1576-			print("[DIAG] _poll_controls: exit aircraft triggered")
1577-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3544-	"togglehud":    KEY_H,
3545-	"flightcheck":  KEY_G,
3546-	"restart":      KEY_R,
3547:	"j":            KEY_J,
3548-	"pause":        KEY_ESCAPE,
3549-}
3550-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
839-	return null
840-
841-
842:func _deploy_canopy() -> void:
843-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
844-	if _canopy_deployed or not _canopy_instance:
845-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1374-	if current_time >= event["time"]:
1375-		match event["action"]:
1376-			"deploy":
1377:				_deploy_canopy()
1378-			"cutaway":
1379-				_do_cutaway()
1380-			"reserve":
--
1605-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1606-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1607-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1608:		_deploy_canopy()
1609-
1610-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1611-		var turn_input := 0.0
--
1643-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1644-			else:
1645-				print("[STEER_TELEM] /root/Main does not exist")
1646:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1647-		# ----- Direct key checks (bypass Input Map) -----
1648-		# Deploy (SPACE)
1649-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1650:		_deploy_canopy()
1651-	# Turn left (Q)
1652-	if Input.is_key_pressed(KEY_Q):
1653-		_turn_input = -1.0
--
1678-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1679-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1680-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1681:			_deploy_canopy()
1682-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1683-	# Turn left (Q)
1684-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
564-	# --- end auto-start ---
565-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
566-	# Headless auto‑start: simulate SPACE press
567:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
568-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
569-		# Input.action_release("deploy") removed — flag-based now
570-		print("[VERBATIM] Headless auto‑start triggered.")
--
1980-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1981-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1982-			#  by _0283.txt showing plane position update before stall)
1983:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1984-				if not ProjectSettings.has_setting("_headless_space_fired"):
1985-					ProjectSettings.set_setting("_headless_space_fired", true)
1986-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804221154 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804222608 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
569-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3543-	"togglehud":    KEY_H,
3544-	"flightcheck":  KEY_G,
3545-	"restart":      KEY_R,
3546:	"j":            KEY_J,
3547-	"pause":        KEY_ESCAPE,
3548-}
3549-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
837-	return null
838-
839-
840:func _deploy_canopy() -> void:
841-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
842-	if _canopy_deployed or not _canopy_instance:
843-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1372-	if current_time >= event["time"]:
1373-		match event["action"]:
1374-			"deploy":
1375:				_deploy_canopy()
1376-			"cutaway":
1377-				_do_cutaway()
1378-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1641-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1642-			else:
1643-				print("[STEER_TELEM] /root/Main does not exist")
1644:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1645-		# ----- Direct key checks (bypass Input Map) -----
1646-		# Deploy (SPACE)
1647-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1648:		_deploy_canopy()
1649-	# Turn left (Q)
1650-	if Input.is_key_pressed(KEY_Q):
1651-		_turn_input = -1.0
--
1676-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1677-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1678-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1679:			_deploy_canopy()
1680-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1681-	# Turn left (Q)
1682-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
562-	# --- end auto-start ---
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
--
1978-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1979-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1980-			#  by _0283.txt showing plane position update before stall)
1981:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1982-				if not ProjectSettings.has_setting("_headless_space_fired"):
1983-					ProjectSettings.set_setting("_headless_space_fired", true)
1984-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804222608 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804222953 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
569-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3545-	"togglehud":    KEY_H,
3546-	"flightcheck":  KEY_G,
3547-	"restart":      KEY_R,
3548:	"j":            KEY_J,
3549-	"pause":        KEY_ESCAPE,
3550-}
3551-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
837-	return null
838-
839-
840:func _deploy_canopy() -> void:
841-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
842-	if _canopy_deployed or not _canopy_instance:
843-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1372-	if current_time >= event["time"]:
1373-		match event["action"]:
1374-			"deploy":
1375:				_deploy_canopy()
1376-			"cutaway":
1377-				_do_cutaway()
1378-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1641-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1642-			else:
1643-				print("[STEER_TELEM] /root/Main does not exist")
1644:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1645-		# ----- Direct key checks (bypass Input Map) -----
1646-		# Deploy (SPACE)
1647-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1648:		_deploy_canopy()
1649-	# Turn left (Q)
1650-	if Input.is_key_pressed(KEY_Q):
1651-		_turn_input = -1.0
--
1676-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1677-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1678-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1679:			_deploy_canopy()
1680-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1681-	# Turn left (Q)
1682-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
562-	# --- end auto-start ---
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
--
1978-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1979-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1980-			#  by _0283.txt showing plane position update before stall)
1981:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1982-				if not ProjectSettings.has_setting("_headless_space_fired"):
1983-					ProjectSettings.set_setting("_headless_space_fired", true)
1984-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804222953 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804225210 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
569-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3546-	"togglehud":    KEY_H,
3547-	"flightcheck":  KEY_G,
3548-	"restart":      KEY_R,
3549:	"j":            KEY_J,
3550-	"pause":        KEY_ESCAPE,
3551-}
3552-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
837-	return null
838-
839-
840:func _deploy_canopy() -> void:
841-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
842-	if _canopy_deployed or not _canopy_instance:
843-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1372-	if current_time >= event["time"]:
1373-		match event["action"]:
1374-			"deploy":
1375:				_deploy_canopy()
1376-			"cutaway":
1377-				_do_cutaway()
1378-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1641-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1642-			else:
1643-				print("[STEER_TELEM] /root/Main does not exist")
1644:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1645-		# ----- Direct key checks (bypass Input Map) -----
1646-		# Deploy (SPACE)
1647-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1648:		_deploy_canopy()
1649-	# Turn left (Q)
1650-	if Input.is_key_pressed(KEY_Q):
1651-		_turn_input = -1.0
--
1676-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1677-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1678-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1679:			_deploy_canopy()
1680-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1681-	# Turn left (Q)
1682-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
562-	# --- end auto-start ---
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
--
1978-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1979-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1980-			#  by _0283.txt showing plane position update before stall)
1981:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1982-				if not ProjectSettings.has_setting("_headless_space_fired"):
1983-					ProjectSettings.set_setting("_headless_space_fired", true)
1984-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804225210 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804225310 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
569-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3547-	"togglehud":    KEY_H,
3548-	"flightcheck":  KEY_G,
3549-	"restart":      KEY_R,
3550:	"j":            KEY_J,
3551-	"pause":        KEY_ESCAPE,
3552-}
3553-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
837-	return null
838-
839-
840:func _deploy_canopy() -> void:
841-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
842-	if _canopy_deployed or not _canopy_instance:
843-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1372-	if current_time >= event["time"]:
1373-		match event["action"]:
1374-			"deploy":
1375:				_deploy_canopy()
1376-			"cutaway":
1377-				_do_cutaway()
1378-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1641-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1642-			else:
1643-				print("[STEER_TELEM] /root/Main does not exist")
1644:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1645-		# ----- Direct key checks (bypass Input Map) -----
1646-		# Deploy (SPACE)
1647-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1648:		_deploy_canopy()
1649-	# Turn left (Q)
1650-	if Input.is_key_pressed(KEY_Q):
1651-		_turn_input = -1.0
--
1676-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1677-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1678-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1679:			_deploy_canopy()
1680-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1681-	# Turn left (Q)
1682-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
562-	# --- end auto-start ---
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
--
1978-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1979-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1980-			#  by _0283.txt showing plane position update before stall)
1981:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1982-				if not ProjectSettings.has_setting("_headless_space_fired"):
1983-					ProjectSettings.set_setting("_headless_space_fired", true)
1984-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804225310 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804225412 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
569-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3547-	"togglehud":    KEY_H,
3548-	"flightcheck":  KEY_G,
3549-	"restart":      KEY_R,
3550:	"j":            KEY_J,
3551-	"pause":        KEY_ESCAPE,
3552-}
3553-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
837-	return null
838-
839-
840:func _deploy_canopy() -> void:
841-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
842-	if _canopy_deployed or not _canopy_instance:
843-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1372-	if current_time >= event["time"]:
1373-		match event["action"]:
1374-			"deploy":
1375:				_deploy_canopy()
1376-			"cutaway":
1377-				_do_cutaway()
1378-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1641-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1642-			else:
1643-				print("[STEER_TELEM] /root/Main does not exist")
1644:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1645-		# ----- Direct key checks (bypass Input Map) -----
1646-		# Deploy (SPACE)
1647-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1648:		_deploy_canopy()
1649-	# Turn left (Q)
1650-	if Input.is_key_pressed(KEY_Q):
1651-		_turn_input = -1.0
--
1676-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1677-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1678-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1679:			_deploy_canopy()
1680-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1681-	# Turn left (Q)
1682-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
562-	# --- end auto-start ---
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
--
1978-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1979-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1980-			#  by _0283.txt showing plane position update before stall)
1981:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1982-				if not ProjectSettings.has_setting("_headless_space_fired"):
1983-					ProjectSettings.set_setting("_headless_space_fired", true)
1984-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804225412 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804230453 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
569-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3547-	"togglehud":    KEY_H,
3548-	"flightcheck":  KEY_G,
3549-	"restart":      KEY_R,
3550:	"j":            KEY_J,
3551-	"pause":        KEY_ESCAPE,
3552-}
3553-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
837-	return null
838-
839-
840:func _deploy_canopy() -> void:
841-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
842-	if _canopy_deployed or not _canopy_instance:
843-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1372-	if current_time >= event["time"]:
1373-		match event["action"]:
1374-			"deploy":
1375:				_deploy_canopy()
1376-			"cutaway":
1377-				_do_cutaway()
1378-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1641-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1642-			else:
1643-				print("[STEER_TELEM] /root/Main does not exist")
1644:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1645-		# ----- Direct key checks (bypass Input Map) -----
1646-		# Deploy (SPACE)
1647-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1648:		_deploy_canopy()
1649-	# Turn left (Q)
1650-	if Input.is_key_pressed(KEY_Q):
1651-		_turn_input = -1.0
--
1676-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1677-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1678-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1679:			_deploy_canopy()
1680-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1681-	# Turn left (Q)
1682-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
562-	# --- end auto-start ---
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
--
1978-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1979-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1980-			#  by _0283.txt showing plane position update before stall)
1981:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1982-				if not ProjectSettings.has_setting("_headless_space_fired"):
1983-					ProjectSettings.set_setting("_headless_space_fired", true)
1984-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804230453 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804230802 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
569-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3546-	"togglehud":    KEY_H,
3547-	"flightcheck":  KEY_G,
3548-	"restart":      KEY_R,
3549:	"j":            KEY_J,
3550-	"pause":        KEY_ESCAPE,
3551-}
3552-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
837-	return null
838-
839-
840:func _deploy_canopy() -> void:
841-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
842-	if _canopy_deployed or not _canopy_instance:
843-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1372-	if current_time >= event["time"]:
1373-		match event["action"]:
1374-			"deploy":
1375:				_deploy_canopy()
1376-			"cutaway":
1377-				_do_cutaway()
1378-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1641-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1642-			else:
1643-				print("[STEER_TELEM] /root/Main does not exist")
1644:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1645-		# ----- Direct key checks (bypass Input Map) -----
1646-		# Deploy (SPACE)
1647-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1648:		_deploy_canopy()
1649-	# Turn left (Q)
1650-	if Input.is_key_pressed(KEY_Q):
1651-		_turn_input = -1.0
--
1676-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1677-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1678-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1679:			_deploy_canopy()
1680-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1681-	# Turn left (Q)
1682-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
562-	# --- end auto-start ---
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
--
1978-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1979-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1980-			#  by _0283.txt showing plane position update before stall)
1981:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1982-				if not ProjectSettings.has_setting("_headless_space_fired"):
1983-					ProjectSettings.set_setting("_headless_space_fired", true)
1984-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804230802 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804230949 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
573-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
574-	# Headless auto‑start: simulate SPACE press
575-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
576:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
577-		# Input.action_release("deploy") removed — flag-based now
578-		print("[VERBATIM] Headless auto‑start triggered.")
579-	_check_arm_pose_safe()
--
1578-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1579-		var _exit_pressed = (
1580-			Input.is_action_just_pressed("deploy")
1581:			or Input.is_key_pressed(KEY_J)
1582:			or _headless_auto_jump
1583-		)
1584:		_headless_auto_jump = false  # consume the flag
1585-		if _exit_pressed:
1586-			print("[DIAG] _poll_controls: exit aircraft triggered")
1587-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3564-	"togglehud":    KEY_H,
3565-	"flightcheck":  KEY_G,
3566-	"restart":      KEY_R,
3567:	"j":            KEY_J,
3568-	"pause":        KEY_ESCAPE,
3569-}
3570-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
849-	return null
850-
851-
852:func _deploy_canopy() -> void:
853-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
854-	if _canopy_deployed or not _canopy_instance:
855-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1384-	if current_time >= event["time"]:
1385-		match event["action"]:
1386-			"deploy":
1387:				_deploy_canopy()
1388-			"cutaway":
1389-				_do_cutaway()
1390-			"reserve":
--
1615-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1616-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1617-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1618:		_deploy_canopy()
1619-
1620-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1621-		var turn_input := 0.0
--
1653-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1654-			else:
1655-				print("[STEER_TELEM] /root/Main does not exist")
1656:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1657-		# ----- Direct key checks (bypass Input Map) -----
1658-		# Deploy (SPACE)
1659-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1660:		_deploy_canopy()
1661-	# Turn left (Q)
1662-	if Input.is_key_pressed(KEY_Q):
1663-		_turn_input = -1.0
--
1688-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1689-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1690-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1691:			_deploy_canopy()
1692-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1693-	# Turn left (Q)
1694-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
572-	# --- end auto-start ---
573-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
574-	# Headless auto‑start: simulate SPACE press
575:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
576-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
577-		# Input.action_release("deploy") removed — flag-based now
578-		print("[VERBATIM] Headless auto‑start triggered.")
--
1990-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1991-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1992-			#  by _0283.txt showing plane position update before stall)
1993:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1994-				if not ProjectSettings.has_setting("_headless_space_fired"):
1995-					ProjectSettings.set_setting("_headless_space_fired", true)
1996-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804230949 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804232641 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
569-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3546-	"togglehud":    KEY_H,
3547-	"flightcheck":  KEY_G,
3548-	"restart":      KEY_R,
3549:	"j":            KEY_J,
3550-	"pause":        KEY_ESCAPE,
3551-}
3552-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
837-	return null
838-
839-
840:func _deploy_canopy() -> void:
841-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
842-	if _canopy_deployed or not _canopy_instance:
843-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1372-	if current_time >= event["time"]:
1373-		match event["action"]:
1374-			"deploy":
1375:				_deploy_canopy()
1376-			"cutaway":
1377-				_do_cutaway()
1378-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1641-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1642-			else:
1643-				print("[STEER_TELEM] /root/Main does not exist")
1644:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1645-		# ----- Direct key checks (bypass Input Map) -----
1646-		# Deploy (SPACE)
1647-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1648:		_deploy_canopy()
1649-	# Turn left (Q)
1650-	if Input.is_key_pressed(KEY_Q):
1651-		_turn_input = -1.0
--
1676-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1677-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1678-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1679:			_deploy_canopy()
1680-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1681-	# Turn left (Q)
1682-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
562-	# --- end auto-start ---
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
--
1978-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1979-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1980-			#  by _0283.txt showing plane position update before stall)
1981:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1982-				if not ProjectSettings.has_setting("_headless_space_fired"):
1983-					ProjectSettings.set_setting("_headless_space_fired", true)
1984-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804232641 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804232736 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
561-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
562-	# Headless auto‑start: simulate SPACE press
563-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
564:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
565-		# Input.action_release("deploy") removed — flag-based now
566-		print("[VERBATIM] Headless auto‑start triggered.")
567-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3546-	"togglehud":    KEY_H,
3547-	"flightcheck":  KEY_G,
3548-	"restart":      KEY_R,
3549:	"j":            KEY_J,
3550-	"pause":        KEY_ESCAPE,
3551-}
3552-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
837-	return null
838-
839-
840:func _deploy_canopy() -> void:
841-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
842-	if _canopy_deployed or not _canopy_instance:
843-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1372-	if current_time >= event["time"]:
1373-		match event["action"]:
1374-			"deploy":
1375:				_deploy_canopy()
1376-			"cutaway":
1377-				_do_cutaway()
1378-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1641-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1642-			else:
1643-				print("[STEER_TELEM] /root/Main does not exist")
1644:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1645-		# ----- Direct key checks (bypass Input Map) -----
1646-		# Deploy (SPACE)
1647-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1648:		_deploy_canopy()
1649-	# Turn left (Q)
1650-	if Input.is_key_pressed(KEY_Q):
1651-		_turn_input = -1.0
--
1676-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1677-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1678-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1679:			_deploy_canopy()
1680-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1681-	# Turn left (Q)
1682-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
560-	# --- end auto-start ---
561-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
562-	# Headless auto‑start: simulate SPACE press
563:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
564-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
565-		# Input.action_release("deploy") removed — flag-based now
566-		print("[VERBATIM] Headless auto‑start triggered.")
--
1978-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1979-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1980-			#  by _0283.txt showing plane position update before stall)
1981:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1982-				if not ProjectSettings.has_setting("_headless_space_fired"):
1983-					ProjectSettings.set_setting("_headless_space_fired", true)
1984-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804232736 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804233430 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
569-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3546-	"togglehud":    KEY_H,
3547-	"flightcheck":  KEY_G,
3548-	"restart":      KEY_R,
3549:	"j":            KEY_J,
3550-	"pause":        KEY_ESCAPE,
3551-}
3552-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
837-	return null
838-
839-
840:func _deploy_canopy() -> void:
841-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
842-	if _canopy_deployed or not _canopy_instance:
843-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1372-	if current_time >= event["time"]:
1373-		match event["action"]:
1374-			"deploy":
1375:				_deploy_canopy()
1376-			"cutaway":
1377-				_do_cutaway()
1378-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1641-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1642-			else:
1643-				print("[STEER_TELEM] /root/Main does not exist")
1644:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1645-		# ----- Direct key checks (bypass Input Map) -----
1646-		# Deploy (SPACE)
1647-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1648:		_deploy_canopy()
1649-	# Turn left (Q)
1650-	if Input.is_key_pressed(KEY_Q):
1651-		_turn_input = -1.0
--
1676-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1677-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1678-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1679:			_deploy_canopy()
1680-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1681-	# Turn left (Q)
1682-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
562-	# --- end auto-start ---
563-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
564-	# Headless auto‑start: simulate SPACE press
565:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
566-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
567-		# Input.action_release("deploy") removed — flag-based now
568-		print("[VERBATIM] Headless auto‑start triggered.")
--
1978-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1979-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1980-			#  by _0283.txt showing plane position update before stall)
1981:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1982-				if not ProjectSettings.has_setting("_headless_space_fired"):
1983-					ProjectSettings.set_setting("_headless_space_fired", true)
1984-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804233430 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804233720 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
567-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
568-	# Headless auto‑start: simulate SPACE press
569-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
570:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
571-		# Input.action_release("deploy") removed — flag-based now
572-		print("[VERBATIM] Headless auto‑start triggered.")
573-	_check_arm_pose_safe()
--
1570-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1571-		var _exit_pressed = (
1572-			Input.is_action_just_pressed("deploy")
1573:			or Input.is_key_pressed(KEY_J)
1574:			or _headless_auto_jump
1575-		)
1576:		_headless_auto_jump = false  # consume the flag
1577-		if _exit_pressed:
1578-			print("[DIAG] _poll_controls: exit aircraft triggered")
1579-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3550-	"togglehud":    KEY_H,
3551-	"flightcheck":  KEY_G,
3552-	"restart":      KEY_R,
3553:	"j":            KEY_J,
3554-	"pause":        KEY_ESCAPE,
3555-}
3556-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
841-	return null
842-
843-
844:func _deploy_canopy() -> void:
845-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
846-	if _canopy_deployed or not _canopy_instance:
847-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1376-	if current_time >= event["time"]:
1377-		match event["action"]:
1378-			"deploy":
1379:				_deploy_canopy()
1380-			"cutaway":
1381-				_do_cutaway()
1382-			"reserve":
--
1607-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1608-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1609-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1610:		_deploy_canopy()
1611-
1612-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1613-		var turn_input := 0.0
--
1645-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1646-			else:
1647-				print("[STEER_TELEM] /root/Main does not exist")
1648:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1649-		# ----- Direct key checks (bypass Input Map) -----
1650-		# Deploy (SPACE)
1651-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1652:		_deploy_canopy()
1653-	# Turn left (Q)
1654-	if Input.is_key_pressed(KEY_Q):
1655-		_turn_input = -1.0
--
1680-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1681-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1682-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1683:			_deploy_canopy()
1684-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1685-	# Turn left (Q)
1686-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
566-	# --- end auto-start ---
567-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
568-	# Headless auto‑start: simulate SPACE press
569:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
570-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
571-		# Input.action_release("deploy") removed — flag-based now
572-		print("[VERBATIM] Headless auto‑start triggered.")
--
1982-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1983-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1984-			#  by _0283.txt showing plane position update before stall)
1985:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1986-				if not ProjectSettings.has_setting("_headless_space_fired"):
1987-					ProjectSettings.set_setting("_headless_space_fired", true)
1988-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804233720 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804234834 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
567-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
568-	# Headless auto‑start: simulate SPACE press
569-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
570:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
571-		# Input.action_release("deploy") removed — flag-based now
572-		print("[VERBATIM] Headless auto‑start triggered.")
573-	_check_arm_pose_safe()
--
1581-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1582-		var _exit_pressed = (
1583-			Input.is_action_just_pressed("deploy")
1584:			or Input.is_key_pressed(KEY_J)
1585:			or _headless_auto_jump
1586-		)
1587:		_headless_auto_jump = false  # consume the flag
1588-		if _exit_pressed:
1589-			print("[DIAG] _poll_controls: exit aircraft triggered")
1590-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3561-	"togglehud":    KEY_H,
3562-	"flightcheck":  KEY_G,
3563-	"restart":      KEY_R,
3564:	"j":            KEY_J,
3565-	"pause":        KEY_ESCAPE,
3566-}
3567-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
852-	return null
853-
854-
855:func _deploy_canopy() -> void:
856-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
857-	if _canopy_deployed or not _canopy_instance:
858-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1387-	if current_time >= event["time"]:
1388-		match event["action"]:
1389-			"deploy":
1390:				_deploy_canopy()
1391-			"cutaway":
1392-				_do_cutaway()
1393-			"reserve":
--
1618-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1619-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1620-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1621:		_deploy_canopy()
1622-
1623-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1624-		var turn_input := 0.0
--
1656-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1657-			else:
1658-				print("[STEER_TELEM] /root/Main does not exist")
1659:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1660-		# ----- Direct key checks (bypass Input Map) -----
1661-		# Deploy (SPACE)
1662-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1663:		_deploy_canopy()
1664-	# Turn left (Q)
1665-	if Input.is_key_pressed(KEY_Q):
1666-		_turn_input = -1.0
--
1691-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1692-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1693-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1694:			_deploy_canopy()
1695-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1696-	# Turn left (Q)
1697-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
566-	# --- end auto-start ---
567-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
568-	# Headless auto‑start: simulate SPACE press
569:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
570-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
571-		# Input.action_release("deploy") removed — flag-based now
572-		print("[VERBATIM] Headless auto‑start triggered.")
--
1993-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1994-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1995-			#  by _0283.txt showing plane position update before stall)
1996:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1997-				if not ProjectSettings.has_setting("_headless_space_fired"):
1998-					ProjectSettings.set_setting("_headless_space_fired", true)
1999-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804234834 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260804235450 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
567-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
568-	# Headless auto‑start: simulate SPACE press
569-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
570:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
571-		# Input.action_release("deploy") removed — flag-based now
572-		print("[VERBATIM] Headless auto‑start triggered.")
573-	_check_arm_pose_safe()
--
1570-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1571-		var _exit_pressed = (
1572-			Input.is_action_just_pressed("deploy")
1573:			or Input.is_key_pressed(KEY_J)
1574:			or _headless_auto_jump
1575-		)
1576:		_headless_auto_jump = false  # consume the flag
1577-		if _exit_pressed:
1578-			print("[DIAG] _poll_controls: exit aircraft triggered")
1579-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3550-	"togglehud":    KEY_H,
3551-	"flightcheck":  KEY_G,
3552-	"restart":      KEY_R,
3553:	"j":            KEY_J,
3554-	"pause":        KEY_ESCAPE,
3555-}
3556-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
841-	return null
842-
843-
844:func _deploy_canopy() -> void:
845-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
846-	if _canopy_deployed or not _canopy_instance:
847-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1376-	if current_time >= event["time"]:
1377-		match event["action"]:
1378-			"deploy":
1379:				_deploy_canopy()
1380-			"cutaway":
1381-				_do_cutaway()
1382-			"reserve":
--
1607-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1608-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1609-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1610:		_deploy_canopy()
1611-
1612-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1613-		var turn_input := 0.0
--
1645-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1646-			else:
1647-				print("[STEER_TELEM] /root/Main does not exist")
1648:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1649-		# ----- Direct key checks (bypass Input Map) -----
1650-		# Deploy (SPACE)
1651-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1652:		_deploy_canopy()
1653-	# Turn left (Q)
1654-	if Input.is_key_pressed(KEY_Q):
1655-		_turn_input = -1.0
--
1680-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1681-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1682-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1683:			_deploy_canopy()
1684-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1685-	# Turn left (Q)
1686-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
566-	# --- end auto-start ---
567-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
568-	# Headless auto‑start: simulate SPACE press
569:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
570-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
571-		# Input.action_release("deploy") removed — flag-based now
572-		print("[VERBATIM] Headless auto‑start triggered.")
--
1982-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1983-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1984-			#  by _0283.txt showing plane position update before stall)
1985:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1986-				if not ProjectSettings.has_setting("_headless_space_fired"):
1987-					ProjectSettings.set_setting("_headless_space_fired", true)
1988-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260804235450 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805002352 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
567-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
568-	# Headless auto‑start: simulate SPACE press
569-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
570:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
571-		# Input.action_release("deploy") removed — flag-based now
572-		print("[VERBATIM] Headless auto‑start triggered.")
573-	_check_arm_pose_safe()
--
1582-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1583-		var _exit_pressed = (
1584-			Input.is_action_just_pressed("deploy")
1585:			or Input.is_key_pressed(KEY_J)
1586:			or _headless_auto_jump
1587-		)
1588:		_headless_auto_jump = false  # consume the flag
1589-		if _exit_pressed:
1590-			print("[DIAG] _poll_controls: exit aircraft triggered")
1591-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3562-	"togglehud":    KEY_H,
3563-	"flightcheck":  KEY_G,
3564-	"restart":      KEY_R,
3565:	"j":            KEY_J,
3566-	"pause":        KEY_ESCAPE,
3567-}
3568-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
853-	return null
854-
855-
856:func _deploy_canopy() -> void:
857-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
858-	if _canopy_deployed or not _canopy_instance:
859-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1388-	if current_time >= event["time"]:
1389-		match event["action"]:
1390-			"deploy":
1391:				_deploy_canopy()
1392-			"cutaway":
1393-				_do_cutaway()
1394-			"reserve":
--
1619-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1620-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1621-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1622:		_deploy_canopy()
1623-
1624-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1625-		var turn_input := 0.0
--
1657-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1658-			else:
1659-				print("[STEER_TELEM] /root/Main does not exist")
1660:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1661-		# ----- Direct key checks (bypass Input Map) -----
1662-		# Deploy (SPACE)
1663-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1664:		_deploy_canopy()
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):
1667-		_turn_input = -1.0
--
1692-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1693-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1694-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1695:			_deploy_canopy()
1696-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1697-	# Turn left (Q)
1698-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
566-	# --- end auto-start ---
567-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
568-	# Headless auto‑start: simulate SPACE press
569:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
570-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
571-		# Input.action_release("deploy") removed — flag-based now
572-		print("[VERBATIM] Headless auto‑start triggered.")
--
1994-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1995-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1996-			#  by _0283.txt showing plane position update before stall)
1997:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1998-				if not ProjectSettings.has_setting("_headless_space_fired"):
1999-					ProjectSettings.set_setting("_headless_space_fired", true)
2000-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805002352 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805003821 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
567-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
568-	# Headless auto‑start: simulate SPACE press
569-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
570:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
571-		# Input.action_release("deploy") removed — flag-based now
572-		print("[VERBATIM] Headless auto‑start triggered.")
573-	_check_arm_pose_safe()
--
1577-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1578-		var _exit_pressed = (
1579-			Input.is_action_just_pressed("deploy")
1580:			or Input.is_key_pressed(KEY_J)
1581:			or _headless_auto_jump
1582-		)
1583:		_headless_auto_jump = false  # consume the flag
1584-		if _exit_pressed:
1585-			print("[DIAG] _poll_controls: exit aircraft triggered")
1586-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3557-	"togglehud":    KEY_H,
3558-	"flightcheck":  KEY_G,
3559-	"restart":      KEY_R,
3560:	"j":            KEY_J,
3561-	"pause":        KEY_ESCAPE,
3562-}
3563-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
848-	return null
849-
850-
851:func _deploy_canopy() -> void:
852-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
853-	if _canopy_deployed or not _canopy_instance:
854-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1383-	if current_time >= event["time"]:
1384-		match event["action"]:
1385-			"deploy":
1386:				_deploy_canopy()
1387-			"cutaway":
1388-				_do_cutaway()
1389-			"reserve":
--
1614-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1615-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1616-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1617:		_deploy_canopy()
1618-
1619-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1620-		var turn_input := 0.0
--
1652-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1653-			else:
1654-				print("[STEER_TELEM] /root/Main does not exist")
1655:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1656-		# ----- Direct key checks (bypass Input Map) -----
1657-		# Deploy (SPACE)
1658-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1659:		_deploy_canopy()
1660-	# Turn left (Q)
1661-	if Input.is_key_pressed(KEY_Q):
1662-		_turn_input = -1.0
--
1687-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1688-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1689-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1690:			_deploy_canopy()
1691-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1692-	# Turn left (Q)
1693-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
566-	# --- end auto-start ---
567-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
568-	# Headless auto‑start: simulate SPACE press
569:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
570-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
571-		# Input.action_release("deploy") removed — flag-based now
572-		print("[VERBATIM] Headless auto‑start triggered.")
--
1989-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1990-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1991-			#  by _0283.txt showing plane position update before stall)
1992:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1993-				if not ProjectSettings.has_setting("_headless_space_fired"):
1994-					ProjectSettings.set_setting("_headless_space_fired", true)
1995-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805003821 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805005703 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
567-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
568-	# Headless auto‑start: simulate SPACE press
569-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
570:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
571-		# Input.action_release("deploy") removed — flag-based now
572-		print("[VERBATIM] Headless auto‑start triggered.")
573-	_check_arm_pose_safe()
--
1565-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1566-		var _exit_pressed = (
1567-			Input.is_action_just_pressed("deploy")
1568:			or Input.is_key_pressed(KEY_J)
1569:			or _headless_auto_jump
1570-		)
1571:		_headless_auto_jump = false  # consume the flag
1572-		if _exit_pressed:
1573-			print("[DIAG] _poll_controls: exit aircraft triggered")
1574-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3545-	"togglehud":    KEY_H,
3546-	"flightcheck":  KEY_G,
3547-	"restart":      KEY_R,
3548:	"j":            KEY_J,
3549-	"pause":        KEY_ESCAPE,
3550-}
3551-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
836-	return null
837-
838-
839:func _deploy_canopy() -> void:
840-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
841-	if _canopy_deployed or not _canopy_instance:
842-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1371-	if current_time >= event["time"]:
1372-		match event["action"]:
1373-			"deploy":
1374:				_deploy_canopy()
1375-			"cutaway":
1376-				_do_cutaway()
1377-			"reserve":
--
1602-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1603-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1604-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1605:		_deploy_canopy()
1606-
1607-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1608-		var turn_input := 0.0
--
1640-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1641-			else:
1642-				print("[STEER_TELEM] /root/Main does not exist")
1643:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1644-		# ----- Direct key checks (bypass Input Map) -----
1645-		# Deploy (SPACE)
1646-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1647:		_deploy_canopy()
1648-	# Turn left (Q)
1649-	if Input.is_key_pressed(KEY_Q):
1650-		_turn_input = -1.0
--
1675-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1676-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1677-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1678:			_deploy_canopy()
1679-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1680-	# Turn left (Q)
1681-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
566-	# --- end auto-start ---
567-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
568-	# Headless auto‑start: simulate SPACE press
569:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
570-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
571-		# Input.action_release("deploy") removed — flag-based now
572-		print("[VERBATIM] Headless auto‑start triggered.")
--
1977-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1978-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1979-			#  by _0283.txt showing plane position update before stall)
1980:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1981-				if not ProjectSettings.has_setting("_headless_space_fired"):
1982-					ProjectSettings.set_setting("_headless_space_fired", true)
1983-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805005703 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805014359 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
568-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
569-	# Headless auto‑start: simulate SPACE press
570-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
571:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
572-		# Input.action_release("deploy") removed — flag-based now
573-		print("[VERBATIM] Headless auto‑start triggered.")
574-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3546-	"togglehud":    KEY_H,
3547-	"flightcheck":  KEY_G,
3548-	"restart":      KEY_R,
3549:	"j":            KEY_J,
3550-	"pause":        KEY_ESCAPE,
3551-}
3552-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
837-	return null
838-
839-
840:func _deploy_canopy() -> void:
841-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
842-	if _canopy_deployed or not _canopy_instance:
843-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1372-	if current_time >= event["time"]:
1373-		match event["action"]:
1374-			"deploy":
1375:				_deploy_canopy()
1376-			"cutaway":
1377-				_do_cutaway()
1378-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1641-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1642-			else:
1643-				print("[STEER_TELEM] /root/Main does not exist")
1644:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1645-		# ----- Direct key checks (bypass Input Map) -----
1646-		# Deploy (SPACE)
1647-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1648:		_deploy_canopy()
1649-	# Turn left (Q)
1650-	if Input.is_key_pressed(KEY_Q):
1651-		_turn_input = -1.0
--
1676-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1677-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1678-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1679:			_deploy_canopy()
1680-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1681-	# Turn left (Q)
1682-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
567-	# --- end auto-start ---
568-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
569-	# Headless auto‑start: simulate SPACE press
570:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
571-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
572-		# Input.action_release("deploy") removed — flag-based now
573-		print("[VERBATIM] Headless auto‑start triggered.")
--
1978-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1979-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1980-			#  by _0283.txt showing plane position update before stall)
1981:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1982-				if not ProjectSettings.has_setting("_headless_space_fired"):
1983-					ProjectSettings.set_setting("_headless_space_fired", true)
1984-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805014359 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805014717 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
568-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
569-	# Headless auto‑start: simulate SPACE press
570-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
571:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
572-		# Input.action_release("deploy") removed — flag-based now
573-		print("[VERBATIM] Headless auto‑start triggered.")
574-	_check_arm_pose_safe()
--
1566-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1567-		var _exit_pressed = (
1568-			Input.is_action_just_pressed("deploy")
1569:			or Input.is_key_pressed(KEY_J)
1570:			or _headless_auto_jump
1571-		)
1572:		_headless_auto_jump = false  # consume the flag
1573-		if _exit_pressed:
1574-			print("[DIAG] _poll_controls: exit aircraft triggered")
1575-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3546-	"togglehud":    KEY_H,
3547-	"flightcheck":  KEY_G,
3548-	"restart":      KEY_R,
3549:	"j":            KEY_J,
3550-	"pause":        KEY_ESCAPE,
3551-}
3552-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
837-	return null
838-
839-
840:func _deploy_canopy() -> void:
841-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
842-	if _canopy_deployed or not _canopy_instance:
843-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1372-	if current_time >= event["time"]:
1373-		match event["action"]:
1374-			"deploy":
1375:				_deploy_canopy()
1376-			"cutaway":
1377-				_do_cutaway()
1378-			"reserve":
--
1603-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1604-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1605-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1606:		_deploy_canopy()
1607-
1608-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1609-		var turn_input := 0.0
--
1641-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1642-			else:
1643-				print("[STEER_TELEM] /root/Main does not exist")
1644:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1645-		# ----- Direct key checks (bypass Input Map) -----
1646-		# Deploy (SPACE)
1647-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1648:		_deploy_canopy()
1649-	# Turn left (Q)
1650-	if Input.is_key_pressed(KEY_Q):
1651-		_turn_input = -1.0
--
1676-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1677-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1678-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1679:			_deploy_canopy()
1680-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1681-	# Turn left (Q)
1682-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
567-	# --- end auto-start ---
568-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
569-	# Headless auto‑start: simulate SPACE press
570:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
571-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
572-		# Input.action_release("deploy") removed — flag-based now
573-		print("[VERBATIM] Headless auto‑start triggered.")
--
1978-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1979-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1980-			#  by _0283.txt showing plane position update before stall)
1981:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1982-				if not ProjectSettings.has_setting("_headless_space_fired"):
1983-					ProjectSettings.set_setting("_headless_space_fired", true)
1984-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805014717 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805104745 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
545-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
546-	# Headless auto‑start: simulate SPACE press
547-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
548:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
549-		# Input.action_release("deploy") removed — flag-based now
550-		print("[VERBATIM] Headless auto‑start triggered.")
551-	_check_arm_pose_safe()
--
1548-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1549-		var _exit_pressed = (
1550-			Input.is_action_just_pressed("deploy")
1551:			or Input.is_key_pressed(KEY_J)
1552:			or _headless_auto_jump
1553-		)
1554:		_headless_auto_jump = false  # consume the flag
1555-		if _exit_pressed:
1556-			print("[DIAG] _poll_controls: exit aircraft triggered")
1557-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3528-	"togglehud":    KEY_H,
3529-	"flightcheck":  KEY_G,
3530-	"restart":      KEY_R,
3531:	"j":            KEY_J,
3532-	"pause":        KEY_ESCAPE,
3533-}
3534-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
819-	return null
820-
821-
822:func _deploy_canopy() -> void:
823-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
824-	if _canopy_deployed or not _canopy_instance:
825-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1354-	if current_time >= event["time"]:
1355-		match event["action"]:
1356-			"deploy":
1357:				_deploy_canopy()
1358-			"cutaway":
1359-				_do_cutaway()
1360-			"reserve":
--
1585-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1586-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1587-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1588:		_deploy_canopy()
1589-
1590-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1591-		var turn_input := 0.0
--
1623-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1624-			else:
1625-				print("[STEER_TELEM] /root/Main does not exist")
1626:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1627-		# ----- Direct key checks (bypass Input Map) -----
1628-		# Deploy (SPACE)
1629-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1630:		_deploy_canopy()
1631-	# Turn left (Q)
1632-	if Input.is_key_pressed(KEY_Q):
1633-		_turn_input = -1.0
--
1658-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1659-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1660-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1661:			_deploy_canopy()
1662-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1663-	# Turn left (Q)
1664-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
544-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
545-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
546-	# Headless auto‑start: simulate SPACE press
547:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
548-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
549-		# Input.action_release("deploy") removed — flag-based now
550-		print("[VERBATIM] Headless auto‑start triggered.")
--
1960-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1961-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1962-			#  by _0283.txt showing plane position update before stall)
1963:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1964-				if not ProjectSettings.has_setting("_headless_space_fired"):
1965-					ProjectSettings.set_setting("_headless_space_fired", true)
1966-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805104745 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805105542 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
545-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
546-	# Headless auto‑start: simulate SPACE press
547-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
548:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
549-		# Input.action_release("deploy") removed — flag-based now
550-		print("[VERBATIM] Headless auto‑start triggered.")
551-	_check_arm_pose_safe()
--
1549-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1550-		var _exit_pressed = (
1551-			Input.is_action_just_pressed("deploy")
1552:			or Input.is_key_pressed(KEY_J)
1553:			or _headless_auto_jump
1554-		)
1555:		_headless_auto_jump = false  # consume the flag
1556-		if _exit_pressed:
1557-			print("[DIAG] _poll_controls: exit aircraft triggered")
1558-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3539-	"togglehud":    KEY_H,
3540-	"flightcheck":  KEY_G,
3541-	"restart":      KEY_R,
3542:	"j":            KEY_J,
3543-	"pause":        KEY_ESCAPE,
3544-}
3545-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
820-	return null
821-
822-
823:func _deploy_canopy() -> void:
824-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
825-	if _canopy_deployed or not _canopy_instance:
826-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1355-	if current_time >= event["time"]:
1356-		match event["action"]:
1357-			"deploy":
1358:				_deploy_canopy()
1359-			"cutaway":
1360-				_do_cutaway()
1361-			"reserve":
--
1586-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1587-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1588-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1589:		_deploy_canopy()
1590-
1591-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1592-		var turn_input := 0.0
--
1624-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1625-			else:
1626-				print("[STEER_TELEM] /root/Main does not exist")
1627:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1628-		# ----- Direct key checks (bypass Input Map) -----
1629-		# Deploy (SPACE)
1630-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1631:		_deploy_canopy()
1632-	# Turn left (Q)
1633-	if Input.is_key_pressed(KEY_Q):
1634-		_turn_input = -1.0
--
1659-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1660-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1661-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1662:			_deploy_canopy()
1663-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1664-	# Turn left (Q)
1665-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
544-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
545-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
546-	# Headless auto‑start: simulate SPACE press
547:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
548-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
549-		# Input.action_release("deploy") removed — flag-based now
550-		print("[VERBATIM] Headless auto‑start triggered.")
--
1961-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1962-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1963-			#  by _0283.txt showing plane position update before stall)
1964:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1965-				if not ProjectSettings.has_setting("_headless_space_fired"):
1966-					ProjectSettings.set_setting("_headless_space_fired", true)
1967-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805105542 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805110331 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
545-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
546-	# Headless auto‑start: simulate SPACE press
547-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
548:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
549-		# Input.action_release("deploy") removed — flag-based now
550-		print("[VERBATIM] Headless auto‑start triggered.")
551-	_check_arm_pose_safe()
--
1549-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1550-		var _exit_pressed = (
1551-			Input.is_action_just_pressed("deploy")
1552:			or Input.is_key_pressed(KEY_J)
1553:			or _headless_auto_jump
1554-		)
1555:		_headless_auto_jump = false  # consume the flag
1556-		if _exit_pressed:
1557-			print("[DIAG] _poll_controls: exit aircraft triggered")
1558-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3541-	"togglehud":    KEY_H,
3542-	"flightcheck":  KEY_G,
3543-	"restart":      KEY_R,
3544:	"j":            KEY_J,
3545-	"pause":        KEY_ESCAPE,
3546-}
3547-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
820-	return null
821-
822-
823:func _deploy_canopy() -> void:
824-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
825-	if _canopy_deployed or not _canopy_instance:
826-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1355-	if current_time >= event["time"]:
1356-		match event["action"]:
1357-			"deploy":
1358:				_deploy_canopy()
1359-			"cutaway":
1360-				_do_cutaway()
1361-			"reserve":
--
1586-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1587-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1588-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1589:		_deploy_canopy()
1590-
1591-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1592-		var turn_input := 0.0
--
1624-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1625-			else:
1626-				print("[STEER_TELEM] /root/Main does not exist")
1627:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1628-		# ----- Direct key checks (bypass Input Map) -----
1629-		# Deploy (SPACE)
1630-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1631:		_deploy_canopy()
1632-	# Turn left (Q)
1633-	if Input.is_key_pressed(KEY_Q):
1634-		_turn_input = -1.0
--
1659-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1660-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1661-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1662:			_deploy_canopy()
1663-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1664-	# Turn left (Q)
1665-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
544-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
545-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
546-	# Headless auto‑start: simulate SPACE press
547:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
548-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
549-		# Input.action_release("deploy") removed — flag-based now
550-		print("[VERBATIM] Headless auto‑start triggered.")
--
1961-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1962-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1963-			#  by _0283.txt showing plane position update before stall)
1964:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1965-				if not ProjectSettings.has_setting("_headless_space_fired"):
1966-					ProjectSettings.set_setting("_headless_space_fired", true)
1967-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805110331 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805112023 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3548-	"togglehud":    KEY_H,
3549-	"flightcheck":  KEY_G,
3550-	"restart":      KEY_R,
3551:	"j":            KEY_J,
3552-	"pause":        KEY_ESCAPE,
3553-}
3554-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1968-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1969-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1970-			#  by _0283.txt showing plane position update before stall)
1971:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1972-				if not ProjectSettings.has_setting("_headless_space_fired"):
1973-					ProjectSettings.set_setting("_headless_space_fired", true)
1974-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805112023 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805113335 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3548-	"togglehud":    KEY_H,
3549-	"flightcheck":  KEY_G,
3550-	"restart":      KEY_R,
3551:	"j":            KEY_J,
3552-	"pause":        KEY_ESCAPE,
3553-}
3554-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1968-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1969-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1970-			#  by _0283.txt showing plane position update before stall)
1971:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1972-				if not ProjectSettings.has_setting("_headless_space_fired"):
1973-					ProjectSettings.set_setting("_headless_space_fired", true)
1974-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805113335 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805113407 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3548-	"togglehud":    KEY_H,
3549-	"flightcheck":  KEY_G,
3550-	"restart":      KEY_R,
3551:	"j":            KEY_J,
3552-	"pause":        KEY_ESCAPE,
3553-}
3554-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1968-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1969-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1970-			#  by _0283.txt showing plane position update before stall)
1971:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1972-				if not ProjectSettings.has_setting("_headless_space_fired"):
1973-					ProjectSettings.set_setting("_headless_space_fired", true)
1974-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805113407 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805113659 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3548-	"togglehud":    KEY_H,
3549-	"flightcheck":  KEY_G,
3550-	"restart":      KEY_R,
3551:	"j":            KEY_J,
3552-	"pause":        KEY_ESCAPE,
3553-}
3554-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1968-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1969-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1970-			#  by _0283.txt showing plane position update before stall)
1971:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1972-				if not ProjectSettings.has_setting("_headless_space_fired"):
1973-					ProjectSettings.set_setting("_headless_space_fired", true)
1974-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805113659 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805115249 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3549-	"togglehud":    KEY_H,
3550-	"flightcheck":  KEY_G,
3551-	"restart":      KEY_R,
3552:	"j":            KEY_J,
3553-	"pause":        KEY_ESCAPE,
3554-}
3555-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805115249 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805160703 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3549-	"togglehud":    KEY_H,
3550-	"flightcheck":  KEY_G,
3551-	"restart":      KEY_R,
3552:	"j":            KEY_J,
3553-	"pause":        KEY_ESCAPE,
3554-}
3555-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805160703 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805161326 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3549-	"togglehud":    KEY_H,
3550-	"flightcheck":  KEY_G,
3551-	"restart":      KEY_R,
3552:	"j":            KEY_J,
3553-	"pause":        KEY_ESCAPE,
3554-}
3555-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805161326 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805161923 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3549-	"togglehud":    KEY_H,
3550-	"flightcheck":  KEY_G,
3551-	"restart":      KEY_R,
3552:	"j":            KEY_J,
3553-	"pause":        KEY_ESCAPE,
3554-}
3555-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805161923 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805163539 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3549-	"togglehud":    KEY_H,
3550-	"flightcheck":  KEY_G,
3551-	"restart":      KEY_R,
3552:	"j":            KEY_J,
3553-	"pause":        KEY_ESCAPE,
3554-}
3555-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805163539 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805164950 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3549-	"togglehud":    KEY_H,
3550-	"flightcheck":  KEY_G,
3551-	"restart":      KEY_R,
3552:	"j":            KEY_J,
3553-	"pause":        KEY_ESCAPE,
3554-}
3555-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805164950 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805174807 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3550-	"togglehud":    KEY_H,
3551-	"flightcheck":  KEY_G,
3552-	"restart":      KEY_R,
3553:	"j":            KEY_J,
3554-	"pause":        KEY_ESCAPE,
3555-}
3556-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805174807 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805175249 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3550-	"togglehud":    KEY_H,
3551-	"flightcheck":  KEY_G,
3552-	"restart":      KEY_R,
3553:	"j":            KEY_J,
3554-	"pause":        KEY_ESCAPE,
3555-}
3556-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805175249 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805180005 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3556-	"togglehud":    KEY_H,
3557-	"flightcheck":  KEY_G,
3558-	"restart":      KEY_R,
3559:	"j":            KEY_J,
3560-	"pause":        KEY_ESCAPE,
3561-}
3562-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805180005 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805180421 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3556-	"togglehud":    KEY_H,
3557-	"flightcheck":  KEY_G,
3558-	"restart":      KEY_R,
3559:	"j":            KEY_J,
3560-	"pause":        KEY_ESCAPE,
3561-}
3562-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805180421 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805180651 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3556-	"togglehud":    KEY_H,
3557-	"flightcheck":  KEY_G,
3558-	"restart":      KEY_R,
3559:	"j":            KEY_J,
3560-	"pause":        KEY_ESCAPE,
3561-}
3562-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805180651 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805181035 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3556-	"togglehud":    KEY_H,
3557-	"flightcheck":  KEY_G,
3558-	"restart":      KEY_R,
3559:	"j":            KEY_J,
3560-	"pause":        KEY_ESCAPE,
3561-}
3562-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805181035 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805182239 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3556-	"togglehud":    KEY_H,
3557-	"flightcheck":  KEY_G,
3558-	"restart":      KEY_R,
3559:	"j":            KEY_J,
3560-	"pause":        KEY_ESCAPE,
3561-}
3562-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805182239 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805182524 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3556-	"togglehud":    KEY_H,
3557-	"flightcheck":  KEY_G,
3558-	"restart":      KEY_R,
3559:	"j":            KEY_J,
3560-	"pause":        KEY_ESCAPE,
3561-}
3562-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805182524 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805182645 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3556-	"togglehud":    KEY_H,
3557-	"flightcheck":  KEY_G,
3558-	"restart":      KEY_R,
3559:	"j":            KEY_J,
3560-	"pause":        KEY_ESCAPE,
3561-}
3562-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805182645 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805183444 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3556-	"togglehud":    KEY_H,
3557-	"flightcheck":  KEY_G,
3558-	"restart":      KEY_R,
3559:	"j":            KEY_J,
3560-	"pause":        KEY_ESCAPE,
3561-}
3562-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805183444 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805183921 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3556-	"togglehud":    KEY_H,
3557-	"flightcheck":  KEY_G,
3558-	"restart":      KEY_R,
3559:	"j":            KEY_J,
3560-	"pause":        KEY_ESCAPE,
3561-}
3562-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805183921 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805184054 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3556-	"togglehud":    KEY_H,
3557-	"flightcheck":  KEY_G,
3558-	"restart":      KEY_R,
3559:	"j":            KEY_J,
3560-	"pause":        KEY_ESCAPE,
3561-}
3562-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805184054 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805185302 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3563-	"togglehud":    KEY_H,
3564-	"flightcheck":  KEY_G,
3565-	"restart":      KEY_R,
3566:	"j":            KEY_J,
3567-	"pause":        KEY_ESCAPE,
3568-}
3569-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805185302 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805195526 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3563-	"togglehud":    KEY_H,
3564-	"flightcheck":  KEY_G,
3565-	"restart":      KEY_R,
3566:	"j":            KEY_J,
3567-	"pause":        KEY_ESCAPE,
3568-}
3569-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805195526 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805195907 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3582-	"togglehud":    KEY_H,
3583-	"flightcheck":  KEY_G,
3584-	"restart":      KEY_R,
3585:	"j":            KEY_J,
3586-	"pause":        KEY_ESCAPE,
3587-}
3588-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805195907 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805201328 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3601-	"togglehud":    KEY_H,
3602-	"flightcheck":  KEY_G,
3603-	"restart":      KEY_R,
3604:	"j":            KEY_J,
3605-	"pause":        KEY_ESCAPE,
3606-}
3607-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805201328 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805213608 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1549-		# reliable across _physics_process/_process frame boundary).
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-	var _exit_pressed = (
1552:		Input.is_key_just_pressed(KEY_J)
1553:		or _headless_auto_jump
1554-	)
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3563-	"togglehud":    KEY_H,
3564-	"flightcheck":  KEY_G,
3565-	"restart":      KEY_R,
3566:	"j":            KEY_J,
3567-	"pause":        KEY_ESCAPE,
3568-}
3569-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed and _game_state == GameState.FREEFALL:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805213608 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805213855 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-	var _exit_pressed = (
1622:		Input.is_key_just_pressed(KEY_J)
1623:		or _headless_auto_jump
1624-	)
1625-		)
1626:		_headless_auto_jump = false  # consume the flag
1627-		if _exit_pressed:
1628-			print("[DIAG] _poll_controls: exit aircraft triggered")
1629-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3701-	"togglehud":    KEY_H,
3702-	"flightcheck":  KEY_G,
3703-	"restart":      KEY_R,
3704:	"j":            KEY_J,
3705-	"pause":        KEY_ESCAPE,
3706-}
3707-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1656-
1657-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1658-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1659:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1660-	# so it fires on the first FREEFALL _poll_controls() call.
1661-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1662-	# (general knowledge — not retrieved this session)
1663-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1664-		_headless_auto_deploy = false
1665-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1666:		_deploy_canopy()
1667-	if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed and _game_state == GameState.FREEFALL:
1668-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1669:		_deploy_canopy()
1670-
1671-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1672-		var turn_input := 0.0
--
1686-
1687-		_turn_input = turn_input
1688-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1689:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1690-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1691-# canopy could never accumulate a turn. Guard was commented out; call removed.
1692-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1705-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1706-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1707-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1708:			_deploy_canopy()
1709-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1710-	# Turn left (Q)
1711-	if Input.is_key_pressed(KEY_Q):
--
2126-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2127-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2128-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2129:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2130-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2131-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2132-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
2017-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2018-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2019-			#  by _0283.txt showing plane position update before stall)
2020:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2021-				if not ProjectSettings.has_setting("_headless_space_fired"):
2022-					ProjectSettings.set_setting("_headless_space_fired", true)
2023-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805213855 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805214005 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-	var _exit_pressed = (
1622:		Input.is_key_just_pressed(KEY_J)
1623:		or _headless_auto_jump
1624-	)
1625-		)
1626:		_headless_auto_jump = false  # consume the flag
1627-		if _exit_pressed:
1628-			print("[DIAG] _poll_controls: exit aircraft triggered")
1629-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3701-	"togglehud":    KEY_H,
3702-	"flightcheck":  KEY_G,
3703-	"restart":      KEY_R,
3704:	"j":            KEY_J,
3705-	"pause":        KEY_ESCAPE,
3706-}
3707-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1656-
1657-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1658-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1659:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1660-	# so it fires on the first FREEFALL _poll_controls() call.
1661-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1662-	# (general knowledge — not retrieved this session)
1663-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1664-		_headless_auto_deploy = false
1665-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1666:		_deploy_canopy()
1667-	if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed and _game_state == GameState.FREEFALL:
1668-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1669:		_deploy_canopy()
1670-
1671-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1672-		var turn_input := 0.0
--
1686-
1687-		_turn_input = turn_input
1688-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1689:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1690-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1691-# canopy could never accumulate a turn. Guard was commented out; call removed.
1692-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1705-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1706-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1707-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1708:			_deploy_canopy()
1709-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1710-	# Turn left (Q)
1711-	if Input.is_key_pressed(KEY_Q):
--
2126-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2127-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2128-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2129:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2130-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2131-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2132-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
2017-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2018-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2019-			#  by _0283.txt showing plane position update before stall)
2020:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2021-				if not ProjectSettings.has_setting("_headless_space_fired"):
2022-					ProjectSettings.set_setting("_headless_space_fired", true)
2023-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805214005 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805214525 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-	var _exit_pressed = (
1622:		Input.is_key_just_pressed(KEY_J)
1623:		or _headless_auto_jump
1624-	)
1625-		)
1626:		_headless_auto_jump = false  # consume the flag
1627-		if _exit_pressed:
1628-			print("[DIAG] _poll_controls: exit aircraft triggered")
1629-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3701-	"togglehud":    KEY_H,
3702-	"flightcheck":  KEY_G,
3703-	"restart":      KEY_R,
3704:	"j":            KEY_J,
3705-	"pause":        KEY_ESCAPE,
3706-}
3707-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1656-
1657-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1658-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1659:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1660-	# so it fires on the first FREEFALL _poll_controls() call.
1661-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1662-	# (general knowledge — not retrieved this session)
1663-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1664-		_headless_auto_deploy = false
1665-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1666:		_deploy_canopy()
1667-	if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed and _game_state == GameState.FREEFALL:
1668-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1669:		_deploy_canopy()
1670-
1671-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1672-		var turn_input := 0.0
--
1686-
1687-		_turn_input = turn_input
1688-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1689:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1690-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1691-# canopy could never accumulate a turn. Guard was commented out; call removed.
1692-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1705-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1706-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1707-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1708:			_deploy_canopy()
1709-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1710-	# Turn left (Q)
1711-	if Input.is_key_pressed(KEY_Q):
--
2126-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2127-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2128-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2129:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2130-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2131-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2132-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
2017-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2018-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2019-			#  by _0283.txt showing plane position update before stall)
2020:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2021-				if not ProjectSettings.has_setting("_headless_space_fired"):
2022-					ProjectSettings.set_setting("_headless_space_fired", true)
2023-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805214525 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805214612 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1622-	)
1623-	)
1624-		)
1625:		_headless_auto_jump = false  # consume the flag
1626-		if _exit_pressed:
1627-			print("[DIAG] _poll_controls: exit aircraft triggered")
1628-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3700-	"togglehud":    KEY_H,
3701-	"flightcheck":  KEY_G,
3702-	"restart":      KEY_R,
3703:	"j":            KEY_J,
3704-	"pause":        KEY_ESCAPE,
3705-}
3706-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1655-
1656-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1657-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1658:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1659-	# so it fires on the first FREEFALL _poll_controls() call.
1660-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1661-	# (general knowledge — not retrieved this session)
1662-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1663-		_headless_auto_deploy = false
1664-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1665:		_deploy_canopy()
1666-	if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed and _game_state == GameState.FREEFALL:
1667-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1668:		_deploy_canopy()
1669-
1670-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1671-		var turn_input := 0.0
--
1685-
1686-		_turn_input = turn_input
1687-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1688:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1689-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1690-# canopy could never accumulate a turn. Guard was commented out; call removed.
1691-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1704-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1705-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1706-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1707:			_deploy_canopy()
1708-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1709-	# Turn left (Q)
1710-	if Input.is_key_pressed(KEY_Q):
--
2125-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2126-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2127-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2128:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2129-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2130-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2131-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
2016-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2017-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2018-			#  by _0283.txt showing plane position update before stall)
2019:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2020-				if not ProjectSettings.has_setting("_headless_space_fired"):
2021-					ProjectSettings.set_setting("_headless_space_fired", true)
2022-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805214612 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805214833 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
3699-	"togglehud":    KEY_H,
3700-	"flightcheck":  KEY_G,
3701-	"restart":      KEY_R,
3702:	"j":            KEY_J,
3703-	"pause":        KEY_ESCAPE,
3704-}
3705-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1654-
1655-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1656-	# Headless auto-deploy: fired when _headless_auto_deploy set in IN_PLANE frame 1.
1657:	# _deploy_canopy() requires state==FREEFALL; this flag is consumed only here,
1658-	# so it fires on the first FREEFALL _poll_controls() call.
1659-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1660-	# (general knowledge — not retrieved this session)
1661-	if _headless_auto_deploy and _game_state == GameState.FREEFALL and not _canopy_deployed:
1662-		_headless_auto_deploy = false
1663-		print("[VERBATIM] Headless auto-deploy triggered (FREEFALL).")
1664:		_deploy_canopy()
1665-	if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed and _game_state == GameState.FREEFALL:
1666-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1667:		_deploy_canopy()
1668-
1669-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1670-		var turn_input := 0.0
--
1684-
1685-		_turn_input = turn_input
1686-		# ParachuteController removed — steering via _turn_input + _arm_pull directly.
1687:# FIX (fix_steering_v1): _deploy_canopy() here was called every frame in
1688-# OPENING_ANIM/DIAGNOSIS, resetting _canopy_heading=0 each frame so the
1689-# canopy could never accumulate a turn. Guard was commented out; call removed.
1690-		# Deploy on SPACE is handled by is_action_just_pressed at line ~1585.
--
1703-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1704-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1705-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1706:			_deploy_canopy()
1707-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1708-	# Turn left (Q)
1709-	if Input.is_key_pressed(KEY_Q):
--
2124-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2125-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2126-		# p2: the three _hud_labels writes that used to live here ran ONLY in
2127:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
2128-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
2129-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
2130-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
2015-			# Headless auto-start: fire SPACE on first IN_PLANE frame
2016-			# (Rule #1 grounded: this is the first rendered frame, confirmed
2017-			#  by _0283.txt showing plane position update before stall)
2018:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
2019-				if not ProjectSettings.has_setting("_headless_space_fired"):
2020-					ProjectSettings.set_setting("_headless_space_fired", true)
2021-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805214833 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805214939 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-	# reliable across _physics_process/_process frame boundary).
1620-	# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-	var _exit_pressed = (
1622:		Input.is_key_just_pressed(KEY_J)
1623:		or _headless_auto_jump
1624-	)
1625-	if _exit_pressed:
1626-		print("[DIAG] _poll_controls: exit aircraft triggered")
--
3517-	"togglehud":    KEY_H,
3518-	"flightcheck":  KEY_G,
3519-	"restart":      KEY_R,
3520:	"j":            KEY_J,
3521-	"pause":        KEY_ESCAPE,
3522-}
3523-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1942-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1943-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1944-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1945:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1946-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1947-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1948-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1833-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1834-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1835-			#  by _0283.txt showing plane position update before stall)
1836:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1837-				if not ProjectSettings.has_setting("_headless_space_fired"):
1838-					ProjectSettings.set_setting("_headless_space_fired", true)
1839-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805214939 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805215757 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622:			Input.is_key_just_pressed(KEY_J)
1623:			or _headless_auto_jump
1624-		)
1625-		if _exit_pressed:
1626-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3517-	"togglehud":    KEY_H,
3518-	"flightcheck":  KEY_G,
3519-	"restart":      KEY_R,
3520:	"j":            KEY_J,
3521-	"pause":        KEY_ESCAPE,
3522-}
3523-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1942-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1943-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1944-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1945:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1946-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1947-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1948-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1833-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1834-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1835-			#  by _0283.txt showing plane position update before stall)
1836:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1837-				if not ProjectSettings.has_setting("_headless_space_fired"):
1838-					ProjectSettings.set_setting("_headless_space_fired", true)
1839-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805215757 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805215951 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622:			Input.is_key_just_pressed(KEY_J)
1623:			or _headless_auto_jump
1624-		)
1625-		if _exit_pressed:
1626-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3517-	"togglehud":    KEY_H,
3518-	"flightcheck":  KEY_G,
3519-	"restart":      KEY_R,
3520:	"j":            KEY_J,
3521-	"pause":        KEY_ESCAPE,
3522-}
3523-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1942-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1943-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1944-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1945:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1946-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1947-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1948-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1833-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1834-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1835-			#  by _0283.txt showing plane position update before stall)
1836:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1837-				if not ProjectSettings.has_setting("_headless_space_fired"):
1838-					ProjectSettings.set_setting("_headless_space_fired", true)
1839-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805215951 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805220521 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622:			Input.is_key_pressed(KEY_J)
1623:			or _headless_auto_jump
1624-		)
1625-		if _exit_pressed:
1626-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3517-	"togglehud":    KEY_H,
3518-	"flightcheck":  KEY_G,
3519-	"restart":      KEY_R,
3520:	"j":            KEY_J,
3521-	"pause":        KEY_ESCAPE,
3522-}
3523-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1942-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1943-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1944-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1945:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1946-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1947-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1948-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1833-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1834-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1835-			#  by _0283.txt showing plane position update before stall)
1836:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1837-				if not ProjectSettings.has_setting("_headless_space_fired"):
1838-					ProjectSettings.set_setting("_headless_space_fired", true)
1839-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805220521 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805220632 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622:			Input.is_key_just_pressed(KEY_J)
1623:			or _headless_auto_jump
1624-		)
1625-		if _exit_pressed:
1626-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3517-	"togglehud":    KEY_H,
3518-	"flightcheck":  KEY_G,
3519-	"restart":      KEY_R,
3520:	"j":            KEY_J,
3521-	"pause":        KEY_ESCAPE,
3522-}
3523-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1942-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1943-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1944-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1945:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1946-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1947-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1948-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1833-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1834-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1835-			#  by _0283.txt showing plane position update before stall)
1836:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1837-				if not ProjectSettings.has_setting("_headless_space_fired"):
1838-					ProjectSettings.set_setting("_headless_space_fired", true)
1839-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805220632 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805223514 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622:			Input.is_key_pressed(KEY_J)
1623:			or _headless_auto_jump
1624-		)
1625-		if _exit_pressed:
1626-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3517-	"togglehud":    KEY_H,
3518-	"flightcheck":  KEY_G,
3519-	"restart":      KEY_R,
3520:	"j":            KEY_J,
3521-	"pause":        KEY_ESCAPE,
3522-}
3523-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1942-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1943-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1944-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1945:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1946-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1947-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1948-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1833-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1834-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1835-			#  by _0283.txt showing plane position update before stall)
1836:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1837-				if not ProjectSettings.has_setting("_headless_space_fired"):
1838-					ProjectSettings.set_setting("_headless_space_fired", true)
1839-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805223514 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805223818 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622:			Input.is_key_pressed(KEY_J)
1623:			or _headless_auto_jump
1624-		)
1625-		if _exit_pressed:
1626-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3517-	"togglehud":    KEY_H,
3518-	"flightcheck":  KEY_G,
3519-	"restart":      KEY_R,
3520:	"j":            KEY_J,
3521-	"pause":        KEY_ESCAPE,
3522-}
3523-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1942-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1943-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1944-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1945:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1946-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1947-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1948-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1833-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1834-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1835-			#  by _0283.txt showing plane position update before stall)
1836:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1837-				if not ProjectSettings.has_setting("_headless_space_fired"):
1838-					ProjectSettings.set_setting("_headless_space_fired", true)
1839-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805223818 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805224914 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622:			Input.is_key_pressed(KEY_J)
1623:			or _headless_auto_jump
1624-		)
1625-		if _exit_pressed:
1626-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3517-	"togglehud":    KEY_H,
3518-	"flightcheck":  KEY_G,
3519-	"restart":      KEY_R,
3520:	"j":            KEY_J,
3521-	"pause":        KEY_ESCAPE,
3522-}
3523-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1942-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1943-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1944-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1945:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1946-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1947-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1948-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1833-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1834-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1835-			#  by _0283.txt showing plane position update before stall)
1836:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1837-				if not ProjectSettings.has_setting("_headless_space_fired"):
1838-					ProjectSettings.set_setting("_headless_space_fired", true)
1839-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805224914 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805230413 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622:			Input.is_key_pressed(KEY_J)
1623:			or _headless_auto_jump
1624-		)
1625-		if _exit_pressed:
1626-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3517-	"togglehud":    KEY_H,
3518-	"flightcheck":  KEY_G,
3519-	"restart":      KEY_R,
3520:	"j":            KEY_J,
3521-	"pause":        KEY_ESCAPE,
3522-}
3523-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1942-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1943-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1944-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1945:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1946-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1947-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1948-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1833-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1834-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1835-			#  by _0283.txt showing plane position update before stall)
1836:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1837-				if not ProjectSettings.has_setting("_headless_space_fired"):
1838-					ProjectSettings.set_setting("_headless_space_fired", true)
1839-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805230413 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805230904 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622:			Input.is_key_pressed(KEY_J)
1623:			or _headless_auto_jump
1624-		)
1625-		if _exit_pressed:
1626-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3517-	"togglehud":    KEY_H,
3518-	"flightcheck":  KEY_G,
3519-	"restart":      KEY_R,
3520:	"j":            KEY_J,
3521-	"pause":        KEY_ESCAPE,
3522-}
3523-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1942-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1943-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1944-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1945:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1946-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1947-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1948-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1833-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1834-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1835-			#  by _0283.txt showing plane position update before stall)
1836:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1837-				if not ProjectSettings.has_setting("_headless_space_fired"):
1838-					ProjectSettings.set_setting("_headless_space_fired", true)
1839-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805230904 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805233109 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622:			Input.is_key_pressed(KEY_J)
1623:			or _headless_auto_jump
1624-		)
1625-		if _exit_pressed:
1626-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3519-	"togglehud":    KEY_H,
3520-	"flightcheck":  KEY_G,
3521-	"restart":      KEY_R,
3522:	"j":            KEY_J,
3523-	"pause":        KEY_ESCAPE,
3524-}
3525-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1944-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1945-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1946-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1947:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1948-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1949-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1950-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1835-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1836-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1837-			#  by _0283.txt showing plane position update before stall)
1838:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1839-				if not ProjectSettings.has_setting("_headless_space_fired"):
1840-					ProjectSettings.set_setting("_headless_space_fired", true)
1841-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805233109 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805233442 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622:			Input.is_key_pressed(KEY_J)
1623:			or _headless_auto_jump
1624-		)
1625-		if _exit_pressed:
1626-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3519-	"togglehud":    KEY_H,
3520-	"flightcheck":  KEY_G,
3521-	"restart":      KEY_R,
3522:	"j":            KEY_J,
3523-	"pause":        KEY_ESCAPE,
3524-}
3525-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1944-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1945-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1946-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1947:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1948-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1949-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1950-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1835-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1836-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1837-			#  by _0283.txt showing plane position update before stall)
1838:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1839-				if not ProjectSettings.has_setting("_headless_space_fired"):
1840-					ProjectSettings.set_setting("_headless_space_fired", true)
1841-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805233442 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805233801 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1619-		# reliable across _physics_process/_process frame boundary).
1620-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1621-		var _exit_pressed = (
1622:			Input.is_key_pressed(KEY_J)
1623:			or _headless_auto_jump
1624-		)
1625-		if _exit_pressed:
1626-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3519-	"togglehud":    KEY_H,
3520-	"flightcheck":  KEY_G,
3521-	"restart":      KEY_R,
3522:	"j":            KEY_J,
3523-	"pause":        KEY_ESCAPE,
3524-}
3525-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1944-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1945-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1946-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1947:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1948-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1949-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1950-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1835-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1836-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1837-			#  by _0283.txt showing plane position update before stall)
1838:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1839-				if not ProjectSettings.has_setting("_headless_space_fired"):
1840-					ProjectSettings.set_setting("_headless_space_fired", true)
1841-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805233801 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805234119 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1627-		# reliable across _physics_process/_process frame boundary).
1628-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1629-		var _exit_pressed = (
1630:			Input.is_key_pressed(KEY_J)
1631:			or _headless_auto_jump
1632-		)
1633-		if _exit_pressed:
1634-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3527-	"togglehud":    KEY_H,
3528-	"flightcheck":  KEY_G,
3529-	"restart":      KEY_R,
3530:	"j":            KEY_J,
3531-	"pause":        KEY_ESCAPE,
3532-}
3533-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1617-		if _game_state == GameState.FREEFALL:
1618-		if Input.is_action_just_pressed("deploy"):
1619-			print("[VERBATIM] Deploy triggered from _poll_controls")
1620:			_deploy_canopy()
1621-		if Input.is_action_just_pressed("turnleft"):
1622-			_turn_left()
1623-		if Input.is_action_just_pressed("turnright"):
--
1952-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1953-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1954-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1955:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1956-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1957-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1958-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1843-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1844-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1845-			#  by _0283.txt showing plane position update before stall)
1846:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1847-				if not ProjectSettings.has_setting("_headless_space_fired"):
1848-					ProjectSettings.set_setting("_headless_space_fired", true)
1849-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805234119 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805234744 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1627-		# reliable across _physics_process/_process frame boundary).
1628-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1629-		var _exit_pressed = (
1630:			Input.is_key_pressed(KEY_J)
1631:			or _headless_auto_jump
1632-		)
1633-		if _exit_pressed:
1634-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3527-	"togglehud":    KEY_H,
3528-	"flightcheck":  KEY_G,
3529-	"restart":      KEY_R,
3530:	"j":            KEY_J,
3531-	"pause":        KEY_ESCAPE,
3532-}
3533-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1617-		if _game_state == GameState.FREEFALL:
1618-		if Input.is_action_just_pressed("deploy"):
1619-			print("[VERBATIM] Deploy triggered from _poll_controls")
1620:			_deploy_canopy()
1621-		if Input.is_action_just_pressed("turnleft"):
1622-			_turn_left()
1623-		if Input.is_action_just_pressed("turnright"):
--
1952-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1953-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1954-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1955:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1956-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1957-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1958-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1843-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1844-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1845-			#  by _0283.txt showing plane position update before stall)
1846:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1847-				if not ProjectSettings.has_setting("_headless_space_fired"):
1848-					ProjectSettings.set_setting("_headless_space_fired", true)
1849-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805234744 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805234941 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1627-		# reliable across _physics_process/_process frame boundary).
1628-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1629-		var _exit_pressed = (
1630:			Input.is_key_pressed(KEY_J)
1631:			or _headless_auto_jump
1632-		)
1633-		if _exit_pressed:
1634-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3527-	"togglehud":    KEY_H,
3528-	"flightcheck":  KEY_G,
3529-	"restart":      KEY_R,
3530:	"j":            KEY_J,
3531-	"pause":        KEY_ESCAPE,
3532-}
3533-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1617-		if _game_state == GameState.FREEFALL:
1618-		if Input.is_action_just_pressed("deploy"):
1619-			print("[VERBATIM] Deploy triggered from _poll_controls")
1620:			_deploy_canopy()
1621-		if Input.is_action_just_pressed("turnleft"):
1622-			_turn_left()
1623-		if Input.is_action_just_pressed("turnright"):
--
1952-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1953-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1954-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1955:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1956-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1957-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1958-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1843-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1844-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1845-			#  by _0283.txt showing plane position update before stall)
1846:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1847-				if not ProjectSettings.has_setting("_headless_space_fired"):
1848-					ProjectSettings.set_setting("_headless_space_fired", true)
1849-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805234941 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.20260805235103 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _headless_auto_deploy: bool = false  # set when headless jump fires; consumed in FREEFALL _poll_controls
130-var _deployment_timer
131-var _screenshot_save_timer: float = 0.0
--
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559-	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
563-	_check_arm_pose_safe()
--
1627-		# reliable across _physics_process/_process frame boundary).
1628-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1629-		var _exit_pressed = (
1630:			Input.is_key_pressed(KEY_J)
1631:			or _headless_auto_jump
1632-		)
1633-		if _exit_pressed:
1634-			print("[DIAG] _poll_controls: exit aircraft triggered")
--
3527-	"togglehud":    KEY_H,
3528-	"flightcheck":  KEY_G,
3529-	"restart":      KEY_R,
3530:	"j":            KEY_J,
3531-	"pause":        KEY_ESCAPE,
3532-}
3533-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
861-	return null
862-
863-
864:func _deploy_canopy() -> void:
865-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
866-	if _canopy_deployed or not _canopy_instance:
867-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1422-	if current_time >= event["time"]:
1423-		match event["action"]:
1424-			"deploy":
1425:				_deploy_canopy()
1426-			"cutaway":
1427-				_do_cutaway()
1428-			"reserve":
--
1617-		if _game_state == GameState.FREEFALL:
1618-		if Input.is_action_just_pressed("deploy"):
1619-			print("[VERBATIM] Deploy triggered from _poll_controls")
1620:			_deploy_canopy()
1621-		if Input.is_action_just_pressed("turnleft"):
1622-			_turn_left()
1623-		if Input.is_action_just_pressed("turnright"):
--
1952-			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
1953-			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
1954-		# p2: the three _hud_labels writes that used to live here ran ONLY in
1955:		# FREEFALL, so the HUD froze the instant _deploy_canopy() moved state
1956-		# to OPENING_ANIM. Screenshots 11:24:47-11:25:57 showed the altimeter
1957-		# panel falling 5142->4175 ft while HUD Label0 stayed at 6044 ft.
1958-		# They are now in _update_hud_readouts(), called unconditionally from

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
550-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
551-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
552-	# Headless auto‑start: simulate SPACE press
553:	if OS.get_environment("GODOT_HEADLESS") == "1":
554-		Input.action_press("ui_accept")
555-		Input.action_release("ui_accept")
556-		print("[VERBATIM] Headless auto‑start triggered.")
557-	# Headless auto-start: simulate SPACE/deploy press
558-	# Ref: OS.get_environment (general knowledge, not retrieved this session)
559:	if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
560-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
561-		# Input.action_release("deploy") removed — flag-based now
562-		print("[VERBATIM] Headless auto‑start triggered.")
--
581-	# autostall's 30 s stall detector cannot trip on it.
582-	# Ref: https://docs.godotengine.org/en/stable/classes/class_scenetree.html
583-	# (general knowledge - not retrieved this session)
584:	if OS.get_environment("GODOT_HEADLESS") == "1" \
585-			or "--headless" in OS.get_cmdline_args():
586-		var _pause_timer := Timer.new()
587-		_pause_timer.wait_time = 8.0
--
1843-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1844-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1845-			#  by _0283.txt showing plane position update before stall)
1846:			if OS.get_environment("GODOT_HEADLESS") == "1" or "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1847-				if not ProjectSettings.has_setting("_headless_space_fired"):
1848-					ProjectSettings.set_setting("_headless_space_fired", true)
1849-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.20260805235103 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.keys.20260805204027 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
550-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
551-	# Headless auto‑start: simulate SPACE press
552-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
553:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
554-		# Input.action_release("deploy") removed — flag-based now
555-		print("[VERBATIM] Headless auto‑start triggered.")
556-	_check_arm_pose_safe()
--
1554-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1555-		var _exit_pressed = (
1556-			Input.is_action_just_pressed("deploy")
1557:			or Input.is_key_pressed(KEY_J)
1558:			or _headless_auto_jump
1559-		)
1560:		_headless_auto_jump = false  # consume the flag
1561-		if _exit_pressed:
1562-			print("[DIAG] _poll_controls: exit aircraft triggered")
1563-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3605-	"togglehud":    KEY_H,
3606-	"flightcheck":  KEY_G,
3607-	"restart":      KEY_R,
3608:	"j":            KEY_J,
3609-	"pause":        KEY_ESCAPE,
3610-}
3611-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
825-	return null
826-
827-
828:func _deploy_canopy() -> void:
829-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
830-	if _canopy_deployed or not _canopy_instance:
831-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1360-	if current_time >= event["time"]:
1361-		match event["action"]:
1362-			"deploy":
1363:				_deploy_canopy()
1364-			"cutaway":
1365-				_do_cutaway()
1366-			"reserve":
--
1591-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1592-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1593-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1594:		_deploy_canopy()
1595-
1596-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1597-		var turn_input := 0.0
--
1629-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1630-			else:
1631-				print("[STEER_TELEM] /root/Main does not exist")
1632:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1633-		# ----- Direct key checks (bypass Input Map) -----
1634-		# Deploy (SPACE)
1635-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1636:		_deploy_canopy()
1637-	# Turn left (Q)
1638-	if Input.is_key_pressed(KEY_Q):
1639-		_turn_input = -1.0
--
1664-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1665-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1666-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1667:			_deploy_canopy()
1668-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1669-	# Turn left (Q)
1670-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
549-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
550-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
551-	# Headless auto‑start: simulate SPACE press
552:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
553-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
554-		# Input.action_release("deploy") removed — flag-based now
555-		print("[VERBATIM] Headless auto‑start triggered.")
--
1971-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1972-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1973-			#  by _0283.txt showing plane position update before stall)
1974:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1975-				if not ProjectSettings.has_setting("_headless_space_fired"):
1976-					ProjectSettings.set_setting("_headless_space_fired", true)
1977-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.keys.20260805204027 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.tabs_fix.20260805213203 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3563-	"togglehud":    KEY_H,
3564-	"flightcheck":  KEY_G,
3565-	"restart":      KEY_R,
3566:	"j":            KEY_J,
3567-	"pause":        KEY_ESCAPE,
3568-}
3569-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.tabs_fix.20260805213203 ---


--- FILE: godot_project/scripts/build_terrain.gd.bak.telemetry.20260805200331 ---

## 1. Exit logic (KEY_J / _headless_auto_jump)
125-var _canopy_instance: Node3D
126-var _canopy_material: StandardMaterial3D
127-var _canopy_deployed: bool = false
128:var _headless_auto_jump: bool = false  # set by IN_PLANE headless patch; consumed in _poll_controls
129-var _deployment_timer
130-var _screenshot_save_timer: float = 0.0
131-var _camera_save_timer := 0.0
--
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548-	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549:		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
552-	_check_arm_pose_safe()
--
1550-		# Ref: https://docs.godotengine.org/en/stable/classes/class_input.html
1551-		var _exit_pressed = (
1552-			Input.is_action_just_pressed("deploy")
1553:			or Input.is_key_pressed(KEY_J)
1554:			or _headless_auto_jump
1555-		)
1556:		_headless_auto_jump = false  # consume the flag
1557-		if _exit_pressed:
1558-			print("[DIAG] _poll_controls: exit aircraft triggered")
1559-			print("[VERBATIM] EXIT AIRCRAFT - transitioning FREEFALL")
--
3601-	"togglehud":    KEY_H,
3602-	"flightcheck":  KEY_G,
3603-	"restart":      KEY_R,
3604:	"j":            KEY_J,
3605-	"pause":        KEY_ESCAPE,
3606-}
3607-

## 2. Deploy handler ([CTRL] pressed=deploy)
  (not found)

## 3. _deploy_canopy() calls (excluding definition)
821-	return null
822-
823-
824:func _deploy_canopy() -> void:
825-	print("[VERBATIM] ENTER _deploy_canopy gate=none")
826-	if _canopy_deployed or not _canopy_instance:
827-		print("[VERBATIM] EXIT _deploy_canopy early=already_deployed_or_no_canopy")
--
1356-	if current_time >= event["time"]:
1357-		match event["action"]:
1358-			"deploy":
1359:				_deploy_canopy()
1360-			"cutaway":
1361-				_do_cutaway()
1362-			"reserve":
--
1587-	print("[VERBATIM] POLL: checking deploy state=", _game_state, " canopy=", _canopy_deployed)
1588-	if Input.is_action_just_pressed("deploy") and not _canopy_deployed:
1589-		print("[VERBATIM] POLL: deploy pressed - calling _deploy_canopy")
1590:		_deploy_canopy()
1591-
1592-	if _game_state == GameState.DIAGNOSIS or _game_state == GameState.OPENING_ANIM:
1593-		var turn_input := 0.0
--
1625-				print("[STEER_TELEM] /root/Main children: ", _children_str)
1626-			else:
1627-				print("[STEER_TELEM] /root/Main does not exist")
1628:# BUG1 FIX: guard restored — was firing _deploy_canopy() every frame unconditionally
1629-		# ----- Direct key checks (bypass Input Map) -----
1630-		# Deploy (SPACE)
1631-		#if Input.is_key_just_pressed(KEY_SPACE) and not _canopy_deployed:
1632:		_deploy_canopy()
1633-	# Turn left (Q)
1634-	if Input.is_key_pressed(KEY_Q):
1635-		_turn_input = -1.0
--
1660-				_canopy_instance.scale = Vector3(0.18, 0.12, 0.18)
1661-				_show_notification("WARNING: Canopy deployed inside aircraft! DO NOT jump (J)")
1662-		elif _game_state == GameState.FREEFALL and not _canopy_deployed:
1663:			_deploy_canopy()
1664-	_last_frame_keys["SPACE"] = Input.is_key_pressed(KEY_SPACE)
1665-	# Turn left (Q)
1666-	if Input.is_key_pressed(KEY_Q):

## 4. Parse error indicators (Parse Error, Expected statement, Unexpected Indent)
  (none found)

## 5. Headless detection (GODOT_HEADLESS / OS.has_feature)
545-	print("[VERBATIM] Initial malfunction: ", _malfunction_name())
546-	print("[VERBATIM] Game ready – press SPACE at ~4000 ft to deploy")
547-	# Headless auto‑start: simulate SPACE press
548:	if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
549-		_headless_auto_jump = true  # timing-safe flag; checked in _poll_controls (_process)
550-		# Input.action_release("deploy") removed — flag-based now
551-		print("[VERBATIM] Headless auto‑start triggered.")
--
1967-			# Headless auto-start: fire SPACE on first IN_PLANE frame
1968-			# (Rule #1 grounded: this is the first rendered frame, confirmed
1969-			#  by _0283.txt showing plane position update before stall)
1970:			if "--headless" in OS.get_cmdline_args():  # was: GODOT_HEADLESS env var — set by autostall in ALL runs. Now requires actual --headless CLI flag (not set by autostall). Restores _0036 behavior: user sees plane, presses SPACE/J manually. Ref: https://docs.godotengine.org/en/stable/classes/class_os.html
1971-				if not ProjectSettings.has_setting("_headless_space_fired"):
1972-					ProjectSettings.set_setting("_headless_space_fired", true)
1973-					Input.action_press("deploy")

--- END OF godot_project/scripts/build_terrain.gd.bak.telemetry.20260805200331 ---


=== END AUDIT ===
