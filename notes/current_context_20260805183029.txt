=== show_current_context.sh — 20260805183029 UTC ===

=== Lines 2060-2070 (with line numbers) ===
2060: 		var turn_dir = Vector3.RIGHT * _turn_input * _turn_force
2061: 		_velocity_vec += turn_dir * delta
2062: 		var forward_vec = target_dir * _forward_speed
2063: 		_velocity_vec = _velocity_vec.move_toward(forward_vec, _accel * delta)
2064: 		_character.position += _velocity_vec * delta
2065: 		if _velocity_vec.length() > 0.5:
2066: 			var angle = atan2(_velocity_vec.x, _velocity_vec.z)
2067: 			# _character.rotation = Vector3(0, angle, 0)  # removed to match plane behaviour
2068: 		var speed_kts = _forward_speed * 1.94384
2069: if _hud_labels.size() > 0: 		_hud_labels[1].text = "SPD: %.0f kts | VARIO: %+.1f m/s" % [speed_kts, _vario_mps]
2070: 	if _hud_labels.size() > 0: 		_hud_labels[4].text = "TURN: %d" % (_turn_input * 100)

=== gdparse error (first 10 lines) ===
/home/owner/Documents/69f7bcc6-1f68-83ea-b9b2-95a4db8629ac/parachute-cfd-game/godot_project/scripts/build_terrain.gd:

if _hud_labels.size() > 0: 		_hud_labels
^

Unexpected token Token('IF', 'if') at line 2069, column 1.
Expected one of: 
	* CONST
	* _NL
	* EXTENDS

=== End of diagnostic ===
